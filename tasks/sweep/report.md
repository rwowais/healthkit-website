# Live persona sweep — 76 confirmed issues (16 personas + 8 critic scenarios, adversarially verified)

## Live recon (prod data shape)
- [low/inconsistency] 9 cms_publications exist; the live app serves bundle_version 9 (id 9, 2026-05-25), 7 protocols / 42 behaviors — count(*)=9. ORDER BY bundle_version DESC LIMIT 1 → id=9, bundle_version=9, checksum 8cd2beea, schema=1, generatedAt 2026-05-25T19:18:32Z, n_protocols=7, total_behaviors=42. NB the table has NO `version` column — the monotonic version lives in `bundle_version` (int col) and bundle->>'version'; activePacks()/applyPublishedBundle() in knowledge.ts select by bundle.version, so 'latest' = highest bundle_version = id 9. (v5 and v9 share checksum 8cd2beea — v9 is a content-identical re-publish of v5.)
- [high/data-risk] Raw published bundle (v9) is uniformly THIN: 0/42 behaviors carry ANY of the 7 BehaviorDef metadata fields — jsonb key-presence over all 42 behaviors: category 0/42, daysActive 0/42, contraindications 0/42, timeWindow 0/42, evidenceTier 0/42, targets 0/42, derivedFrom 0/42 (also intensity 0/42, timingReason 0/42, recommendedBy 0/42). The ONLY keys present are anchor/block/canonicalKey/icon/kind/leverage/offsetMin/rationale/title (42/42) + dose (30/42). This matches knowledge.ts:141-149 comment: cms_behaviors has no columns for code-only structural/safety metadata, so the whole bundle ships without it. The runtime is therefore 100% dependent on the load-time heal (healProtocols, knowledge.ts:174-190) for category/daysActive/contraindications/timeWindow/evidenceTier/targets/derivedFrom on every behavior it serves.
- [high/data-risk] 6 bundle canonicalKeys have NO code-atom match → heal cannot fire → they STAY thin (real risk) — Heal index is built ONLY from PACKS behaviors + STANDALONE_ATOMS_REGISTRY keyed by exact canonicalKey (knowledge.ts:164-172), and the overlay is `idx.get(b.canonicalKey)` — exact-equality only, NO derivedFrom fallback (knowledge.ts:179). Grepping all 31 unique bundle keys against packs.ts (`canonicalKey: "<key>"`, and confirmed via substring + single-quote check): 6 have ZERO match → (1) low-dose-melatonin-paqa, (2) melatonin-e699, (3) coq10-ubiquinone-tfgh, (4) test-1-1-6h48, (5) test-1-2-6om2, (6) test-1-3-6wzy. The other 25 keys all match (some 2x — harmless, index is first-wins). So 6/42 behaviors across 3 protocols (better-sleep, daily-essentials, test-protocol-1-61e2) serve permanently thin with no category/daysActive/contraindications/timeWindow/evidenceTier/targets.
- [medium/data-risk] Bundle ships a `test-protocol-1-61e2` protocol with 3 placeholder behaviors to live users — Latest bundle protocols include pack_id 'test-protocol-1-61e2' with behaviors test-1-1-6h48 / test-1-2-6om2 / test-1-3-6wzy. These are the same 3 keys with no code-atom match (above). Title/rationale come from CMS but they are obvious scaffolding ('test-1-1' etc.). Present since the 42-behavior bundles (v4 onward, 'New protocol added').
- [low/data-risk] Heal is exact-key only with no derivedFrom fallback — any future CMS key drift silently degrades, not errors — healProtocols does `idx.get(b.canonicalKey)`; if undefined it returns the behavior untouched (knowledge.ts:182 `if (!base) return b;`). There is no secondary lookup by derivedFrom, no warning, no telemetry. The melatonin/coq10 cases prove this fails silently — a CMS author renaming or AI-generating a key that doesn't exactly equal a code atom produces a permanently-thin live behavior with zero signal.
- [high/data-risk] Two live users finished onboarding with NO reverse trial ever stamped — silently free forever, unrecoverable — protocolize_state: user 141ea373-39f9-4dbd-8496-3aded394778f and 8644247d-6dde-4b18-b414-c84730103d4d both have settings.completedOnboarding='true', tier='free', and NO premiumTrialEndsAt key (jsonb ? 'premiumTrialEndsAt' = false). getAccess() (entitlements.ts:60-88) skips the whole `if (endIso)` block when premiumTrialEndsAt is undefined → returns premium=false, inTrial=false, trialExpired=false, trialDaysLeft=0. maybeExtendTrial (entitlements.ts:95-122) early-returns on `!premiumTrialEndsAt`, so it can never heal them. getDefaultSettings() (storage.ts:140-151) does NOT include premiumTrialEndsAt and normalize() backfills only from defaults, so there is no self-heal on load. The trial is stamped in exactly one place (onboarding/page.tsx:311-313); the re-tune `redo` branch (line 306-314) deliberately skips it.
- [low/ux-confusion] adapt() mode override can be decoupled from banner copy only via deliberately contradictory CMS free-text (not by mode-only rules) — Latest bundle v9 has adaptationRules=[] (cms_publications: bundle->'adaptationRules' is empty; cms_adaptation_rules table has 0 rows), so prod runs the pure hardcoded baseline. The 'mode-only decouples banner' risk is already mitigated: adapt() (engine.ts:1172-1185) fills headline/tone from MODE_DEFAULT_COPY[mode] when a rule sets a new mode but supplies no copy, and Today feeds that SAME adaptation.mode into both shapeTimeline (today/page.tsx:588) and the banner (today/page.tsx:1313/1318). The only residual vector: sanitizeEffect (cms/rules.ts:196-208) accepts free-text tone/headline (length-clamped only) with no coherence check against setMode, so an admin could publish setMode='primed' with tone='take it easy' — banner would contradict the shaped (full-intensity) timeline.
- [low/inconsistency] Bundle integrity checksum is a weak 32-bit non-crypto hash with a live collision (v5 == v9) — cms_publications: bundle_version 5 and 9 share checksum '8cd2beea' despite different payloads (v9 config={AHA_DAYS:4}, schema_version field=9; v5 differs). bundleChecksum (knowledge.ts:90-93) is `h = (h*31 + charCode)|0` folded to 32-bit. fetchAndApplyPublished (publish.ts:739-750) only uses it as a tamper check (recompute === stored) and gates application by version monotonicity (applyPublishedBundle, knowledge.ts:286-290 requires b.version > current), so the collision does not cause a wrong bundle to load.
- [low/safety] SECURITY DEFINER RPCs (cms_is_admin, cms_resolve_email, delete_my_account) executable by anon/authenticated — get_advisors(security): cms_is_admin() callable by anon AND authenticated as SECURITY DEFINER via /rest/v1/rpc; cms_resolve_email(text) and delete_my_account() callable by authenticated. Also: auth leaked-password protection (HaveIBeenPwned) is disabled. The in-scope data tables are NOT affected: pg_policies confirms protocolize_state ('own row') and protocolize_logs ('own logs') are own-row for cmd=ALL with both qual and with_check = (auth.uid() = user_id); push_subscriptions has per-cmd own-row policies.
- [high/data-risk] Live published bundle (v9) ships a junk "Test Protocol 1" to every online user — cms_publications max bundle_version=9 (the bundle the runtime applies). Query of v9 protocols returns id=test-protocol-1-61e2, source=custom, 3 behaviors: keys test-1-1-6h48/test-1-2-6om2/test-1-3-6wzy, titles 'Test 1.1/1.2/1.3', rationale 'Custom behavior.', has_days=false, has_timewindow=false. In CMS this protocol is now status=archived, but publications are immutable snapshots; assembleBundleFromCMS only filters archived at *assemble* time, so v9 still carries it. knowledge.ts activePacks() returns [...healed bundle protocols, ...builtin extras] with NO source/status filter — every bundle protocol becomes a live, installable pack.
- [high/data-risk] cms_behaviors lacks every safety/scheduling column the publish backfill depends on (16 columns present, ~11 critical ones absent) — information_schema columns for cms_behaviors = [id, canonical_key, title, block, anchor, offset_min, dose, leverage, kind, icon, rationale, status, version, updated_at, updated_by, ai_unverified] — 16 total. It has NO columns for contraindications, daysActive, evidenceTier, derivedFrom, targets, evidence, timingReason, recommendedBy, customTime, category, intensity, timeWindow. authoring.ts backfillBuiltinFields() re-attaches these from the code atom (matched by canonicalKey) at assemble time; knowledge.ts healProtocols() re-attaches HEAL_FIELDS at load time for older thin bundles.
- [medium/data-risk] v9 actually exercises the missing-column gap for daily-essentials supplements (shipped without contraindications) — v9 daily-essentials has 5 behaviors; querying their jsonb shows has_contra=false and has_tier=false for ALL of them (vitamin-d3, omega-3, creatine, magnesium-pm, coq10-ubiquinone-tfgh). v9 was published 2026-05-25, before the publish-time backfill/heal existed. For the 4 keys that exist in code (vitamin-d3/omega-3/creatine/magnesium-pm) knowledge.ts healProtocols() now self-repairs contraindications at LOAD time (HEAL_FIELDS includes 'contraindications'). But coq10-ubiquinone-tfgh has no code atom AND is status=draft, so it isn't even in the live runtime via assemble — though it IS frozen into the v9 snapshot.
- [low/inconsistency] Only 1 of 26 cms_interactions is published; it references canonicalKeys that DO exist in code (resolves correctly) — cms_interactions by status: draft=25, published=1. The single published row: a_key=alcohol-cutoff, b_key=strength, type=synergy, severity=soft, evidence_tier=emerging, source_verified_at=2026-05-31 (stamped, so it passes the assemble gate at authoring.ts:1707 'evidence_tier && !source_verified_at → skip'), nudge is a full non-empty sentence, source is a PMC URL. Both keys exist in src/lib/packs.ts: canonicalKey "strength" (lines 449, 960) and canonicalKey "alcohol-cutoff" (line 1299). So validateBundleGovernance (publish.ts:670) would NOT flag it as inert.
- [medium/nonsense-but-works] No published bundle currently contains interactions — the next publish silently activates the runtime interaction engine for the first time — jsonb_array_length(bundle->'interactions') is NULL for every publication v1..v9 (all predate the interactions field; latest v9 is 2026-05-25, the cms_interactions table/feature came later per tasks #159-162). assembleBundleFromCMS now emits interactions, so the FIRST new publish jumps the live bundle from 0 interactions to 1 (alcohol-cutoff×strength synergy), changing runtime behavior (Up Next tiebreak / calm block notes per engine Phase 4-5).
- [medium/ux-confusion] daily-essentials is the one CMS 'official' protocol with no matching code PACK — free-pack-cap edge — CMS official+published protocols: better-sleep, blood-sugar, burnout-recovery, daily-essentials, deep-focus, longevity-foundation. Code PACKS ids (19) include better-sleep/blood-sugar/burnout-recovery/deep-focus/longevity-foundation but NOT daily-essentials (comm -23 of the two lists yields exactly 'daily-essentials'). protocols/page.tsx free-cap = officialInstalledCount >= getFreePacks() where officialPackIds = catalog.filter(p=>p.source==='official'). daily-essentials only enters the catalog via the published bundle (activePacks merges it in as source='official'), so when v9 (or any future bundle) is live it counts toward the 3-pack free cap; with NO bundle applied (offline/local-only) it doesn't exist at all.

## HIGH (9)
### [HIGH] (inconsistency) Weekly review tells the burnout-recovery user to "tighten Strength training — your highest-leverage gap", the exact behavior the no-intense rule is resting
- Persona: power-user-conflicts
- Area: src/lib/intel.ts weeklyReview() focus (828-853) + continuity (862-906); src/app/insights/page.tsx:372
- Evidence: weeklyReview() computes focus from `essentials = items.filter(i=>i.leverage===3)` over analyticsItems (intel.ts:829), picks the essential with the fewest completions (focusCount<tracked.length), and emits the 'weekly-focus-tighten' copy: "Next week, tighten one thing: \"{title}\". It's your highest-leverage gap." (intel.ts:843-849). Strength is leverage 3 (packs.ts:461,972), conflict-muted, hence 0 completions ⇒ it's always the weakest essential ⇒ always selected as the focus. There is no conflictMutedKeys / `.muted` filter here (unlike suggestions/Focus chip). The same unfiltered essentials drive the 'continuity' line (intel.ts:867-905), which will keep reporting strength as 'still light'. insights/page.tsx:372 renders review.focus unconditionally when review exists (free = time-delayed, premium = live).
- Why: The coach explicitly instructs the user to do more of the one thing their own active protocol forbids. It also poisons the 'rebalancing/holding' continuity narrative the weekly review is built around. Same root cause as the report-card finding: the Insights narrative layer was never taught about firm conflict mutes.
- Fix: Filter conflictMutedKeys(state) out of the `essentials` set used for both `focus` and `continuity` in weeklyReview() (mirror intel.ts:553). A behavior the engine is actively suppressing must not be eligible as the week's 'highest-leverage gap'.
- Verify: CONFIRMED OPEN. weeklyReview() builds essentials with NO conflict-mute filter: intel.ts:829 `const essentials = items.filter((i) => i.leverage === 3)` over analyticsItems(state) (intel.ts:787). analyticsItems (intel.ts:50-62) compiles raw compileTimeline per weekday and dedupes by canonicalKey — it never runs shapeTimeline/applyConflictMutes, so a conflict-muted strength behavior IS present in `items`. The conflictMutedKeys(state) helper exists (intel.ts:74-90, gated on muteReason startsWith "conflict pair:") and is correctly used in suggestions (intel.ts:546,553) and keystone (intel.ts:647) — but it is NOT referenced anywhere inside weeklyReview() (which begins at intel.ts:750). Strength is leverage 3 (packs.ts:461 and packs.ts:972, both intensity:"high"). For a burnout-recovery user it is firm-conflict muted via CONFLICT_PAIRS {restraint:"no-intense", target:"strength"} (engine.ts:1330) → applyConflictMutes sets muted+muteReason "conflict pair:..." (engine.ts:1454-1463), so it is non-completable and tracked completions = 0. The focus loop (intel.ts:832-840) picks the essential with the fewest completions; 0 always wins, and 0 < tracked.length (≥4) fires the 'weekly-focus-tighten' branch (intel.ts:842-849): "Next week, tighten one thing: \"Strength training\". It's your highest-leverage gap." The same unfiltered `essentials` is reused for continuity (intel.ts:867), so strength is also last week's min and nowC(0) > pMin(0) is false → emits 'continuity-light' "still light" (intel.ts:894-904). Both `focus` and `continuity` are always returned (intel.ts:953-962) and `review.focus` renders unconditionally at insights/page.tsx:372. The coach instructs the user to do more of exactly what their active protocol mutes. This precondition (Burnout Recovery + a strength-bearing pack installed together) is the named persona; the mute requires a curated active restraint (engine.ts:1431), satisfied in that config. Not in the ALREADY-FIXED intel/insights list (those were overclaim/consistency/correlation/unit fixes) and not in KNOWN-DEFERRED. Same root cause as the report-card mute-blindness finding.
### [HIGH] (data-risk) A future-dated daily log silently zeroes the streak AND flips the day to "rebuild / away 366 days" — reachable by a westward traveler
- Persona: traveler-tz
- Area: src/lib/scoring.ts (calculateStreak head check) + src/lib/engine.ts (getSignals gap loop) + src/lib/storage.ts (normalize)
- Evidence: normalize() clamps future-dated BIOMARKERS to today (storage.ts:421-423: `b.date > today ? {...b, date: today} : b`) but does NOT clamp dailyLogs — it only sorts them ascending (storage.ts:418-420). The same comment that justifies the biomarker clamp says a future date "poisons every band/sparkline/engine read" (storage.ts:182). Two engine readers then break on a future-dated log: (1) calculateStreak's head check accepts ONLY today / yesterday / today-2 / vacation as the head (scoring.ts:127-132); a log dated today+1 is none of these → returns 0 (scoring.ts:133-135). (2) getSignals builds `active` filtering only `l.date !== tKey` (engine.ts:865-867), so `lastKey` can be a FUTURE date; the gap loop steps the cursor BACKWARD from today and breaks on `cursor === lastKey` (engine.ts:872-879) — which never matches a future key, so gapDays runs to the 366 cap. adapt() then takes the rebuild branch (engine.ts:1005 `gapDays>=2 && hasHistory`) and emits "You were away 366 days — easing back in" (engine.ts:1011-1020). I PROVED both with the real exported functions on a user active every day incl. today plus one 2025-06-10 log (today=2025-06-09): calculateStreak 6→0, and getSignals gapDays=366 / adapt.mode='rebuild' / reason 'You were away 366 days'. Reachable two ways: (a) westward TimezoneSentry Update — while stored tz (e.g. Asia/Tokyo) has already rolled to the next calendar day, the Today board shows that next day [selectedDate = dateKeyInTz(tz), today/page.tsx:531] and any completion writes it via toggleBehavior→getOrCreateLog; then tapping Update→America/Los_Angeles rewinds today behind that log. (b) Multi-device: a device whose tz is ahead writes a log keyed to its later local day (storage.ts:64 dateKeyInTz), and mergeStates unions logs by date with NO future-date filter (datasource.ts:403-408), propagating the ahead log to the behind device.
- Why: This is the exact traveler-tz failure mode the tz work was meant to prevent: a user who has logged faithfully every single day loses their entire streak to 0 and gets demoted into 'welcome back / easing in' essentials mode the moment one log lands on a calendar day ahead of their (rewound or behind) local today. It is silent, data-driven, and self-inflicted by the app's own day-key + Update flow.
- Fix: Clamp future-dated daily logs in normalize() exactly as biomarkers are: in the dailyLogs map, drop or re-key any log with `date > today` (today = getDateString(undefined, getTz(s.settings))). Belt-and-suspenders: in getSignals, exclude `l.date > tKey` from `active` (engine.ts:866) so a stray future log can't run gapDays to the cap; and in calculateStreak, ignore head logs with `date > todayStr` (or treat them as today) so the streak isn't zeroed. The normalize clamp is the root fix; the two reader guards prevent recurrence from any other future-log source.
- Verify: CONFIRMED real and open, verified by reading the cited lines AND by executing the real exported functions. Code reads: storage.ts:421-423 clamps future BIOMARKERS to today but storage.ts:418-420 only SORTS dailyLogs (no clamp); the justifying comment at storage.ts:182 says a future date "poisons every band/sparkline/engine read." The write path has no guard: toggleBehavior (storage.ts:680-709) -> getOrCreateLog (597-601) -> putDayLog (610-616) persists whatever date key it's given verbatim, and the Today board key follows the STORED tz (today/page.tsx:531 selectedDate=addDaysToKey(dateKeyInTz(tz),-offset)), so a tz ahead-of-now writes a future-dated log. mergeStates (datasource.ts:403-408) unions logs by date with NO future filter, giving the multi-device vector. Two engine readers break: calculateStreak head check accepts only today/yesterday/today-2/vacation (scoring.ts:127-132) and returns 0 otherwise (133-135); getSignals builds active filtering only l.date!==tKey (engine.ts:865-867) so lastKey can be future, and the gap loop steps cursor BACK from today and breaks on cursor===lastKey (872-879) which never matches a future key -> gapDays hits the 366 cap; baselineAdapt then takes gapDays>=2 && hasHistory -> rebuild (engine.ts:1005) with reason "You were away ${gapDays} days" (1020). EMPIRICAL PROOF via the real getDefaultState/toggleBehavior/calculateStreak/getSignals/adapt with system time frozen to 2025-06-09 (tz America/Los_Angeles), user active 2025-06-04..09 plus one 2025-06-10 log: futureLogStored=true, streakNoFuture=6 vs streakWithFuture=0, gapDays=366, and public adapt() returned mode="rebuild" / reason="You were away 366 days — easing back in" — exactly as claimed. NOT in the already-fixed list (that covers biomarker future-date healing only, not dailyLogs) and not intended behavior. Minor caveat: rebuild requires hasHistory (a scored prior log, engine.ts:858) — trivially true for any genuinely-active user; with score-0 synthetic logs the mode stays "normal" but the streak still collapses to 0, so the data-risk holds regardless.
### [HIGH] (data-risk) Supplement-only days never count as activity: streak, weekly goal, freeze bank and Insights countdown all stay frozen at zero despite perfect adherence
- Persona: supplement-only
- Area: src/lib/scoring.ts (hasAnyActivity) + src/lib/storage.ts (toggleSupplement/bulkCheckSupplements/setSupplementsSkipped)
- Evidence: hasAnyActivity (scoring.ts:238-264) detects supplements ONLY via the LEGACY model — line 244: `const hasSupplements = log.supplementEntries.some((s) => s.taken || s.skipped)`. It never inspects the live model fields `supplementCompletions` (Record<string,boolean>) or `supplementSkips` (string[]). Meanwhile the only supplement mutators — toggleSupplement (storage.ts:1006-1060), bulkCheckSupplements (1067-1104), setSupplementsSkipped (1113-1129) — write ONLY supplementCompletions/supplementSkips and never call recalculate(), so log.score also stays 0. I proved this by executing the real functions: for a state with installedPacks=[] and one curated supplement taken today, supplementCompletions={"magnesium-pm":true} but log.score=0 and hasAnyActivity=false; a 5-day perfect supplement streak yields calculateStreak(...)=0 and weeklyActiveDays(...)=0. calculateStreak (scoring.ts:90) and weeklyActiveDays (scoring.ts:179) both filter on hasAnyActivity; freezeStatus.activeTotal (scoring.ts:227) and the Insights loggedDays countdown (insights/page.tsx:165-170, gated on score>0||behaviorCompletions) use the same gate. Today's liveStreak (today/page.tsx:271-274) and Insights streak (insights/page.tsx:90) both call calculateStreak. So a user who only takes supplements sees their streak flame never appear (it needs >=2), the WeeklyGoal ring (components/today/WeeklyGoal.tsx:18) stuck at 0/N forever, and the cold-start '~N check-ins to go' never advancing.
- Why: This is the assigned persona's entire loop. The app supports a supplement-only user (Browse → add → take daily, and as of this session a supplement-only day even reaches 'Day complete'), but the single most-visible reward — the streak — and the weekly-goal progress are computed from a model that can't see supplement completions at all. The user does exactly what the product asks every day and is silently told (via a 0 streak and an empty ring) that they've done nothing. It also corrupts downstream stats (freeze allowance, milestones that key off active days).
- Fix: Make hasAnyActivity see the live supplement model: in scoring.ts add `const hasSuppNew = Object.values(log.supplementCompletions ?? {}).some(Boolean) || (log.supplementSkips?.length ?? 0) > 0;` and include it in the final OR (alongside the existing hasSupplements legacy check). Lowest-risk, single-function change; no migration needed. (Optionally also have the supplement mutators stamp a recalc so log.score reflects supplement adherence, but the hasAnyActivity fix alone restores streak/weeklyActiveDays/freeze/countdown.) Add a regression test asserting calculateStreak>0 for a behavior-less state with N consecutive supplement-taken days.
- Verify: Verified end-to-end against live code. (1) hasAnyActivity (src/lib/scoring.ts:238-264) detects supplements ONLY via the legacy model at line 244 `log.supplementEntries.some((s) => s.taken || s.skipped)`; it never inspects supplementCompletions or supplementSkips. (2) The three live mutators write only the live model and never recalculate: toggleSupplement (storage.ts:1053-1059) and bulkCheckSupplements (1097-1103) write supplementCompletions/supplementSkips and `return { ...state, dailyLogs, supplements }` with no saveDailyLog/recalculate; setSupplementsSkipped (1126-1128) likewise. (3) Even if they did recalc, score is blind to supplements: recalculate (storage.ts:1375) and saveDailyLog (1439) set score = computeBehaviorScore(state, date, behaviorCompletions) (642-678), which scores only the compileTimeline behavior set keyed by canonicalKey — and engine.ts:216 explicitly excludes supplements ("Supplements have their own surface"). (4) supplementEntries is only ever populated from the legacy protocols.supplements pillar (storage.ts:111-125) and a separate itemId mutator (1530-1544); the Browse→add flow (today/page.tsx:2865 calls toggleSupplement) never writes supplementEntries, so there is no back-fill that would rescue hasAnyActivity. (5) All cited gates use hasAnyActivity: calculateStreak (scoring.ts:90), weeklyActiveDays (179, used by WeeklyGoal.tsx:18), freezeStatus.activeTotal (227); Insights streak (insights/page.tsx:90) calls calculateStreak and the cold-start countdown (insights/page.tsx:165-169) gates on `l.score > 0 || behaviorCompletions` — both stay 0/empty for a supplement-only day. So a supplement-only persona gets streak=0, weekly ring 0/N, freeze bank 0, and a non-advancing countdown despite perfect adherence. Note: the session's "supplement-only day completes" fix (today/page.tsx:743-786 suppProgToday/dayComplete) only fixes the on-screen Day-complete banner; it does NOT touch hasAnyActivity or any scoring gate, so this is a genuinely open, distinct issue (and arguably worsened: the day reads 'complete' while every reward stays at zero).
### [HIGH] (bug) Swap-replacement workout silently vanishes (and scores 0) when the day is in recovery/essentials/rebuild mode
- Persona: workout-swapper
- Area: src/lib/engine.ts shapeTimeline (recovery branch 1743-1753, essentials 1714-1718, rebuild 1811-1819) vs applySwaps (481-511); pipeline at src/app/today/page.tsx:587-588 and src/lib/storage.ts:655-659  ⚠️ REGRESSION (recent commit)
- Evidence: applySwaps runs BEFORE shapeTimeline and injects the replacement with muted:false + swappedFrom (engine.ts:501-510). shapeTimeline then rebuilds `muted` from scratch per-mode with NO awareness of swappedFrom: recovery mutes anything in RECOVERY_DEMOTE={strength,zone2,vo2max-intervals} (1745-1751); essentials/rebuild mute by leverage. Runnable probe (default install, Tue 2026-06-09 where zone2 is today's workout): poor check-in (sleepQuality=1,energy=1) → adapt() mode='recovery'; swapBehavior(zone2→strength); after applySwaps+shapeTimeline the replacement strength has muted=true, muteReason='recovery mode: training/cognitive demands eased', swappedFrom='zone2', and ZERO workouts are visible (visibleWorkouts=[]); computeBehaviorScore=0 even though behaviorCompletions={strength:true}. Today hides muted rows in a collapsed 'Resting' section (page.tsx:2078 `items.filter(i=>!i.muted||showOpt)`), so the user's chosen workout — and the Undo button that lives on that row (page.tsx:2756) — disappear from the board. Same vanish for essentials/rebuild + swap to extended-walk/tabata (leverage<3).
- Why: The persona taps 'Swap workout', picks the workout they actually did, and the entire workout disappears from Today while the score stays flat — it looks like the swap deleted their training and silently didn't count it. The recovery banner explicitly invites this ('Keep anything that still feels right'), so swapping to a still-trained option on a low-recovery day is an expected user action, not an edge case. Undo is buried in a collapsed section, so recovery feels broken too.
- Fix: Make the swap markers authoritative over the mode passes. After shapeTimeline (or as a guard inside it, mirroring the keystone-protection block at engine.ts:1843-1849), force any item with swappedFrom set back to muted:false/muteReason:undefined, and keep any item with swappedTo set muted:true. Cleanest: add a final pass in shapeTimeline `shaped = shaped.map(it => it.swappedFrom ? {...it, muted:false, muteReason:undefined} : it.swappedTo ? {...it, muted:true} : it)`. The user explicitly chose this replacement for today — adaptation modes must not re-mute it.
- Verify: CONFIRMED real and currently open. Full chain verified against live code: (1) applySwaps runs before shapeTimeline in both render (today/page.tsx:587-588) and score (storage.ts:655-659). (2) applySwaps injects the replacement with muted:false + swappedFrom (engine.ts:501-510). (3) shapeTimeline recovery branch sets muted = RECOVERY_DEMOTE.has(effectiveKey(it)) with ZERO swappedFrom awareness (engine.ts:1743-1753); RECOVERY_DEMOTE = {strength, zone2, deep-work, vo2max-intervals} (engine.ts:1291-1299); effectiveKey of plain strength = "strength" (engine.ts:1197-1199). I read the whole function (1706-1880) — no swap exemption exists, and guaranteePerBlock's recovery predicate explicitly refuses to resurrect RECOVERY_DEMOTE items (engine.ts:1766). (4) strength is a genuinely offered swap target: WORKOUT_KEYS includes it (workouts.ts:47), availableWorkoutAlternatives surfaces it (workouts.ts:71-95) into the swap sheet (today/page.tsx:3061-3067); default install longevity-foundation+better-sleep (packs.ts:1388) carries both zone2 and strength. So swapping zone2->strength on a recovery day is reachable on a default install. (5) Today hides muted rows via items.filter(i=>!i.muted||showOpt) (today/page.tsx:2078); the Undo button is gated on it.swappedFrom on that same row (today/page.tsx:2756-2773), so it vanishes too. (6) computeBehaviorScore does active = planned.filter(i=>!i.muted) (storage.ts:673), so the muted replacement is excluded from both numerator and denominator — behaviorCompletions[strength]=true counts for nothing. Essentials (engine.ts:1714-1718, leverage<3) and rebuild (engine.ts:1811-1819, top-3 only) re-mute low-leverage replacements (e.g. extended-walk) identically — neither reads swappedFrom. NOT already-fixed: this session moved swaps before shaping, which is precisely what creates this for RECOVERY_DEMOTE replacements; the injured-athlete persona test (injured-athlete.test.ts:469-479) covers swap+recovery but uses REPLACEMENT_KEY=extended-walk (not a RECOVERY_DEMOTE key), so it never trips this path and no test guards it. Severity high: a logged, user-chosen workout silently disappears and scores 0 on an explicitly-invited action (recovery banner "Keep anything that still feels right"), with the Undo affordance buried in a collapsed section.
### [HIGH] (bug) Swapped-away original workout resurrects as an active to-do (with its Swap chip back) in lighter/essentials mode — including the persona's own easier-swap path
- Persona: workout-swapper
- Area: src/lib/engine.ts shapeTimeline lighter branch (1820-1829) + essentials branch (1714-1718); chip gating src/app/today/page.tsx:2738-2755  ⚠️ REGRESSION (recent commit)
- Evidence: applySwaps marks the original muted:true + swappedTo (engine.ts:484-489). The lighter branch does `it.leverage===1 ? mute : {...it, muted:false, muteReason:undefined}` (1820-1829) and essentials does the same for leverage<3 (1714-1718) — both unconditionally UN-mute the swapped-away original. Probe (swap zone2→extended-walk, default install): mode='lighter' → original zone2 has muted=FALSE but swappedTo='extended-walk' still set; mode='essentials' → zone2 muted=FALSE and the replacement extended-walk muted=TRUE (worst case: original back, replacement gone). Critically this fires on the persona's INTENDED action: swapping to an easier workout makes easierDayFromSwap (workouts.ts:127-140) true, which adapt() turns into mode='lighter' (engine.ts:1067-1073) — the same pass that resurrects the original. On Today the un-muted zone2 renders as a normal active workout (clock+dose, page.tsx:2700-2703) and its 'Swap workout' chip reappears because the gate only excludes swappedFrom, not swappedTo (page.tsx:2743). Block-complete counts zone2 as undone (visibleItems include it, page.tsx:2092-2095), so the user can never reach 'day complete'.
- Why: The persona swaps strength/zone2 for an easier workout (the headline use case). Instead of the original leaving, it pops back onto the board as an unchecked workout they will never complete (they did the easier one), permanently blocking day-complete and inflating the denominator — while the replacement shows done. It reads as 'the swap didn't take', and offering the Swap chip again on a row that's already been swapped is incoherent.
- Fix: Same root fix as the replacement-vanish bug: after the mode passes, re-assert swap state — any item with swappedTo must stay muted:true regardless of mode/leverage, and exclude swappedTo items from the leverage-based un-mute in the essentials/lighter/rebuild branches. Also harden the chip gate at page.tsx:2738-2743 to require `!it.swappedTo` (defense in depth) so a swapped-away original never re-offers Swap.
- Verify: CONFIRMED OPEN. The full chain holds against live code. (1) Ordering — the crux — is real: today/page.tsx:587-588 calls applySwaps THEN shapeTimeline (comment at 583: "Apply per-day workout swaps BEFORE shapeTimeline"; engine.ts:462 says the same). (2) applySwaps sets the swapped-away original muted:true + swappedTo (engine.ts:484-489). (3) The lighter branch un-mutes any leverage!==1 item via `{...it, muted:false, muteReason:undefined}` with NO swappedTo guard (engine.ts:1821-1828); essentials does the same for leverage<3 (engine.ts:1714-1717). (4) No later pass in shapeTimeline re-mutes a swappedTo item — read the full tail (keystone 1843-1849, applyConflictMutes 1862, mastery 1866-1877); none key off swappedTo, and swappedTo is never cleared in the render path (grep: zero hits in today/page.tsx). (5) The persona's intended easier-swap triggers this: easierDayFromSwap true on moderate/high->low (workouts.ts:127-140) -> adapt returns mode:"lighter" (engine.ts:1067-1073). Verified leverage/intensity: zone2=leverage3/moderate (packs.ts:443,446), extended-walk=leverage2/low (packs.ts:2466,2487), strength=leverage3/high (packs.ts:461,464) — all leverage!==1, so all get un-muted in lighter. (6) Chip gate is `!it.muted && !done && !it.swappedFrom` (page.tsx:2738-2743) — excludes swappedFrom only, so the resurrected original (has swappedTo, lacks swappedFrom) re-shows "Swap workout". (7) visibleItems=items.filter(!muted) (page.tsx:2070); baseItems=visibleItems drives behaviorDone/blockTotal (2092-2095, 2115) and fullyDone (2124) — the un-muted never-done original permanently blocks day-complete and inflates the denominator while the replacement shows done. isWorkoutBehavior(zone2/strength)=true (workouts.ts:60-63, WORKOUT_KEYS). The only existing test (storage.test.ts:692) asserts mode selection only, NOT the original's muted state — so this is unguarded, not intended. This is the exact inverse of the recovery-mode resurrection guard (recovery-demote.test.ts; guaranteePerBlock predicate at engine.ts:1764-1766) that lighter/essentials lack. Not in the ALREADY-FIXED list.
### [HIGH] (data-risk) Two guest tabs silently last-write-wins each other's check-ins (no cross-tab sync at all)
- Persona: two-tabs-same-browser
- Area: src/lib/datasource.ts (LocalDataSource.save) + src/lib/storage.ts (saveState) + src/hooks/useAppState.ts (sync listener)
- Evidence: LocalDataSource.save() (datasource.ts:45-52) does `saveState(state)` then `window.dispatchEvent(new CustomEvent("pz:state"))`. saveState (storage.ts:573-586) is a wholesale `localStorage.setItem(STORAGE_KEY, JSON.stringify(state))` — it overwrites the ENTIRE document with the in-memory snapshot and never merges against what is currently on disk. toggleBehavior (storage.ts:680-722) is a pure in-memory transform on the snapshot captured at tab-open `load()`; putDayLog replaces the whole day object. The only cross-instance signal is the in-page CustomEvent `pz:state`, which is same-document only and does NOT propagate to other browser tabs. Verified by grep: there is ZERO `addEventListener("storage"...)` and ZERO `BroadcastChannel` anywhere in src/ (search returned empty). useAppState's resync listener (useAppState.ts:160-184) only subscribes to STATE_EVENT/`focus`/`visibilitychange` — none of which fire in tab A when tab B writes localStorage. Concrete clobber for a guest: Tab A and Tab B both load identical state at open → Tab A checks behavior X → disk now has {X done} → Tab B (never learned of X) checks behavior Y → its saveState writes {Y done, X NOT done} over disk → Tab A's check-in X is permanently gone, and the day's `score` (storage.ts:707) silently reverts. Nothing notifies either tab; tab A keeps showing X done (stale memory) until its next reload, when X has vanished.
- Why: This persona's exact scenario. A user with two open tabs (common: Today open in one, Protocols/Insights in another, or just a duplicated tab) loses check-ins with no warning and no error banner — StorageSafetyNet (StorageSafetyNet.tsx) only fires on save *failures*, never on a cross-tab overwrite. The cross-device `updated_at` guard in SupabaseDataSource does NOT exist on the local/guest path, so guests are completely unprotected. Lost habit completions are the core value of the app, so silent loss is severe.
- Fix: Add a cross-tab listener in useAppState's resync effect (useAppState.ts:160-184): `window.addEventListener("storage", e => { if (e.key === STORAGE_KEY) sync(); })` so tab A re-loads when tab B writes the shared key (the native `storage` event fires in OTHER tabs only — exactly the missing signal). For true safety against the interleaved-write clobber, also make LocalDataSource.save() merge against the current on-disk state before writing — e.g. `saveState(mergeStates(loadState(), state))` (mergeStates already exists, datasource.ts:402, and unions per-day behaviorCompletions) — or gate writes behind a BroadcastChannel-coordinated re-read. The `storage`-event resync alone closes the visible-staleness window; the merge-on-save closes the true interleaved-write data loss.
- Verify: CONFIRMED REAL and currently-open. Every link in the claim chain verified against live code:

1. saveState (storage.ts:573-586) is a wholesale `localStorage.setItem(STORAGE_KEY, JSON.stringify(state))` with NO read-merge against disk — it overwrites the entire document with the in-memory snapshot.
2. toggleBehavior (storage.ts:680-722) is a pure in-memory transform on the passed `state` (via getOrCreateLog + putDayLog), never re-reading disk; `score` set at line 707 exactly as claimed. putDayLog (storage.ts:610-616) inserts-or-replaces the whole day object.
3. LocalDataSource.save (datasource.ts:45-52) does saveState(state) then dispatches `new CustomEvent("pz:state")` — a same-document-only event.
4. Cross-tab sync is ABSENT: grep for addEventListener("storage"...) and BroadcastChannel across src/ returns empty (exit 1). The only "storage" hit is a comment string in StorageSafetyNet.tsx, not a listener.
5. useAppState's resync (useAppState.ts:160-184) subscribes ONLY to STATE_EVENT (pz:state), `focus`, `visibilitychange` (lines 176-178) — none fire in tab A when tab B writes localStorage. The save/flush path (useAppState.ts:129) writes the in-memory snapshot directly with no disk re-read.
6. StorageSafetyNet (StorageSafetyNet.tsx:45) listens only for SAVE_ERROR_EVENT (save *failures*), never a silent cross-tab overwrite.
7. The cross-device updated_at concurrency guard exists ONLY in SupabaseDataSource.save (datasource.ts:834-847); LocalDataSource has no equivalent, so guests are unprotected. supabaseEnabled (supabase.ts:19) is false with no env, making LocalDataSource (datasource.ts:928-930) the default path.

Clobber confirmed: Tab A toggles X → disk={X done}; Tab B (stale snapshot, never heard pz:state) toggles Y → saveState({Y done, X NOT done}) overwrites disk → X permanently lost; Tab A keeps showing X done from stale memory until its next focus/visibility/refresh re-read, when X has vanished. NOT in the 'already fixed this session' list (those touch the Supabase conflict gate, client-clock baseline, and engine/UI — not cross-tab/saveState/LocalDataSource). High-severity silent data loss of habit completions, the app's core value.
### [HIGH] (data-risk) Restore-from-backup is a silent wholesale overwrite — every day tracked since the backup is erased, with merge helpers sitting unused right beside it
- Persona: import-backup-overwrites
- Area: src/app/profile/page.tsx:114-129 (doImport) → src/lib/storage.ts:1364 (importState) → src/lib/datasource.ts:45-52 & 850-855 (save)
- Evidence: doImport (profile/page.tsx:117) does `const parsed = importState(...)` then line 124 `await activeDataSource.save(parsed)` and line 125 `toast.show("Data restored")` with NO merge, NO confirm. importState (storage.ts:1364-1366) is just `return parseState(raw)` → normalize() (storage.ts:179) which produces a COMPLETE replacement AppState (dailyLogs/biomarkers/settings all from the file). Both save() implementations replace, not merge: LocalDataSource.save (datasource.ts:45-46) → saveState → `localStorage.setItem(STORAGE_KEY, JSON.stringify(state))` (storage.ts:576) overwrites the whole blob; SupabaseDataSource.save (datasource.ts:850-855) → `sb.from(STATE_TABLE).upsert({ user_id, state, ... })` replaces the whole cloud row. Meanwhile mergeStates (datasource.ts:402) + mergeDailyLog (datasource.ts:218) implement a full non-destructive union (logs unioned by date with field-level merge; biomarkers by id; customPacks/supplements/settings reconciled) and are already exported + used by chooseCloudLoad (datasource.ts:531) and resolveConflict (datasource.ts:551). doImport never calls them.
- Why: A user restoring a week-old export to recover one lost setting silently destroys every day, biomarker reading, reflection, streak day and supplement log they recorded AFTER that export — and it propagates to the cloud (line 124 comment explicitly says it pushes so 'the old cloud row [doesn't] win', i.e. it overwrites the authoritative copy too). The toast 'Data restored' frames a destructive replace as a benign recovery; the user has no idea they just deleted newer data until a reload (line 126) shows it gone. This is the single highest-stakes data path in the app and it's the one with no confirm and no merge, despite a battle-tested merger being one import away.
- Fix: Default Import to a MERGE: load the current state, `mergeStates(parsed, current)` (or mergeStates(current, parsed) chosen so the import is the cloud/base side), then save the union — nothing recorded since the backup is lost, and a genuinely older backup just contributes any days the current state is missing. If a true overwrite is ever wanted, make it an explicit second choice. At minimum, gate doImport behind the same confirm Sheet that Reset uses (see next finding) with copy that states it replaces current data and offers Export-first.
- Verify: CONFIRMED open. doImport (src/app/profile/page.tsx:117,124-126) does parsed = importState(...) then await activeDataSource.save(parsed) + toast "Data restored" + reload — no merge, no confirm. importState (src/lib/storage.ts:1364-1366) = parseState → parseState (storage.ts:458-466) JSON.parses and returns normalize(parsed); normalize (storage.ts:179-180) builds a COMPLETE AppState from getDefaultState() reconciled with the file's fields — it does not read or merge current state, so it is a full replacement. Both saves replace, not merge: LocalDataSource.save (src/lib/datasource.ts:45-46) → saveState(state) overwrites the whole blob; SupabaseDataSource.save (datasource.ts:850-855) → sb.from(STATE_TABLE).upsert({ user_id, state, updated_at }) replaces the whole cloud row (comment at profile/page.tsx:122-123 confirms it intentionally pushes so the old cloud row can't win). A non-destructive merger mergeStates (datasource.ts:402-431) — logs unioned by date via mergeDailyLog, biomarkers/customPacks by id, settings goals by id — is exported and already used by chooseCloudLoad (datasource.ts:531) and resolveConflict (datasource.ts:551), but doImport never calls it. The import button (profile/page.tsx:840-856) invokes doImport directly on file pick with no gate, whereas "Reset all data" (line 859) uses setConfirmReset and delete-account requires typing DELETE — so the most destructive data path is the only one without a confirm. Restoring a week-old export therefore silently erases every day/biomarker/reflection/log recorded since the export and propagates the loss to the authoritative cloud row, mislabeled "Data restored". Not in the ALREADY-FIXED data list (conflict-gate/deleteAccount/reset/sync-baseline/biomarker-heal — none touch doImport) and not KNOWN-DEFERRED. Severity high: highest-stakes data path, no confirm, no merge, misleading copy, and a non-technical owner per project profile; caveat — it is a manual user-initiated action behind a file picker, not an automatic/background overwrite.
### [HIGH] (data-risk) First-sign-in conflict gate skipped for a packs-only guest → cloud silently wins, losing the guest's install/remove choices
- Persona: guest-packs-only-merge
- Area: src/lib/datasource.ts — hasMeaningfulData() (183-191) gating the genuineFirstSignIn check (740-753); fall-through to chooseCloudLoad (763)
- Evidence: hasMeaningfulData() (datasource.ts:183-191) returns true ONLY for dailyLogs / biomarkers / customPacks / supplements / behaviorOverrides — it deliberately omits installedPacks. The conflict gate is `hasMeaningfulData(local) && slicesDiffer(local, cloud)` (line 744-745), so when a guest's ONLY divergence is installedPacks, hasMeaningfulData is false, `genuineFirstSignIn` is false, and execution falls through to line 755 markReconciled(userId) then line 763 chooseCloudLoad(local, cloud, dirty). dirty = hasPendingSync() (line 756) is FALSE for this user because the guest's last save took the guest branch in save() (line 822-828: `if (!userId) { clearPendingSync(); ... return; }`) — guests never set the pending-sync flag. chooseCloudLoad(local, cloud, false) returns `cloud` verbatim (line 531), so the guest's installedPacks set is overwritten by the cloud account's. This is a REAL, default path: getDefaultState() (storage.ts:154-173) seeds dailyLogs:[], biomarkers:[], customPacks:[], behaviorOverrides:{}, NO top-level supplements field, and installedPacks:[...DEFAULT_INSTALLED] — so a guest who only curates packs (onboarding writes ONLY installedPacks: onboarding/page.tsx:318-323; or Library install/remove) has hasMeaningfulData()===false while installedPacks legitimately diverges. The author's own slicesDiffer() (line 198-199) DOES include installedPacks, proving the intent was to treat pack divergence as conflict-worthy — the two predicates disagree and hasMeaningfulData (first && operand) wins. Net: guest installs/removes packs, signs into a populated account, and their curated pack set vanishes with no prompt.
- Why: A guest who curates protocol packs (the app's core onboarding action) and then creates/signs into an account that already has packs loses every install/remove choice silently — the exact data-loss class the 2026-06-09 conflict-gate fix was meant to close for supplements/customPacks/overrides, but installedPacks was left out of hasMeaningfulData. It's first-impression data loss on the single most common guest→account transition, and markReconciled (line 755) makes it permanent: the conflict can never re-raise for this device+account afterward.
- Fix: Add an installedPacks divergence check to the gate so it matches slicesDiffer's intent. Minimal: in hasMeaningfulData add a non-default installedPacks signal, OR (cleaner) gate on `(hasMeaningfulData(local) || installedPacksDiverge(local, cloud)) && slicesDiffer(local, cloud)`. Best: gate purely on slicesDiffer(local, cloud) plus a cheap 'local isn't a pristine default' guard — hasMeaningfulData and slicesDiffer should agree on which slices count. Whatever the form, a guest whose installedPacks differs from the cloud account must reach the conflict prompt (pendingConflict / CONFLICT_EVENT, line 748-752) instead of falling through to cloud-wins. mergeStates already unions installedPacks (line 490-493), so 'merge' resolves correctly once the prompt fires.
- Verify: CONFIRMED REAL and currently-open. The two conflict predicates genuinely disagree on installedPacks, and the gating one (hasMeaningfulData) wins, dropping a packs-only guest through to silent cloud-wins.

Verified chain in live code:
- datasource.ts:183-191 hasMeaningfulData() returns true ONLY for dailyLogs/biomarkers/customPacks/supplements/behaviorOverrides — installedPacks is omitted.
- datasource.ts:193-208 slicesDiffer() DOES compare installedPacks (lines 198-199). So the two predicates disagree exactly on installedPacks.
- datasource.ts:740-745 genuineFirstSignIn = !conflictHandled && !conflictEvaluated && !isReconciled && hasMeaningfulData(local) && slicesDiffer(local,cloud). hasMeaningfulData is a required && operand and is the FIRST data gate, so when it is false the prompt is skipped regardless of slicesDiffer.
- A packs-only guest lands with supplements:[], dailyLogs:[], biomarkers:[], customPacks:[], behaviorOverrides:{} → hasMeaningfulData()===false. Confirmed seeding: storage.ts getDefaultState() (154-173) seeds installedPacks:[...DEFAULT_INSTALLED] (line 169) and NO top-level supplements field; normalize() (storage.ts:345-348, 434) sets supplements from s.supplements only, defaulting to [] — it never injects the default protocol's supplements. DEFAULT_INSTALLED = ["longevity-foundation","better-sleep"] (packs.ts:1388); onboarding writes ONLY installedPacks (onboarding/page.tsx:318-322), so a guest who curates packs diverges on installedPacks alone.
- Fall-through: line 755 markReconciled(userId) (permanent for this device+account), line 756 dirty=hasPendingSync(). Guest save took the !userId branch (datasource.ts:823-828) which calls clearPendingSync(), so dirty===false. Line 763 chooseCloudLoad(local,cloud,false) returns cloud verbatim (line 531). The dirty merge path (mergeStates, lines 490-493) WOULD have unioned installedPacks, but it is never reached.

Test suite corroborates the gap, not a guard: src/lib/__tests__/data-safety-merge.test.ts:84-91 pins hasMeaningfulData true for supplements and behaviorOverrides but has NO installedPacks case; line 59 comment states the prior gate "compared only logs/biomarkers/installedPacks," confirming the 2026-06-09 fix added supplements/customPacks/overrides to slicesDiffer AND hasMeaningfulData but left installedPacks out of hasMeaningfulData. qa-fixes.test.ts:488 confirms chooseCloudLoad(local,cloud,false) === cloud (exact overwrite). No other guard exists between datasource.ts:732 and 765.

Not in the 'already fixed this session' list (that item covers slicesCfg/slicesDiffer seeing config slices — the SECOND operand — already expanded; the residual gap is hasMeaningfulData/installedPacks). Not in KNOWN-DEFERRED. The sibling audit finding (tasks/full-audit-2026-06-09.md:43-45) explicitly recommended hasMeaningfulData also count non-log slices; installedPacks was left uncovered.

Severity calibration note (still high): the loss requires signing into a PRE-EXISTING populated cloud account whose installedPacks differ — a brand-new account hits the no-cloud-row branch (datasource.ts:776-794) and lifts local up with no loss. So the finding's "single most common transition" framing is slightly overstated (new-account signup is safe), but the hit case (returning user / reinstall who curated as guest then signs into their old account) is real, silent, and made permanent by markReconciled (line 755) — first-impression config data loss on a core onboarding action. High is warranted.
### [HIGH] (data-risk) New admin-authored behavior ships permanently thin (no category/timeWindow/contraindications/intensity) — heal can never fix a brand-new canonicalKey
- Persona: cms-publish-thin-bundle-source
- Area: src/lib/cms/authoring.ts createBehavior + backfillBuiltinFields ↔ src/lib/knowledge.ts healProtocols
- Evidence: createBehavior mints a guaranteed-unique key: `fields.title.toLowerCase().replace(...).slice(0,32) + '-' + Date.now().toString(36).slice(-4)` (authoring.ts:1235-1242). assembleBundleFromCMS builds `base` from only the 10 CMS columns then calls backfillBuiltinFields (authoring.ts:1576-1590). backfillBuiltinFields does `const bi = builtinAtom(base.canonicalKey); if (!bi) return base;` (authoring.ts:47-48) — a brand-new key has no builtin atom, so it returns the thin base untouched. At load, healProtocols overlays only by EXACT key: `const base = idx.get(b.canonicalKey); if (!base) return b;` (knowledge.ts:179-182). A net-new key is in neither PACKS nor STANDALONE_ATOMS_REGISTRY (knowledge.ts:167-170), so heal is a no-op forever. Result: the behavior serves with category/timeWindow/contraindications/intensity/evidenceTier/targets/daysActive all undefined for every user, permanently. (This is the exact mechanism that already left coq10-ubiquinone-tfgh / melatonin-e699 / low-dose-melatonin-paqa live-thin in bundle v9 per recon.)
- Why: This is the persona's core scenario, and it is real. A genuinely new behavior (one with no code twin) is the ONE case the heal was explicitly designed NOT to cover — yet the authoring flow's whole purpose is to let admins add new behaviors. The two safety/scheduling guardrails that go dead are the most consequential: timeWindow undefined means the engine's hard circadian/safety clamp (engine.ts:381-385) and the BehaviorSheet editor lock never apply (e.g. a 'morning sunlight'-type behavior could be scheduled at 11pm with no snap-back); contraindications undefined means the behavior can never be safety-suppressed. category undefined silently disables the workout-swap flow (workouts.ts:60 `if (b.category === 'workout') return true`).
- Fix: Persist the structural/safety fields in cms_behaviors (add columns category, time_window, contraindications, intensity, days_active, evidence_tier, targets, derived_from) and snapshot them in assembleBundleFromCMS so the bundle is self-describing — the heal then becomes a backstop, not the sole source. Minimum viable: in validateBundleGovernance, ERROR (block publish) when a behavior has NO matching code atom AND is missing timeWindow on a circadian-keyworded behavior, or warn loudly that the row will ship without safety/scheduling metadata. The heal-by-canonicalKey design is fundamentally unable to cover net-new content; the bundle must carry the data.
- Verify: CONFIRMED real and open. Full chain verified against live code: (1) createBehavior mints a net-new key `slug.slice(0,32)+'-'+Date.now().toString(36).slice(-4)` at authoring.ts:1235-1242 and can only write the 10 CMS columns (authoring.ts:1243-1256); NewBehaviorFields has no category/timeWindow/contraindications — the only references to those fields in the file are inside backfillBuiltinFields (authoring.ts:50,59,65), never in the create path. (2) backfillBuiltinFields does `const bi = builtinAtom(base.canonicalKey); if (!bi) return base;` (authoring.ts:47-48) — a net-new key has no builtin atom, so the thin base is returned untouched. (3) assembleBundleFromCMS (authoring.ts:1576-1590) is the real publish source (consumed by publish.ts:51), and isPublishableBehavior (authoring.ts:108-112) gates ONLY on `status !== 'archived' && ai_unverified !== true` — a manual add omits ai_unverified (authoring.ts:1257-1260), so the thin behavior publishes. No publish-time guard requires category/timeWindow/contraindications (grep for require/reject/block on those fields = nothing). (4) healProtocols overlays by EXACT key: `const base = idx.get(b.canonicalKey); if (!base) return b;` (knowledge.ts:179-182), and codeAtomIndex is built only from PACKS + STANDALONE_ATOMS_REGISTRY (knowledge.ts:167-170) — net-new key absent → heal is a permanent no-op. (5) Downstream impact verified: engine.ts:381 `if (it.timeWindow?.strict)` — undefined timeWindow skips the hard circadian/safety clamp; workouts.ts:60 `if (b.category === 'workout') return true` — undefined category disables workout-swap; contraindications undefined means no safety suppression. The cited stale keys (coq10-ubiquinone-tfgh, melatonin-e699, low-dose-melatonin-paqa) are absent from source, consistent with living in the published DB bundle and matching the exact key-mint suffix pattern. The session's heal fix overlays 'by canonicalKey' (exact match only) — by design it covers stale copies of code-twinned behaviors, NOT genuinely new admin-authored ones, so this is NOT already fixed and is NOT in KNOWN-DEFERRED. Honest caveat on severity: this requires an admin to author+publish a new behavior (not user-triggerable), but that is the authoring flow's stated purpose, and the dead guardrails are safety-critical (no strict-window clamp, no contraindication suppression), so high is warranted.

## MEDIUM (44)
### [MEDIUM] (inconsistency) PWA app-icon badge shows full pending count on day 1, contradicting the "nothing's expected of you yet" soft entry
- Persona: new-user-midday
- Area: src/app/today/page.tsx (badge effect) vs firstDaySoft soft-entry banner
- Evidence: today/page.tsx:724-733 — the badge effect is gated ONLY by `badgeDisabled` and `isToday`: `const remaining = Math.max(0, prog.total - prog.done); setBadge(remaining);`. It is NOT gated by `firstDaySoft`. For a fresh better-sleep onboard, `timelineProgress` (engine.ts:1897) counts the 6 non-muted behaviors (morning-sunlight, caffeine-cutoff, dim-lights, screens-off, cool-room, wind-down) → prog.total=6, prog.done=0 → setBadge(6). Meanwhile every ON-SCREEN pressure surface IS suppressed during first-day soft entry: the operating-summary headline/tone (1307-1318: "You're joining mid-day — that's fine… nothing's expected of you yet."), the progress bar (1391 `!firstDaySoft`), the Up Next hero (1825 `!firstDaySoft`), and the check-in (1639 `!firstDaySoft`). appBadge.ts:24-29 documents the badge as "the single biggest re-engagement nudge short of a notification." So a day-1 user who installed the PWA gets a red "6" on the home-screen icon — the exact "you're already behind on 6 tasks" pressure firstDaySoft exists to remove.
- Why: firstDaySoft's whole contract (today/page.tsx:788-798 comment) is to replace the high-pressure surface with a calm acknowledgement on a mid-day first day so the app doesn't read as "you're already behind on a routine you never started." The badge silently re-introduces that pressure on the OS icon, where it's most salient, directly undercutting the calm/safe onboarding promise for this persona.
- Fix: Gate the badge on the same first-day soft-entry condition the on-screen surfaces use: in the badge effect, force-clear during soft entry, e.g. `if (firstDaySoft) { setBadge(0); return; }` placed alongside the existing `badgeDisabled` early-return (today/page.tsx:725-733). Then the icon stays clean on day 1 and starts reflecting remaining work only after the user engages (which is also when firstDaySoft naturally drops).
- Verify: CONFIRMED real and open. The badge effect at /Users/rami/Claude/healthkit-website/src/app/today/page.tsx:725-733 is gated ONLY by `badgeDisabled` (724) and `!isToday` (730); its dep array (733) is `[isToday, prog.done, prog.total, badgeDisabled]` with NO `firstDaySoft`. It computes `remaining = Math.max(0, prog.total - prog.done)` and calls `setBadge(remaining)` (731-732). grep confirms these are the only setBadge call sites in the app — nothing else overrides it during soft entry. `firstDaySoft` is a separate useMemo (today/page.tsx:799-816) that fires when it's the first calendar day, `prog.done===0` (810), and a scheduled item is already past-due (812-815). engine.ts:1897-1909 confirms `timelineProgress` counts `items.filter(i => !i.muted)` for `total` — so a fresh better-sleep onboard yields prog.total>0, prog.done=0, hence setBadge(N>0). Meanwhile every on-screen pressure surface IS suppressed under firstDaySoft: operating-summary tone "nothing's expected of you yet" (1316-1318), the progress/essentials bar (`!firstDaySoft`, 1391), the daily check-in (`!firstDaySoft`, 1639), and the Up Next hero (`!firstDaySoft`, 1825). appBadge.ts:24-29 documents the badge as "the single biggest re-engagement nudge short of a notification." So a PWA-installed day-1 mid-day user gets a red count on the home-screen icon — the exact "you're already behind" pressure firstDaySoft (commented contract at 788-798) exists to remove. The session "fixed" list only mentions "banner gated to isToday" (a different gate); the badge is gated to isToday but NOT firstDaySoft, so this is distinct and unfixed. Medium is right: tone/UX contradiction, no crash/data loss, scoped to installed PWA users on a mid-day first day, but undercuts the documented calm-onboarding promise on the most salient surface. Fix: add `&& !firstDaySoft` to the badge effect (force setBadge(0) during soft entry), mirroring the on-screen gates.
### [MEDIUM] (overclaim) /upgrade shows trial urgency ("Lock it in so nothing resets") + a live price selector that nothing can act on — contradicted by "coming soon / full access today" on the same screen
- Persona: trial-active-day3
- Area: src/app/upgrade/page.tsx
- Evidence: The trial header copy at upgrade/page.tsx:97-105 renders for `access.inTrial` UNCONDITIONALLY (no `billingConfigured` check): "You have {trialDaysLeft} days of full intelligence left. Lock it in so nothing resets." The plan selector + PRICING ($79.99/yr, $8.99/mo, $179) at lines 145-177 also render unconditionally. The ONLY thing gated on `billingConfigured` is the button row (line 180). On the live app Stripe is inert (billing.ts:18-20 — no NEXT_PUBLIC_STRIPE_CHECKOUT_* env), so the billingConfigured===false branch (lines 205-216) renders directly below the prices: "Premium is coming soon — you have full access today" + a "Continue with full access" button. So a day-3 trial user (driven here by the Today trial row's "Details" button, today/page.tsx:1508, and Profile's "Keep Premium", profile/page.tsx:794-799) sees countdown urgency + a full pricing table they cannot buy, immediately contradicted by 'nothing to do, you have full access.'
- Why: This is the single conversion surface the entire reverse-trial mechanic funnels the persona to (today/page.tsx:517 calls the trial row "the whole conversion mechanic"). Presenting "lock it in so nothing resets" + real prices next to "coming soon / full access today" is a flat self-contradiction that reads as broken or bait. The session already reconciled the trial-EXPIRED button copy (commit 15c0f0a) but only touched the button block — the in-trial header + price selector were left ungated, so the honesty fix is incomplete for exactly this persona.
- Fix: Gate the urgency on real checkout availability. When `!billingConfigured`: for `access.inTrial`, replace line 104's "Lock it in so nothing resets" with a calm, true line (e.g. "You have full access during your trial — paid plans aren't switched on yet, so there's nothing you need to do.") and hide/disable the PRICING selector (lines 145-177) so a user is never shown a price they can't pay. Keep the current urgency copy only inside the `billingConfigured` branch.
- Verify: Confirmed open against live code. upgrade/page.tsx:97-105 renders the trial header "You have {trialDaysLeft} days of full intelligence left. Lock it in so nothing resets." gated ONLY on `access.inTrial` — no billingConfigured check. The plan selector + PRICING ($79.99/yr, $8.99/mo, $179) at upgrade/page.tsx:145-177 also render unconditionally. The ONLY billingConfigured gate is the button row at upgrade/page.tsx:180. billing.ts:18-20 derives billingConfigured from NEXT_PUBLIC_STRIPE_CHECKOUT_* env, so with no env it is false. For a day-3 in-trial user, entitlements.ts:82 makes access.premium===true (premium = paid || inTrial), so the false-branch at upgrade/page.tsx:205 hits the `access.premium ?` true arm → lines 208/214 render "Premium is coming soon — you have full access today" + "Continue with full access" directly below the urgency header and the dead price table. The three are co-located on one screen = a real self-contradiction. Funnel framing verified: today/page.tsx:517 comment calls the trial row "the whole conversion mechanic"; today/page.tsx:1508 "Details" and profile/page.tsx:795-798 "Keep Premium" both router.push('/upgrade'). The in-code comment at upgrade/page.tsx:200-204 shows the session tried to "state the TRUTH" but only gated the button block — the header (line 97) and prices (line 145) were left ungated, so the honesty fix is incomplete for the in-trial persona exactly as claimed. Not intended behavior, not already fixed. Downgraded from high to medium: this contradictory state is transient/pre-launch only (it vanishes the moment Stripe env is wired and billingConfigured flips true, at which point prices become actionable), and it is copy/conditional-rendering, not a data or safety defect. Could not independently verify the cited fix SHA 15c0f0a (no git history checked) but it is non-load-bearing — the live code state alone substantiates the finding.
### [MEDIUM] (overclaim) /upgrade sells "the full intelligence layer (Keystone, weekly review, … adaptive suggestions)" as Premium, but a lapsed-trial free user already gets all of it (live on Today, delayed on Insights) except the Correlation Explorer
- Persona: trial-expired-free
- Area: src/app/upgrade/page.tsx (VALUE list) vs src/app/today/page.tsx + src/app/insights/page.tsx
- Evidence: upgrade/page.tsx:13-18 lists the first Premium value as { t: "The full intelligence layer", s: "Keystone, weekly review, correlations & adaptive suggestions — built from your own data." }. But for access.premium===false: (a) Keystone is shown LIVE in Today's Operating Summary Focus chip — today/page.tsx:580 `const ks = useMemo(() => keystone(state) …)` and today/page.tsx:1368 renders `ksItem.title` with no access gate; (b) Adaptive suggestions render LIVE on Today — today/page.tsx:892-895 `suggestions(state).filter(...)` (no premium check) surfaced at today/page.tsx:1956-1958 `{isToday && activeSuggestions.length > 0 && …}`; (c) Keystone + weekly review render on Insights for free users on a 3-day delay — insights/page.tsx:150 `keystone(intelState)`, :146 `weeklyReview(intelState)`. Only CorrelationExplorer is genuinely Premium-exclusive (CorrelationExplorer.tsx:56-89 returns a lock card when !premium). The session's honesty pass (commit 15c0f0a) gated the "full access today" *button* on access.premium but never reconciled this VALUE bullet.
- Why: This is the payment screen for the most evidence-literate audience in the category. Telling a lapsed-trial user that Keystone, weekly review and adaptive suggestions are what they'd be buying — when those are already on their Today tab live, and on Insights with only a 3-day lag — is a credibility rupture and a weak conversion pitch (the real differentiators are: live/real-time insights, the Correlation Explorer, biomarker-aware adaptation, unlimited Library, and removing the 3-day delay).
- Fix: Rewrite the bullet to match what is actually withheld from free: emphasize "Insights in real time (free is on a 3-day delay)", "the Correlation Explorer", and "biomarker-aware adaptation" rather than naming Keystone / weekly review / adaptive suggestions as if they were paywalled. E.g. t: "Your intelligence, live", s: "Free sees patterns on a 3-day delay — Premium updates them the moment they form, adds the correlation explorer, and lets your day flex around your own recovery signals."
- Verify: CONFIRMED open. upgrade/page.tsx:13-18 sells the top Premium bullet as "The full intelligence layer — Keystone, weekly review, correlations & adaptive suggestions," shown to lapsed-trial free users (access.paid early-return at :57 only catches paid users; the VALUE list renders for everyone else at :114-143). But for access.premium===false: (a) Keystone runs live on Today — today/page.tsx:580 `keystone(state)` (no gate), derived into ksItem at :734-738, rendered ungated in the Operating Summary Focus chip at :1368; (b) adaptive suggestions run live — today/page.tsx:892-895 `suggestions(state)` (no premium check), rendered at :1956-1958 gated only on isToday; the sole `access` use on Today (:518) is the trial-countdown banner at :1493/:1503, never the chip or suggestions; (c) keystone + weekly review render on Insights for free users on a 3-day delay, not blocked — insights/page.tsx:69-83 builds intelState by filtering logs to today-minus-FREE_INSIGHT_DAYS, then :146-147 weeklyReview(intelState) and :150 keystone(intelState). intel.ts:223/481/750 take plain AppState with zero internal premium checks (grep found none). Only the "correlations" item is genuinely Premium-exclusive — CorrelationExplorer.tsx:56-89 returns a lock card when !premium. The honesty pass gated the /upgrade button (page.tsx:205-231) but left this VALUE bullet unreconciled. Medium: payment-screen overclaim / weak pitch, not a functional bug.
### [MEDIUM] (overclaim) Upgrade page sells "weight trends" as driving biomarker-aware adaptation, but weight never affects the day
- Persona: paid-premium
- Area: src/app/upgrade/page.tsx:27 (VALUE prop) ↔ src/lib/engine.ts:760 (biomarkerConcern) + biomarkers.ts:45 (weight def)
- Evidence: upgrade/page.tsx:27 — the "Biomarker-aware adaptation" value bullet reads: "Your day flexes around your own recovery signals — the HRV, resting-heart-rate and weight trends you log in Body Trends." But `weight` is `direction: "range"` (biomarkers.ts:45), and biomarkerConcern() skips every range marker outright: `if (!def || def.direction === "range") continue;` (engine.ts:760). So weight can NEVER produce a bioConcern, set bioRecoveryFlag, or change adaptation mode. Worse, adaptation only EASES the day for HRV/restingHR (BIO_RECOVERY = new Set(["hrv","restingHR"]), engine.ts:734 → the "Ease in"/lighter branch at engine.ts:1075); the other body markers never reshape the day either. This copy was edited this session by commit 15c0f0a ("truthful /upgrade copy") yet the weight overclaim survived the honesty pass.
- Why: This is the headline PAID value prop for the persona ('biomarker-aware adaptation'). A discerning premium subscriber logs their weight in Body Trends expecting their day to 'flex around' it, and it does literally nothing — the exact 'pay for it and it doesn't deliver' trust hit the rest of the app's honesty work is trying to avoid.
- Fix: Reword upgrade/page.tsx:27 to name only the signals that actually drive adaptation: "…flexes around your own recovery signals — the HRV and resting-heart-rate readings you log in Body Trends." Drop "weight trends" (weight is trend-only, intentionally not a band/adaptation input per its own `why` copy). Optionally add weight to the engine as a real (slow-trend) signal if the claim is meant to stay.
- Verify: CONFIRMED open. upgrade/page.tsx:27 sells "weight trends you log in Body Trends" as a recovery signal the day "flexes around." But biomarkers.ts:45 sets weight to direction:"range", and engine.ts:760 (`if (!def || def.direction === "range") continue;`) makes biomarkerConcern() skip every range marker — so weight can NEVER populate bioConcern (the text) or bioRecoveryFlag (the recovery bool), which are that function's only two outputs (engine.ts:769-772). Those two fields are the sole path into adaptation: the easing "Ease in"/lighter branch needs `s.bioRecoveryFlag && s.bioConcern` (engine.ts:1075), and even the non-easing reasons surfacing at engine.ts:1032/1108 needs bioConcern. `grep` confirms `weight` appears nowhere else in engine.ts (only an unrelated comment at line 519), so there is no alternate code path. Net: weight has literally zero effect on the day, yet it is named in the headline PAID value bullet ("Biomarker-aware adaptation"). Secondary point in the finding is also correct: BIO_RECOVERY = new Set(["hrv","restingHR"]) (engine.ts:734) means only HRV/restingHR ease the day; other non-range body markers (waist, bodyFat) can populate the bioConcern note but never set bioRecoveryFlag. Severity medium not high: it is user-facing copy/trust, not a crash/data-loss, and 2 of the 3 named signals (HRV, resting HR) genuinely do drive adaptation — only "weight trends" is the false claim. Not on the ALREADY-FIXED list (those /upgrade items cover access.premium gating, FREE_INSIGHT_DAYS, trial row, and biomarker cap — none touch this overclaim).
### [MEDIUM] (inconsistency) Insights "What's sticking" report shows the conflict-muted keystone (Strength) at 0% in alert-red — the app blames the user for a behavior it itself suppresses
- Persona: power-user-conflicts
- Area: src/lib/intel.ts behaviorAdherence() → src/components/BehaviorReportCard.tsx; src/app/insights/page.tsx:156,468
- Evidence: For Burnout Recovery + Longevity/Heart, no-intense fires CONFLICT_PAIRS (engine.ts:1330-1332) so strength/zone2/vo2max are muted every day (no-intense is block:anytime, daysActive undefined ⇒ all 7 days; packs.ts:582-594). The whole engine is careful to honor this: Today makes muted rows non-completable (today/page.tsx:2495 `if (it.muted) return`), scoring/UpNext/MorningBriefing all filter `!i.muted`, and intel even built conflictMutedKeys() (intel.ts:74-90) specifically so suggestions (intel.ts:553) and the keystone "light week" nudge (intel.ts:647) never blame a muted behavior. But behaviorAdherence() (intel.ts:1129-1173) iterates compileTimeline() — which does NOT apply conflict mutes — filters only `isActionable`, and never checks `.muted`/conflictMutedKeys. Strength is kind:'action', scheduled ~6 days/wk, and the user CANNOT complete it, so done=0 over scheduled≥3 ⇒ rate 0. BehaviorReportCard.tsx:14-16 paints rate<0.4 in var(--alert) and sorts it to the bottom as the single worst behavior. insights/page.tsx:468 renders it for free (delayed) and premium alike.
- Why: On one screen the system suppresses strength ('Resting today'), and on the next it flags Strength as 0% in red as the habit most 'needing re-engineering'. For the exact audience that consciously layered a recovery restraint over a training protocol, this reads as the app not understanding its own rules — the trust-destroying contradiction the conflictMutedKeys plumbing was added to prevent everywhere else.
- Fix: In behaviorAdherence(), exclude keys in conflictMutedKeys(state) (export/import the existing helper) the same way suggestions() does at intel.ts:553 — or skip any item whose shaped form is conflict-muted. A muted-by-design behavior should not appear as a 0% adherence gap.
- Verify: CONFIRMED open. behaviorAdherence() (src/lib/intel.ts:1129-1173) iterates compileTimeline (line 1144), filters only isActionable (line 1145), and never checks .muted or conflictMutedKeys before pushing rows at scheduled>=3 (line 1168). compileTimeline applies NO conflict mutes — it hardcodes muted:false (src/lib/engine.ts:258); mutes live only in applyConflictMutes inside shapeTimeline. CONFLICT_PAIRS has {restraint:"no-intense",target:"strength"} (engine.ts:1330); Burnout Recovery's no-intense is block:"anytime" with no daysActive (src/lib/packs.ts:582-594) → mutes all 7 days. strength is kind:"action" (isActionable true, intel.ts:1038-1040), scheduled 3 days/wk in Longevity (packs.ts:465) and complementary days in Heart (packs.ts:976) → union ~6 days/wk, clears scheduled>=3. The user cannot complete a muted row: Today's tap handler returns early `if (it.muted) return` (src/app/today/page.tsx:2495), so done=0 → rate=0. BehaviorReportCard band() paints rate<0.4 as var(--alert) (src/components/BehaviorReportCard.tsx:12-16) and intel sorts worst-to-bottom (intel.ts:1171). insights/page.tsx:156 + :468 render it with no premium gate and no muted filter. Asymmetry is real: conflictMutedKeys is used only at intel.ts:546/553 (suggestions) and :647 (keystone nudge) — both with explicit "don't blame a behavior the app suppresses" comments — but behaviorAdherence omits the guard. Not in ALREADY-FIXED (which covers suggestions/keystone/non-completable, not this card) nor KNOWN-DEFERRED. Severity medium not high: gated behind rows.length>=2 + enough logged days for scheduled>=3 + the specific Burnout-Recovery+training multi-pack overlay; it is a trust/coherence defect, not data-loss or safety.
### [MEDIUM] (overclaim) Recovery-mode banner claims "the demanding work is set aside" even when the user has no demanding work installed (sleep-only persona = the default recovery user)
- Persona: recovery-mode
- Area: src/lib/engine.ts (baselineAdapt recovery branch + MODE_DEFAULT_COPY) → rendered in src/app/today/page.tsx adaptive banner
- Evidence: engine.ts:1023-1034 the recovery branch returns a FIXED tone: "Your recovery is low, so today leans lighter — the demanding work is set aside." The recovery shape pass mutes ONLY RECOVERY_DEMOTE keys = {strength, zone2, deep-work, vo2max-intervals} (engine.ts:1291-1299, applied at engine.ts:1743-1753). The regression test recovery-demote.test.ts:45-62 proves every NON-demoted behavior (e.g. morning-sunlight) stays a normal todo in recovery mode. The recovery-mode persona's default pack is Better Sleep alone: onboarding.ts:192-193 sets starter="better-sleep" for goal==sleep OR sleepBaseline=="rough", and extras are added ONLY when experience==="deep" (onboarding.ts:204). Live bundle v9 (Supabase, verified) Better-Sleep behaviors = [morning-sunlight, caffeine-cutoff, dim-lights, screens-off, cool-room, magnesium-pm, wind-down, melatonin-e699, low-dose-melatonin-paqa] — NONE are in RECOVERY_DEMOTE. So a non-"deep" sleep/recovery user who logs Poor sleep+Low energy (recoveryProxy=40 → recovery mode) gets a byte-identical timeline yet a banner asserting work was "set aside." MODE_DEFAULT_COPY.recovery (engine.ts:1122-1125) repeats the same unconditional claim if a CMS rule sets the mode.
- Why: This is the persona's PRIMARY path and a research-savvy audience (Attia/Huberman-style) will immediately notice the board didn't change while the system claims it eased their day. It erodes the core trust signal ("it read me and acted"). It is the same banner-vs-reality contradiction class the session already fixed for carryover (engine.ts:901-907) and CMS-mode copy — just via a different gap (no-demotable-content).
- Fix: Make the recovery tone conditional on whether anything was actually demoted. Cheapest correct fix: compute in baselineAdapt whether the user's compiled timeline contains any RECOVERY_DEMOTE key (or pass an `easedCount` from shapeTimeline) and branch the copy — when nothing is demotable, say something honest like "Your recovery is low. Nothing here is high-intensity, so just move gently and protect tonight's sleep." Apply the same conditional to MODE_DEFAULT_COPY.recovery and to DailyCheckInCard.tsx:54 ("ease back on the harder blocks" — there are none).
- Verify: CONFIRMED REAL and open. All load-bearing claims verified against live code. (1) engine.ts:1027 — recovery branch (entered when recoveryProxy<45, RECOVERY_EASE_BELOW=45 at :996) returns the unconditional tone "...the demanding work is set aside." (2) RECOVERY_DEMOTE = {strength, zone2, deep-work, vo2max-intervals} at engine.ts:1291-1299. (3) The recovery shape pass at engine.ts:1743-1767 mutes ONLY RECOVERY_DEMOTE keys; recovery-demote.test.ts:60-61 proves a non-demoted behavior (morning-sunlight) stays a normal todo. (4) Default recovery persona: onboarding path is src/app/onboarding/page.tsx (NOT the cited src/lib/onboarding.ts — path wrong but logic correct): :192-193 sets starter="better-sleep" for goal==sleep OR sleepBaseline=="rough" OR focus includes sleep, and :204 adds extras ONLY when experience==="deep". (5) Better Sleep behaviors in code (packs.ts:498-523) resolve to canonicalKeys morning-sunlight, caffeine-cutoff, dim-lights, screens-off, cool-room, wind-down — verified at packs.ts:16,88,130,262,279,504 — NONE in RECOVERY_DEMOTE. (6) recoveryProxy math (engine.ts:912-919): sleep=2,energy=2 → (40*0.6+40*0.4)=40<45 → recovery mode. (7) today/page.tsx:1318 renders adaptation.tone gated only on isToday && !dayComplete (line 1285) — no guard on whether anything was actually muted. (8) MODE_DEFAULT_COPY.recovery (engine.ts:1124) repeats the same claim for CMS-set modes. A grep for the tone strings/any mute-count flag found only the literal strings — there is NO conditioning on demotion having occurred. This is DISTINCT from everything fixed this session: the MODE_DEFAULT_COPY fix (audit line 235) solved the opposite case (mode=recovery showing normal copy); the carryover fix (audit line 94) and banner-vs-checkin/past-day-scrub fixes are unrelated. Net: a non-"deep" sleep/recovery user who logs a poor check-in gets a byte-identical timeline plus a banner asserting work was set aside. Severity medium not high: it is a copy/trust overclaim on a narrow (default sleep-only) persona, not a data/safety/functional defect — same honesty-bug class the session already addressed for CMS-mode and carryover banners; the board itself is correct.
### [MEDIUM] (ux-confusion) First check-in tap flips the banner to a finished "Recovery mode" read while the check-in card below still shows the unanswered two-tap prompt
- Persona: recovery-mode
- Area: src/app/today/page.tsx (banner uses sig=getSignals; card uses done=both-taps) + src/components/today/DailyCheckInCard.tsx
- Evidence: getSignals sets recoveryProxy from EITHER signal alone — sleepQuality and energy are pushed independently and averaged over whatever is present (engine.ts:910-919). So tapping only "Poor sleep" (energy still null) yields proxy=40 → adapt() returns recovery mode (engine.ts:1023). The banner is driven by `adaptation`/`sig` computed from full state (today/page.tsx:578-579) and renders "Recovery mode / Your recovery is low, so today leans lighter — the demanding work is set aside" (today/page.tsx:1313-1318). Meanwhile DailyCheckInCard's `done` requires BOTH (`sleepQ != null && energy != null`, DailyCheckInCard.tsx:38), so it still renders the prompt asking "Energy right now?" (DailyCheckInCard.tsx:80-131). Verified combos: "Poor sleep only"→proxy40/recovery/checkInDone=false; "Great sleep only"→proxy100/primed/checkInDone=false. checkInAcked starts false on a fresh day (today/page.tsx:314-321), so the prompt is showing when the first tap lands — both surfaces visible simultaneously.
- Why: The banner asserts a completed "read" (mode chosen, day eased) that the card directly below says is still in progress (energy unanswered). It's the same banner-contradicts-check-in problem the carryover fix targeted (engine.ts:901-907), reachable here via a single tap within today rather than via yesterday's data.
- Fix: Hold the adaptive read until the check-in the user is actively filling is complete. Simplest: gate the banner's mode-derived copy (or the chips) on the same `done` condition for an un-acked check-in — e.g., while the check-in card is in prompt mode and only one of sleep/energy is set, show the neutral "Today" copy and defer the recovery read until both taps land (or the card is acked).
- Verify: CONFIRMED OPEN. Verified the single-tap asymmetry against live code. getSignals builds recoveryProxy from EITHER signal alone: tapping only "Poor sleep" pushes one part {v:40,w:0.6} and skips the null energy push, yielding proxy=round(40*0.6/0.6)=40 (engine.ts:910-919). baselineAdapt then returns mode:"recovery"/headline "Recovery mode"/tone "Your recovery is low, so today leans lighter — the demanding work is set aside" because 40 < RECOVERY_EASE_BELOW(45) (engine.ts:1023-1027). The banner renders adaptation.headline/adaptation.tone gated only by !dayComplete && isToday (today/page.tsx:1285, 1313, 1318), driven by adapt(state)/getSignals(state) recomputed on the state write from onSleep→updateSleepLog (today/page.tsx:578-579, 1644). Meanwhile DailyCheckInCard's done = sleepQ != null && energy != null (DailyCheckInCard.tsx:38); with energy still null, done=false, so it keeps rendering the two-tap prompt incl. "Energy right now?" (DailyCheckInCard.tsx:80-131). Its render gate isToday && !firstDaySoft && !checkInAcked holds on a fresh active day — checkInAcked starts false (today/page.tsx:314-321) and firstDaySoft is false for a returning/non-day-1 user (today/page.tsx:799-816). Both are siblings in the same tree, both isToday, day not complete. "Great sleep only" → proxy=100 ≥ PRIMED_AT_OR_ABOVE(78) → mode "primed"/"Primed" (engine.ts:1083-1089), prompt still showing — same contradiction. This is the SAME banner-vs-checkin class the carryover fix targeted (engine.ts:901-907 comment), but reachable via a single tap within today rather than yesterday's data, which that fix did not close. Not intended: DailyCheckInCard's own header comment (lines 3-10) states the read must "never contradict the operating-summary banner," which is exactly what breaks in the one-tap window. Distinct from the FIXED carryover item, so not a regression and not already-fixed.
### [MEDIUM] (ux-confusion) Whole waking day mis-blocked: wake-anchored "morning" routine files under "Evening", bedtime routine files under "Morning"
- Persona: night-shift
- Area: src/lib/time.ts:293-299 (blockForMinutes awake-clock split) + src/lib/engine.ts:393 (it.block = blockForMinutes)
- Evidence: For wake=18:00/bed=10:00, blockForMinutes uses a PURE CLOCK split for awake hours (Morning 05:00–11:59, Afternoon 12:00–16:59, Evening 17:00+). Verified by resolving real behaviors: Morning sunlight (anchor wake+30) → 6:30 PM → block 'evening'; Strength (wake+180) → 9:00 PM → 'evening'; Wind-down (bed-60) → 9:00 AM → 'morning'; Melatonin (bed-30) → 9:30 AM → 'morning'. So in compileTimeline (engine.ts:393) the user's first wake activity sits under a section literally headed 'Evening' (today/page.tsx:2179 renders blockLabel(block)), while their pre-sleep wind-down sits under 'Morning'. greeting() (today/page.tsx:142-145) shows 'Good evening' at their 6 PM wake and stays 'evening' through 4 AM. Their entire active day collapses into the 'Evening' section.
- Why: The product's core promise is a calm, correctly-ordered daily timeline. For a night-shift worker the section headers are inverted from their lived day — they wake to 'Good evening', their morning sunlight/cold-shower/training are stamped 'Evening', and their bedtime melatonin is stamped 'Morning'. It reads as broken on day one and undermines trust in the whole engine.
- Fix: Make the awake-hours block split wake-relative instead of absolute-clock when the schedule wraps (bed<=wake). Partition the [wake, bed) awake span into thirds (or honor blockBoundaries as OFFSETS from wake) so 'morning' = first third after wake, etc. At minimum, when bed<=wake, derive blocks from sinceWake fraction rather than raw clock so a 6 PM wake reads as the user's morning. Sync greeting() to the same source.
- Verify: CONFIRMED open. blockForMinutes (src/lib/time.ts:293-299) splits AWAKE hours by a pure clock using resolveBlockBounds, which returns fixed defaults {morning:300, afternoon:720, evening:1020} = 05:00/12:00/17:00 when no custom blockBoundaries are set (time.ts:113-115). Only the ASLEEP branch (time.ts:287-291) is wake-anchored; the awake branch ignores the wake anchor. I executed a faithful replica for wake=18:00/bed=10:00 and reproduced the finding exactly: morning sunlight wake+30 (18:30)->'evening', strength wake+180 (21:00)->'evening', wind-down bed-60 (09:00)->'morning', melatonin bed-30 (09:30)->'morning'. engine.ts:393 (it.block = blockForMinutes(m, settings)) and the strict-window branch at engine.ts:384 file behaviors under these inverted blocks; today/page.tsx:2179 renders blockLabel(block) as the header. greeting() (today/page.tsx:142-145) returns 'Good evening' at the 18:00 wake (currentBlock='evening', isOvernight=false per time.ts:333-347) and still 'evening' at 04:00 (verified). The schedule is reachable: wakeTime/bedtime are unconstrained type=\"time\" inputs (onboarding/page.tsx:507-517, profile/page.tsx:226-236). NOT a re-report of a session fix: the 'customTime no longer contradicts block header' / shared-rule fix only guarantees header/NOW-marker CONSISTENCY (which holds here — both say evening); it does not fix the SEMANTIC INVERSION for an inverted schedule, and no fix regressed it. This is intended clock-based design (comments engine.ts:363-366, founder call task #171) that silently assumes a wake-before-noon schedule. Downgraded to medium (not high) because it only manifests for the minority with a substantially inverted/night-shift schedule; for typical users (wake 05:00-11:00) the labels are correct.
### [MEDIUM] (dead-option) The night-owl escape hatch (custom blockBoundaries) is structurally impossible to set for a wrap schedule — silently falls back to 5/12/17
- Persona: night-shift
- Area: src/lib/time.ts:108-133 (resolveBlockBounds ascending guard) vs src/lib/types.ts:105-109 (documented "shift worker / night owl" purpose)
- Evidence: types.ts:107-108 says blockBoundaries 'Lets a shift worker / night owl define when their morning, afternoon, and evening begin'. But resolveBlockBounds (time.ts:120) requires the three raw HH:MM minute values to be strictly ascending: `!(mo>=0 && mo<af && af<ev && ev<1440)` → defaults. A night-shift day (18:00→10:00) needs a mapping that wraps midnight, e.g. morning=18:00(1080), afternoon=23:00(1380), evening=04:00(240). Verified all natural attempts fall back: {18:00,23:00,04:00}→defaults, {18:00,22:00,02:00}→defaults, {19:00,00:00,05:00}→defaults — because evening (early AM) is numerically < morning (evening). There is no value the night-shift user can enter that both wraps their day and passes the ascending check, so the feature built for them is inert and silently snaps back to 05:00/12:00/17:00.
- Why: This is the one knob the app advertises as the fix for exactly this persona, and it cannot be configured to help them. They have no way to make their timeline read correctly. Combined with finding #1, the night-shift user is stuck with an inverted day and the documented remedy does nothing.
- Fix: Allow a wrapping boundary set when the schedule wraps (bed<=wake): validate the three boundaries as positions on the wake→bed circle (each strictly later than the previous in sinceWake terms) rather than as ascending raw minutes. Or redefine blockBoundaries as minutes-after-wake offsets. Until then, surface in Profile that custom day blocks aren't supported for overnight/wrap schedules instead of silently reverting.
- Verify: CONFIRMED open. types.ts:107-108 documents blockBoundaries as letting "a shift worker / night owl define when their morning, afternoon, and evening begin." time.ts:120 (`if (!(mo >= 0 && mo < af && af < ev && ev < 1440)) return d;`) requires the three raw HH:MM minute values strictly ascending. A night-shift awake window (e.g. 18:00→10:00) requires a wrapping mapping where the evening start is post-midnight (small minute value) and thus numerically < the morning start (evening, large value), so `af < ev` can never hold — any wrapping triple falls back to the 05:00/12:00/17:00 defaults (300/720/1020 at time.ts:113). The finding's three sample inputs all wrap and all fail. The defect is reinforced by the consumer itself: blockForMinutes (time.ts:296-299) does plain numeric `m>=morning && m<afternoon` comparisons that are inherently non-wrapping, so the whole awake-window block model can't serve the night-shift persona the comment names. clock-blocks.test.ts:223-231 explicitly asserts the fallback-to-defaults behavior for non-ascending sets, confirming it's the live behavior. Not in the ALREADY-FIXED session list, not in KNOWN-DEFERRED, and not logged in tasks/full-audit-2026-06-09.md (which has only the unrelated #340 quiet-hours night-shift item and the unrelated qa-review-2026-06-05.md:50 anchor=bed window wrap). Severity dialed to medium (not high): the user-facing Profile copy at profile/page.tsx:315-316 honestly warns "Start times must go in order; otherwise the 5am/12pm/5pm defaults apply" — so users are not deceived in the UI; the "advertised fix" framing rests on an internal code comment. It is a genuine missing-capability/over-promise (the named persona cannot configure the knob to help them, and it silently snaps back), but not data-loss or safety, and the cited "combined with finding #1 inverted day" is out of scope here.
### [MEDIUM] (ux-confusion) Suggested default quiet hours (22:00→07:00) silently drop the night-shift user's core overnight reminders, and the copy is inverted for them
- Persona: night-shift
- Area: src/app/profile/page.tsx:571-606 (quiet-hours prefill + "nothing buzzes overnight" copy) → src/lib/smartReminders.ts:56-69 (resolveReminderMinutes returns null)
- Evidence: When the night-shift user enables Protocol reminders and touches Quiet hours, the fields prefill to start 22:00 (profile/page.tsx:593) / end 07:00 (profile/page.tsx:577), and the helper text (profile/page.tsx:603-605) says 'No reminders fire between these times — a wind-down window so nothing buzzes overnight.' For wake=18:00/bed=10:00, 22:00–07:00 falls in the MIDDLE of their waking shift. Verified via resolveReminderMinutes: Evening meal (wake+360 → 12:00 AM) → DROPPED (null); Late caffeine cutoff (wake+480 → 2:00 AM) → DROPPED — 2 of 6 representative reminders silently suppressed, precisely the overnight-prime behaviors. Their actual sleep window (10:00–18:00) gets zero quiet-hours protection.
- Why: The reminder feature is a primary engagement loop. A night-shift user who accepts the sensible-looking defaults silently loses nudges for the very behaviors that happen during their working hours, while the words tell them this protects their overnight sleep — the opposite of what it does for them.
- Fix: Default the quiet-hours prefill to the user's actual sleep window when bed<=wake (e.g. start=bedtime, end=wakeTime) instead of hardcoded 22:00/07:00, and make the helper copy reference 'your sleep window (10:00 AM–6:00 PM)' rather than 'overnight'. resolveReminderMinutes already handles wrap windows correctly; only the default + copy are wrong.
- Verify: CONFIRMED open. (1) Copy is the daytime-only framing: profile/page.tsx:603-605 reads "No reminders fire between these times — a wind-down window so nothing buzzes overnight." (2) The 22:00/07:00 defaults are reachable: profile/page.tsx:577 fills end with `|| "07:00"` and :593 fills start with `|| "22:00"` whenever the user touches one field while the other is empty (and there is NO seeded quietHours default — absent from storage.ts, types.ts:104 is just an optional field, onboarding sets nothing — so inputs start empty per `?? ""` at :571/:588). (3) Reminder times are absolute clock minutes: time.ts:22 `resolveMinutes` normalizes wake+offset to 0..1439; inQuietHours wraps midnight at time.ts:66 (`m >= s || m < e`); resolveReminderMinutes returns null when both chosen and scheduled time fall in quiet hours (smartReminders.ts:64-67). (4) Real catalog behaviors land in the night-shift quiet window with no timeWindow clamp to save them: packs.ts:226-233 NSDR (anchor wake, offsetMin 420, no timeWindow) → for wake=18:00 = 01:00 AM → dropped; packs.ts:246-253 SOCIAL_CONNECTION (anchor wake, offsetMin 600, no timeWindow) → 04:00 AM → dropped. Both are afternoon/evening behaviors for a normal user that fall in clock-night for a 18:00-waker, while their true sleep window (10:00–18:00) gets zero quiet-hours coverage. Caveats on the finding: it says fields "prefill" — technically they auto-fill the partner field on first touch, not at render; and its named examples ("evening meal wake+360", "late caffeine cutoff wake+480") are illustrative, not exact catalog keys — but the mechanism and the real wake+420/wake+600 behaviors confirm the substance. Not in the ALREADY-FIXED list: the reminders fix added the clamp + quiet-hours fallback (resolveReminderMinutes) but did not address inverted defaults/copy for night-shift schedules.
### [MEDIUM] (data-risk) Day boundary splits the night-shift user's single continuous waking period across two calendar dates at midnight
- Persona: night-shift
- Area: src/lib/storage.ts:619-621 (getLogForDate keyed purely on dateKeyInTz) + src/app/today/page.tsx:531-534 (selectedDate=dateKeyInTz at offset 0) + 781-786 (dayComplete)
- Evidence: Logs are keyed solely by calendar date (dateKeyInTz → getOrCreateLog); there is no subjective/wake-anchored day. The night-shift user is awake 18:00→10:00 next day. Behaviors before midnight (morning sunlight 6:30 PM, training 9 PM) log to June 9; behaviors after midnight (evening meal 12 AM, caffeine cutoff 2 AM) log to June 10. Verified isOvernight(00:00)=false and currentBlock(00:00)='evening', so at midnight Today stays 'evening/NOW' but selectedDate rolls to June 10 and the timeline recompiles for the new selDayIdx — mid-routine, the day they're working 'resets'. dayComplete (today/page.tsx:781) can fire at ~midnight when only the pre-midnight half is done, and the just-completed pre-midnight items disappear from 'today'.
- Why: For this persona the split lands in the dead center of their day, so streak/score/day-complete and the whole Today surface fracture every shift. A day-2 user could complete their full routine yet never see a single 'day complete' because each calendar day only ever holds half their behaviors.
- Fix: Introduce a wake-anchored 'logical day' for date-key derivation: when computing the user's current day key for Today/logging, subtract the wake offset (or treat the day as starting at wakeTime) so a continuous waking period maps to one key. This is an architectural change; scope it behind the wrap-schedule case (bed<=wake) to limit blast radius, and reuse the existing isOvernight anchor math.
- Verify: CONFIRMED open and architectural. All cited claims verify against live code. (1) Logs are keyed purely on calendar date: storage.ts:619-621 getLogForDate→getOrCreateLog matches/creates by log.date===date (storage.ts:597-600); no wake-anchored/subjective day exists (tz.ts:108-114 dateKeyInTz is raw Intl calendar parts; grep for subjective/wake-anchored *day key* found only timing-within-day refs). (2) today/page.tsx:530-533 selectedDate=addDaysToKey(dateKeyInTz(tz),-offset), so at offset 0 the key rolls at calendar midnight regardless of wake state; log/selDayIdx follow (today/page.tsx:555-571). (3) Traced the cited persona (wake=18:00=1080, bed=10:00→2040, now=00:00=0) through time.ts: isOvernight (time.ts:337-346) → sinceWake=360 < dayLength=960 → returns false; blockForMinutes (time.ts:280-299) → t=1440 < bed=2040 → awake branch, m=0 < morning bound → 'evening'. So at midnight Today correctly stays evening/NOW yet selectedDate rolls forward, splitting the single waking period and dropping pre-midnight completed items off Today. (4) dayComplete (today/page.tsx:781-786) is isToday-gated and computed from the rolled-over log's prog/suppProg, so it recomputes against the fresh (empty) calendar day mid-shift — consistent with this session's isToday-gating changes, NOT a regression of them. Not in KNOWN-DEFERRED. Severity kept at medium (lower end): the 'data-risk' label is slightly overstated — no data is lost; pre-midnight items, their score and streak remain stored under the prior calendar key (tz.ts:185-194 addDaysToKey) and are viewable by scrubbing back one day. The real defect is Today-surface continuity + never reaching 'day complete' for one overnight shift, and it requires an uncommon (but app-permitted, unconstrained type=time inputs at profile/page.tsx:225-236) night-shift persona.
### [MEDIUM] (ux-confusion) TimezoneSentry "Update" on a westward move rewinds the Today board behind already-logged days, orphaning that day's progress
- Persona: traveler-tz
- Area: src/components/TimezoneSentry.tsx (Update handler) + src/app/today/page.tsx (selectedDate)
- Evidence: On Update, the sentry only does `updateSettings({ timezone: device })` (TimezoneSentry.tsx:94-99). The board's day is `selectedDate = addDaysToKey(dateKeyInTz(tz), -offset)` with offset 0 (today/page.tsx:531,534). For a westward move (stored Asia/Tokyo → device America/Los_Angeles), `dateKeyInTz` returns an EARLIER calendar day after the switch. If the user opened the app in the pre-update window — when stored Tokyo time had already rolled to the next day — they saw and could complete behaviors on that later date (board showed 'tomorrow' relative to their physical location). After Update the board jumps backward to the now-current LA day, and the completions they just made are stranded on a future-dated log they can no longer see on the timeline (offset can't go negative: today/page.tsx:1091 `Math.max(o-1,0)`), and that orphan log triggers the streak/gap collapse in the prior finding.
- Why: The traveler experiences the day visibly moving backwards and their just-logged progress vanishing from view, with no explanation. The sentry copy ('Update to your new timezone?') gives no hint that the current day will rewind, so the user can't anticipate or avoid the orphaning.
- Fix: When applying a westward Update where `dateKeyInTz(newTz) < dateKeyInTz(oldTz)`, fold any log keyed to the now-future date into the (earlier) current day before switching — or at minimum re-key it to the new today — so no log is left ahead of today. Pair with the normalize clamp from the prior finding (which heals it on next load regardless). Optionally soften the sentry copy to note the day may shift.
- Verify: CONFIRMED open. (1) TimezoneSentry.tsx:94-99 Update handler only calls updateSettings({timezone: device}) + clears the dismiss key — no log re-home, and the copy (TimezoneSentry.tsx:79 "Update to your new timezone?") gives no hint the current day will rewind. (2) useAppState.ts:252-258 updateSettings merges into settings only, never touches dailyLogs. (3) tz flows from the stored setting: today/page.tsx:253 getTz(settings); tz.ts:37-38 returns settings.timezone when valid — so Update changes tz Tokyo→LA. (4) Board day = today/page.tsx:530-531 selectedDate = addDaysToKey(dateKeyInTz(tz), -offset), offset 0. I ran dateKeyInTz at a real boundary instant (UTC 2026-06-08 16:00): Asia/Tokyo → 2026-06-09, America/Los_Angeles → 2026-06-08, so the board rewinds one calendar day on a westward Update. (5) Completions write via toggleBehavior(selectedDate,…) (today/page.tsx:1862, 2501) → storage.ts:680-709 getOrCreateLog(state, date), keyed purely by the date string; the pre-update Tokyo-day log (2026-06-09) is now future-dated and unreachable because the next-day control clamps at today/page.tsx:1091 Math.max(o-1,0) and is disabled when isToday (1092) — offset cannot go negative. (6) No healing: storage.ts normalize clamps only future-dated BIOMARKER readings (storage.ts:181-185), nothing re-homes future dailyLogs, so the session's biomarker future-date heal does not cover this. Caveat: the trigger window is narrow (user must log during the interval when the eastern stored zone has rolled to the next day but the western device zone has not), and the orphan log persists rather than being deleted — but for the relocated user the just-logged progress visibly vanishes and the day moves backward with no explanation, matching the medium UX-confusion claim. The "streak/gap collapse in the prior finding" downstream claim was not independently re-verified here.
### [MEDIUM] (inconsistency) Stored day score disagrees with the visible board after a swap in essentials mode (denominator excludes the completed replacement)
- Persona: workout-swapper
- Area: src/lib/storage.ts computeBehaviorScore (642-678) — shares the shapeTimeline pipeline with the two findings above  ⚠️ REGRESSION (recent commit)
- Evidence: computeBehaviorScore runs the identical applySwaps→shapeTimeline pipeline (storage.ts:655-666), so it inherits the mute clobbering. Probe (swap zone2→extended-walk, essentials mode): the completed replacement extended-walk is muted, so it's dropped from BOTH numerator and denominator (active set = [morning-sunlight, protein-breakfast, zone2(resurrected,undone), caffeine-cutoff, wind-down, fiber-veg], doneInActive=[]) yet computeBehaviorScore returns 8 — a value that matches neither 'replacement counted' nor the visible board (which now shows the resurrected zone2 as the afternoon workout). The single workout the user logged via swap contributes nothing to the score in this mode.
- Why: For a persona whose whole interaction is 'I did a different workout', the score quietly ignoring that workout (or counting a phantom denominator) undermines the core promise that the swap 'doesn't pollute scoring'. It's the data-side symptom of the same mute bug and should be re-verified once the mute fix lands.
- Fix: Fixing the swap-mute authority (findings 1 & 2) makes computeBehaviorScore consistent automatically, since the replacement will be active+done and the original will be muted out of the denominator. Add a regression test asserting that for every adapt mode, a swapped day has exactly one active workout (the replacement), it is counted done, and the original is excluded from the score denominator.
- Verify: CONFIRMED via direct execution against live code. computeBehaviorScore (storage.ts:654-666) runs applySwaps→shapeTimeline; the Today board uses the identical pipeline (today/page.tsx:587-602, renders only !muted items at 2070-2078). In essentials mode, shapeTimeline mutes every leverage<3 item (engine.ts:1713-1718). The swap replacement extended-walk has leverage:2 (packs.ts:2484), so it is muted and dropped from BOTH numerator and denominator — even though applySwaps injected it muted:false (engine.ts:504). Meanwhile the swapped-out original zone2 (leverage:3, packs.ts:443) survives essentials and is resurrected un-muted by guaranteePerBlock (engine.ts:1496-1502) to keep the afternoon block non-empty. Probe (getDefaultState, swap zone2→extended-walk, replacement completed, essentials): score=0, active=[morning-sunlight,protein-breakfast,zone2,caffeine-cutoff,wind-down,fiber-veg], done=[]. Control in normal mode: score=9 with extended-walk un-muted and counted. So the one workout the user logged contributes nothing in essentials, the board shows the resurrected (undone) zone2 as the active workout while hiding the completed extended-walk, and the stored score (0) matches neither 'replacement counted' (9) nor the visible board. Reachable: adapt() evaluates essentials (low 7d adherence, engine.ts:1036-1047) before the easierDayFromSwap→lighter branch (engine.ts:1067), and zone2(moderate)→extended-walk(low) is exactly an easier-day swap. Two harmless inaccuracies in the finding: actual score is 0 not '8' (the 8/9 figure matches NORMAL mode), and the 6th active item is caffeine-cutoff (odd day-index) or strength (even) — day-of-week dependent. Not in the already-fixed-this-session list, not in known-deferred.
### [MEDIUM] (inconsistency) Today's "Suggestion" card runs on stale pre-gap data and contradicts the "Welcome back, ease in" banner right above it
- Persona: long-gap-returner
- Area: src/app/today/page.tsx:1957 (render) + src/lib/intel.ts:481-600 (suggestions())
- Evidence: Today renders activeSuggestions[0] unconditionally for isToday (today/page.tsx:1957-1958) with NO gapDays / returning guard. suggestions() itself has no getSignals/gapDays check (grep of intel.ts shows gapDays only inside behaviorStats' vacationDates, never in suggestions). Its two suggestion types both read the most-recent scored/sleep-rated days of ALL TIME, not a recent calendar window: the chronic-skip retime/pause gate is `state.dailyLogs.length >= 21 && activeDays.length >= 5` where activeDays = `[...dailyLogs].filter(score>0).sort(desc).slice(0,7)` (intel.ts:521-525) — for a 40-day returner those 5+ "recent" scored days are all 40+ days stale, and `doneRecently` (intel.ts:564) is evaluated against them; the sleep-install gate is `sq.length>=4 && mean<=2.7` over `slice(0,7)` of all sleep-rated logs (intel.ts:486-494), again pre-gap. So on the welcome-back day the user can see "&lsquo;Cold plunge&rsquo; hasn't found its time yet — set it aside" or "Your sleep keeps coming up short → Install Better Sleep," computed from data before the break, rendered directly below the rebuild banner (today/page.tsx:1285) whose copy is "It's been a while — welcome back. I've trimmed today to a few essentials; the rest will catch back up on its own as you re-engage" (engine.ts:1012). Insights got an explicit returner banner for exactly this stale-claim problem (insights/page.tsx:207-255: `returning = s.hasHistory && s.gapDays >= 2`), but Today's suggestion card never received the equivalent guard.
- Why: For the long-gap-returner the welcome-back surface is the whole product promise — calm, non-punishing re-entry. A nag to pause/retime a behavior, or an "install another protocol" pitch, derived from how they behaved 40+ days ago, reads as the system NOT noticing they were away (the opposite of the banner's "I've trimmed today… as you re-engage") and pushes friction at the exact moment the persona is most likely to bounce.
- Fix: Suppress (or reframe) the Today suggestion card while returning. Cheapest: in today/page.tsx gate the suggestion block on `!(sig.hasHistory && sig.gapDays >= 2)` (sig is already computed at line 579). Better/root: have suggestions() early-return [] when getSignals(state).gapDays >= 2 (and/or window activeDays/sq by recent calendar days like adherence7 does in engine.ts:839-844) so the chronic-skip and sleep-trend reads only fire on genuinely recent engagement, not pre-gap residue.
- Verify: CONFIRMED open. (1) suggestions() has no gap/returning guard: grep for gapDays|getSignals|returning|hasHistory in src/lib/intel.ts returns zero hits; the function (intel.ts:481-722) never consults getSignals/gapDays. (2) Both gates read all-time most-recent data: sleep-install gate sq = all sleepQuality logs sorted desc .slice(0,7), gated sq.length>=4 && mean<=2.7 (intel.ts:486-494); chronic-skip gate activeDays = dailyLogs.filter(score>0).sort(desc).slice(0,7), gated dailyLogs.length>=21 && activeDays.length>=5, with doneRecently evaluated against those same stale activeDays (intel.ts:521-525, 564-566). For a 40-day-gap returner those "recent" days are all pre-gap, so the pause/retime or "Install Better Sleep" suggestion can fire on stale data. (3) Today renders it with no gap guard: activeSuggestions = suggestions(state).filter(dismissed) only (today/page.tsx:892-895); render block gates solely on isToday && activeSuggestions.length>0 then shows activeSuggestions[0] (today/page.tsx:1957-1960). (4) The contradicting rebuild banner sits right above, gated on !dayComplete && isToday (today/page.tsx:1285); for gapDays>30 its copy is "It's been a while — welcome back. I've trimmed today to a few essentials; the rest will catch back up on its own as you re-engage." (engine.ts:1012). On a fresh (incomplete) welcome-back day both surfaces co-render. (5) Insights got the exact guard Today lacks: returning = s.hasHistory && s.gapDays>=2 (insights/page.tsx:207-209) drives a "reflections are from before the break" disclaimer (insights/page.tsx:243-255). Not in the already-fixed list (which only covers "suggestions skip conflict-muted" and "banner gated to isToday", neither a gap guard) and not in known-deferred. Medium: no crash/data-loss, but it breaks the calm re-entry promise for the long-gap returner and visibly contradicts the banner directly above it.
### [MEDIUM] (ux-confusion) Rebuild mode trims behaviors to "a few essentials" but the FULL supplement stack still renders and blocks "day complete"
- Persona: long-gap-returner
- Area: src/app/today/page.tsx:781-786 (dayComplete) + 2079-2114 (supplement render) ; src/lib/supplements.ts supplementsForBlock ; src/lib/engine.ts:1768-1819 (rebuild shaping)
- Evidence: Rebuild shaping mutes everything except 3 high-leverage BEHAVIORS (engine.ts:1768-1819, muteReason "rebuild mode: keeping 3 high-leverage behaviors"). But supplements live in a separate array (state.supplements) and render via SupplementBlockCard with zero adaptation-mode awareness — supplementsForBlock(all, block, dayIndex, flags) (supplements.ts) only filters by block/daysActive/contraindication, never by adapt mode. And dayComplete requires every scheduled supplement handled: `dayComplete = isToday && (prog.total>0 ? prog.done===prog.total : suppProgToday.total>0) && allSuppHandledToday` (today/page.tsx:781-786), where allSuppHandledToday is true only when every supplement is taken-or-skipped (today/page.tsx:743-758). So a returner whose rebuild banner promises "I've trimmed today to a few essentials; the rest will catch back up on its own" (engine.ts:1012) is still shown their entire supplement regimen (could be 5-10 cards) and cannot reach "You've closed the day" until all of them are individually handled.
- Why: The persona's contract is a low-friction re-entry ("few essentials," "no punishing zero-days"). Trimming behaviors to 3 while leaving the full pill stack — and gating the day's completion reward on it — half-delivers that promise: the banner says light, the actual checklist says full. A returner reasonably expects "the rest catches up later" to include their supplements.
- Fix: Make supplements respect rebuild (and essentials/recovery) the way behaviors do. Either (a) in rebuild/essentials mode collapse the supplement stack into an optional "Supplements (resume when ready)" section that does NOT block dayComplete (drop allSuppHandledToday from the completion gate when adaptation.mode is rebuild/essentials), or (b) at minimum soften the rebuild banner copy so it doesn't claim "the rest will catch back up on its own" while the full supplement stack is still required for completion.
- Verify: Confirmed open against live code. Rebuild shaping at engine.ts:1811-1818 mutes all but 3 BEHAVIORS (muteReason "rebuild mode: keeping 3 high-leverage behaviors" verbatim), and the 40-day-gap banner at engine.ts:1012 promises "I've trimmed today to a few essentials; the rest will catch back up on its own." But engine.ts:222 (`if (isSupplementBehavior(b)) continue;`) excludes supplements from compileTimeline entirely, so shapeTimeline/rebuild never touches them. supplements.ts:303-317 supplementsForBlock filters ONLY by block/daysActive/contraindication — it has no mode/adapt param, and grep for supplement+mode/rebuild/adapt in today/page.tsx returns nothing, so the SupplementBlockCard render (today/page.tsx:2079-2084) is fully mode-blind. today/page.tsx:743-758 allSuppHandledToday is true only when EVERY scheduled supplement across all blocks is taken-or-skipped, and today/page.tsx:781-786 dayComplete requires allSuppHandledToday. So a rebuild-mode returner is shown their entire (un-trimmed) supplement stack and cannot reach "day complete" until all of it is individually handled — the banner says light, the checklist stays full. Not in the ALREADY-FIXED list (the "supplement-only day completes" fix addresses a different case and in fact reinforces that supplements gate completion); distinct from KNOWN-DEFERRED #184 (take/undo symmetry). chaotic-returner.test.ts exercises rebuild mode for this exact persona but only asserts banner/mode/streak, never supplement trimming, so nothing guards this. Severity medium-leaning-low: supplements are still individually skippable and the day does close once handled, so it's a promise/UX-coherence gap, not a hard block.
### [MEDIUM] (bug) Mastery streak treats an override/atom-scheduled weekend-only behavior as daily — it can never graduate
- Persona: weekend-only-custom
- Area: src/lib/engine.ts masteredKeys() daysActiveFor lookup (lines 1580-1595, used in streak walk 1648-1654)
- Evidence: masteredKeys builds its scheduling lookup ONLY from the official catalog: `const allBehaviors = activePacks().flatMap(p => p.behaviors)` then `daysActiveFor.set(b.canonicalKey, b.daysActive)` (engine.ts:1580-1585). It ignores BOTH state.behaviorOverrides[k].daysActive AND state.customPacks. So for a behavior the persona scheduled Sat/Sun via the BehaviorSheet 'Active days' toggle (which writes behaviorOverrides[key].daysActive, see protocols/page.tsx:1527-1529 + BehaviorSheet.tsx:474-477), isScheduledOn() (engine.ts:1591-1595) finds no entry → falls back to `return true` (line 1593) → every weekday is 'scheduled'. The streak walk (1648-1654) then breaks on the first weekday with no completion (`else break`). I proved this with a probe: an official behavior (catalog daysActive undefined) re-scheduled weekend-only via override, with 200 days of PERFECT weekend completion (~28 weekends), still returns masteredKeys(...).has(key)===false. By contrast behaviorAdherence (intel.ts:1142-1166) and keystone/analyticsItems (intel.ts:50-62) both read the COMPILED item's daysActive (which engine.ts:234 merges from the override) and get it right — only masteredKeys re-derives from the raw catalog.
- Why: Atom-library picks set weekend-only (custom:/fork: WITH derivedFrom are mastery-eligible per isCustomTier at engine.ts:1631-1637) and official behaviors rescheduled weekend-only can NEVER reach maintenance, no matter how perfect the weekend adherence. The whole graduate-to-maintenance feature is silently dead for any weekend-scheduled behavior — the exact thing this persona does. Pure free-text customs dodge it only because they're mastery-exempt.
- Fix: Derive scheduling the same way every other intel surface does: read daysActive off the COMPILED timeline (which already folds in behaviorOverrides + customPacks via engine.ts:234), not off activePacks() alone. Concretely, replace the catalog-only daysActiveFor build (engine.ts:1580-1585) with a per-key map seeded from compileTimeline(state, d) across d=0..6 (mirror analyticsItems in intel.ts:50-62), or pass an override/customPack-aware lookup. Then isScheduledOn honors the user's weekend-only schedule and the streak walks scheduled weekends only.
- Verify: CONFIRMED OPEN. activePacks() (knowledge.ts:196-207) returns only the healed bundle + built-in PACKS; it takes no `state` arg and has zero `customPacks` references (grep empty), so it cannot see per-user scheduling. masteredKeys (engine.ts:1580-1585) builds daysActiveFor exclusively from activePacks() catalog `b.daysActive`; the function body (1565-1720) references `behaviorOverrides` zero times, and `customPacks` only at 1626 for derivedFrom tier classification, NOT scheduling. isScheduledOn (engine.ts:1591-1595) therefore falls back to `return true` (1593) whenever a key's catalog daysActive is undefined/missing — which is exactly the case for (a) an official behavior re-scheduled weekend-only via behaviorOverrides[k].daysActive, and (b) any custom:/fork: atom-library pick living in state.customPacks. The streak walk (1648-1654) then `else break`s on the first uncompleted weekday, so a weekend-only behavior can never accumulate 21 scheduled-day completions. The divergence is real: the compiled timeline DOES honor the override at engine.ts:234 (`daysActive: ov?.daysActive ?? b.daysActive`), so masteredKeys is the only place that re-derives schedule from the raw catalog. Override write path verified: BehaviorSheet.tsx:477 patch({daysActive}) -> protocols/page.tsx:1528 setBehaviorOverride. Custom/fork atoms with derivedFrom are mastery-ELIGIBLE (isCustomTier at 1631-1637 returns false for them, so line 1640 does not `continue`), confirming they hit the bug too. Feature is live: masteredKeys -> freshlyMastered (1542) and the `mastered` set drives shapeTimeline graduation at 1864-1873 (muteReason "graduated to maintenance"), called at 937. This is NOT covered by the session's "all-days union" protocols-display fix (a different surface) nor by activePacks() healing (overlays catalog metadata, not per-user overrides/customPacks).
### [MEDIUM] (bug) Background push reminders for a weekend-only behavior fire every day (server has no day-of-week dimension)
- Persona: weekend-only-custom
- Area: src/app/api/push/send-due/route.ts (108-115) + subscribe/route.ts (77) + Reminders.tsx Effect 1 (62-123)
- Evidence: The stored subscription is a flat list of clock times with no weekday: push_subscriptions.reminder_times is string[] of 'HH:MM' (subscribe/route.ts:54,77). The cron sends to any sub whose times include the current minute in its tz — `const hm = hmInTz(tz, now); if (times.includes(hm)) due.push(s)` (send-due/route.ts:112-114) — with zero day-of-week check. Reminders.tsx Effect 1 computes reminderTimes from `compileTimeline(state, dayIndexInTz(tz))` i.e. TODAY only (Reminders.tsx:65,78), so when the persona opens the app on Saturday its weekend behavior's time (say 09:00) is pushed to the server, and the cron then pings 09:00 EVERY day including Mon–Fri. It self-corrects only if the user re-opens the app on a weekday (re-subscribe drops the time) — but a user who doesn't open it Mon–Fri keeps getting weekday pings for a Sat/Sun-only behavior. The in-tab path (Effect 2) is fine because it recompiles live.
- Why: Background reminders are the entire point of a PWA habit app; a weekend-only behavior nagging the user every weekday morning directly contradicts the schedule the user set and erodes trust in the reminder system. Conditional on Supabase + web-push being configured (env-gated), which the live deployment uses (it serves a published CMS bundle).
- Fix: Give reminders a weekday dimension. Minimal: change reminder_times to carry day info (e.g. store {time, days:boolean[7]} or a per-weekday list) and have send-due also check the weekday in the sub's tz before sending. Cheaper interim: have Reminders.tsx Effect 1 only subscribe times for behaviors active EVERY day (skip any item whose daysActive isn't all-true) so partial-week behaviors rely on the live in-tab path until the schema is day-aware — at least it stops false weekday pings.
- Verify: CONFIRMED real and open against live code. The server-side push pipeline has no day-of-week dimension: (1) supabase/schema.sql:460-483 — push_subscriptions stores only `reminder_times text[]` + `timezone`, no weekday column; (2) send-due/route.ts:112-114 — cron computes `hm = hmInTz(tz, now)` and fires `if (times.includes(hm))` with ZERO weekday check; (3) subscribe/route.ts:77 — upsert blindly overwrites reminder_times with the client-sent array, so a given day's times persist in the DB until the next subscribe call. On the client, Reminders.tsx Effect 1 builds times from `compileTimeline(state, dayIndexInTz(tz))` (Reminders.tsx:65,78), and dayIndexInTz defaults to NOW (tz.ts:121-125), while engine.ts:397 filters the timeline to `it.daysActive[dayIndex]` — i.e. TODAY only. So if the user opens the app on a day a day-restricted behavior is active, that behavior's HH:MM is pushed to the server and the cron then pings it EVERY day until a re-open recomputes a different sub `key` (Reminders.tsx:120-122). Effect 1's deps (Reminders.tsx:124-133) contain no timer/midnight trigger, so there is no autonomous correction. Real day-restricted TIMED behaviors exist (exercise.ts:43,63,103 MWF/Tu-Th/Saturday workouts; packs.ts:954,993,2548 single-day), and workouts carry real anchored times so effectiveMinutes is non-null and they ARE pushable. NOT in the ALREADY-FIXED list (that batch touched resolveReminderMinutes window-clamp/quiet-hours/copy and Protocols all-days-union *display* reachability — neither adds a push day dimension) and NOT in KNOWN-DEFERRED. Severity medium not high: the two cleanest 'weekend' examples (cgm-tracking, quarterly-labs at packs.ts:2406,2422) are block:"anytime" and likely skipped as untimed (Reminders.tsx:99), and the defect self-heals the moment the user opens the app on another weekday — bounded blast radius for daily-active users — but a user who skips weekdays keeps getting weekday pings for a Sat/Sun-restricted timed behavior, contradicting their schedule.
### [MEDIUM] (nonsense-but-works) Vacation user with ANY supplements never sees the "You're on a break" surface — Today renders fully active
- Persona: vacation-mode
- Area: src/app/today/page.tsx:928-1008 (hasSupplementsToday gate) + src/lib/supplements.ts:303-317 (supplementsForBlock) + src/lib/engine.ts:181
- Evidence: engine.ts:181 `if (state.settings?.vacationMode) return [];` empties the behavior timeline, so the calm break surface SHOULD show. But today/page.tsx:939 gates it on `if (timeline.length === 0 && !hasSupplementsToday)`. `hasSupplementsToday` (928-938) is computed from `supplementsForBlock(state.supplements ?? [], ...)` which (supplements.ts:303-317) filters ONLY by block / daysActive / safety flags — it has NO vacationMode check. So any user who has added a supplement stack (Magnesium, Omega-3, CoQ10, etc. via the Supplements tab) keeps `hasSupplementsToday === true` during vacation, the `if` is false, and the FULL Today timeline render (line 1012+) runs instead: greeting, date scrubber, 'The week ahead', supplement pills, QuickAdd, etc. The persona promise 'Today cleared / break surface' silently fails for the entire supplement-using segment.
- Why: This is the core vacation contract ('Your day is cleared'). For the very users the app encourages to build a supplement stack, turning on vacation does NOT clear Today — they get the normal working board with no acknowledgement they're on a break. The calm 'moon / You're on a break / Day N' surface is unreachable for them.
- Fix: Make the break surface authoritative on vacation regardless of supplements: in today/page.tsx change the gate to `if (state.settings.vacationMode || (timeline.length === 0 && !hasSupplementsToday))`. Cleaner still: have `supplementsForBlock` (or a thin wrapper used by Today) return [] when `vacationMode` is on, so supplements are 'quiet' during a break exactly like behaviors — then the existing empty-state gate just works and the rest of Today (banner, StreakFreeze, dayComplete) also goes quiet.
- Verify: Confirmed against live code. engine.ts:181 `if (state.settings?.vacationMode) return [];` empties the behavior timeline (so timeline.length===0). But today/page.tsx:939 gates the break surface on `if (timeline.length === 0 && !hasSupplementsToday)`. hasSupplementsToday (today/page.tsx:928-938) is computed from supplementsForBlock(state.supplements ?? [], ...), and supplementsForBlock (supplements.ts:303-317) filters ONLY by block / daysActive / contraindication flags — grep confirms ZERO `vacationMode` references in supplements.ts. So any vacation user with a scheduled supplement has hasSupplementsToday===true, the gate is false, and the entire break surface (today/page.tsx:939-1008, incl. the moon icon, "You're on a break" @986, "Your day is cleared" @995, and "Day N" @990) is skipped — the full active board renders instead (greeting @1075, "The week ahead" @1125, supplement cards @2079, QuickAdd @2930). Grep confirms ALL 12 onVacation/vacationMode refs in today/page.tsx live inside the skipped block (lines 928-1002) — no vacation acknowledgement exists on the active surface, and state.supplements is never filtered by vacationMode in engine.ts or supplements.ts. Real, currently-open, not in the already-fixed list, contradicts the "Your day is cleared" promise. Rated medium not high: behaviors ARE correctly cleared and streak-pause/data-safety mechanics hold (engine.ts:181) — only the calm break UI surface fails to render for the supplement-using segment; no data loss or safety impact.
### [MEDIUM] (bug) Protocols view is WIPED during vacation — every behavior vanishes, stats read "0 behaviors" (includeDisabled escape hatch is dead)
- Persona: vacation-mode
- Area: src/app/protocols/page.tsx:217-241,265-274,757-758 + src/lib/engine.ts:181 vs 212-215
- Evidence: Protocols builds its management list via `compileTimeline(state, d, undefined, { includeDisabled: true })` (protocols/page.tsx:234) precisely so paused rows stay visible/dimmed (the engine.ts:212-215 comment: 'A management surface can pass includeDisabled:true ... otherwise pause is a one-way trip'). But engine.ts:181 `if (state.settings?.vacationMode) return [];` returns BEFORE that opt-in is ever read. So during vacation `manageRows` is empty → the per-block render at protocols/page.tsx:757-758 (`if (items.length === 0) return null`) drops EVERY block → the entire 'Your behaviors' editor disappears. `timeline` (line 218) is also empty, so `sysStats` (265-274) shows behaviors:0, protocols:0, merged:0. The persona's 'Protocols view' (system still legible while frozen) breaks: the user sees their system apparently erased, not paused.
- Why: Vacation is sold as a reversible freeze gentler than Pause ('everything resumes where you left off', profile/page.tsx:407-412). Yet Pause keeps behaviors visible-but-dimmed while Vacation makes them vanish from the system editor entirely and reports 0 behaviors — the opposite of reversible/legible, and alarming for someone deciding whether it's safe to turn vacation on. The pause-reversibility fix this session added includeDisabled but didn't account for the earlier vacation short-circuit.
- Fix: Move the vacation guard so it respects the management opt-in: in engine.ts:181 change to `if (state.settings?.vacationMode && !opts?.includeDisabled) return [];`. That keeps Today empty (Today calls compileTimeline without includeDisabled) while letting Protocols' includeDisabled call still enumerate the (frozen) system so rows render dimmed with honest 'Paused for your break' copy, mirroring the pause UX.
- Verify: CONFIRMED OPEN. engine.ts:181 `if (state.settings?.vacationMode) return [];` returns an empty timeline BEFORE the `opts?.includeDisabled` opt-in is ever consulted (that check is downstream at engine.ts:215). So protocols/page.tsx:234, which calls `compileTimeline(state, d, undefined, { includeDisabled: true })` 7 times specifically to keep paused rows visible, gets `[]` for every day → `manageRows` (226-241) is empty. The per-block render at protocols/page.tsx:757-758 (`const items = manageRows.filter(...); if (items.length === 0) return null`) then drops EVERY block, and I confirmed via grep that this per-block guard is the ONLY empty-state in the management list — there is no aggregate `manageRows.length === 0` fallback and no `vacation`/"on a break"/"No behaviors" guard anywhere in protocols/page.tsx (only unrelated catalog/atom-search empty-states at 591/1109). `timeline` (line 218) is likewise empty, so `sysStats` (266-274) computes behaviors:0/protocols:0/merged:0, and the unconditional "Your orchestrated system" panel literally renders "0 protocols resolved into 0 clear behaviors" (protocols/page.tsx:713-718). The whole orchestrated-system section (line 699) and BLOCKS.map (754) are inside the default `viewMode === "yours"` branch with no vacation conditional — yet the installed-pack cards above (section at 613, which reads `installedPacks`, not the timeline) still render, producing the contradictory state: protocols shown as installed, but "0 behaviors" and an empty editor below. This contradicts the reversibility/legibility promise in profile/page.tsx:407-412 ("everything resumes where you left off") and silently defeats the management-surface contract documented in engine.ts:212-215. Not in the already-fixed list: the same-session pause fix added `includeDisabled` but the earlier vacation short-circuit at line 181 sits above it, exactly as the finding states; not known-deferred; not intended (the empty Today timeline is intended/tested, but wiping the Protocols editor and reporting 0 behaviors is an unhandled inconsistency, with no vacation messaging on this page). Downgraded from high to medium: non-destructive and fully reversible — installedPacks/behaviorOverrides/logs are untouched (engine.ts:181 only suppresses the computed view), nothing wrong is written to state, the editor returns intact the instant vacation is toggled off, and the installed protocol cards remain visible the whole time. It is a legibility/UX regression, not data loss.
### [MEDIUM] (nonsense-but-works) StreakFreeze prompt fires during vacation — "Off day? Spend a freeze to keep your streak" while the streak is already frozen, and it burns a real token
- Persona: vacation-mode
- Area: src/app/today/page.tsx:1185-1194 + src/components/today/StreakFreeze.tsx:57 + src/lib/scoring.ts:218-235 + src/lib/storage.ts:955-962
- Evidence: On the supplement-bearing vacation Today (see finding #1), StreakFreeze renders (today/page.tsx:1185 gates only on `isToday && !restDay`, NOT vacationMode). Its self-gate (StreakFreeze.tsx:57 `if (streak < 3 || todayActive || fz.available < 1) return null`) passes during vacation: `liveStreak` stays ≥3 (calculateStreak treats vacation days as transparent, scoring.ts:117/131) and `todayActive` is false (no logs). So the user is told 'Protect your N-day streak — Off day? Spend a freeze' even though the empty-state copy promises 'your streak is paused'. Worse, tapping 'Freeze today' calls useStreakFreeze (storage.ts:955) which adds today to usedFreezeDates, and freezeStatus (scoring.ts:224-226) counts it against the rolling 2–3/month allowance — wasting a scarce token on a day vacation already protects.
- Why: Direct contradiction of the 'streak frozen' contract and a small but real resource loss. The user is nudged to spend a finite freeze token to protect something the system says is already safe.
- Fix: Gate StreakFreeze (and the flame chip / WeeklyGoal, today/page.tsx:1107,1157) on `!state.settings.vacationMode`. If the supplement-wrapper fix from finding #1 is taken (supplements quiet on vacation → break surface shows), these sub-surfaces never render and this resolves automatically.
- Verify: Confirmed open. StreakFreeze's render gate (src/app/today/page.tsx:1185-1186) is only `isToday && !restDays.includes(selectedDate)` — no vacationMode check. The vacation "streak is paused" empty-state is an early return gated by `if (timeline.length === 0 && !hasSupplementsToday)` (today/page.tsx:939, 963), so a vacation day that still has supplements scheduled falls through to the full render where StreakFreeze is reached. Its self-gate (src/components/today/StreakFreeze.tsx:57 `streak < 3 || todayActive || fz.available < 1`) passes during vacation: liveStreak = calculateStreak(..., getVacationDates(state), settings) (today/page.tsx:271-272) stays >= the preserved pre-vacation streak because vacation dates are transparent (headOk via isVacation(todayStr) at scoring.ts:131; backward walk skips vacation at scoring.ts:142-147), and todayActive (StreakFreeze.tsx:30) is false until today's supplements are logged. Tapping "Freeze today" calls useStreakFreeze (src/lib/storage.ts:955-961), which appends dateKey to usedFreezeDates with NO check that the date already lies inside an open vacationPeriod, and freezeStatus (src/lib/scoring.ts:224-226) counts it against the rolling 2-3/30d allowance. The day was already protected (getVacationDates uses today as the end for end===null periods, storage.ts:917), so a finite token is spent for zero benefit. Two caveats: (1) the "streak is paused" copy and the freeze prompt are in mutually exclusive branches, so it is not a literal same-screen contradiction as worded; (2) it requires the specific combo (active vacation + supplements today + pre-vacation streak >=3 + token available + today unlogged). Bounded resource loss + confusing nudge, hence medium not high.
### [MEDIUM] (inconsistency) Vacation Today shows an adaptive mode banner ("Recovery mode / Easing back") — contradicts "timeline goes quiet"
- Persona: vacation-mode
- Area: src/app/today/page.tsx:1285 (banner gate) + src/lib/engine.ts:1154-1185 adapt()
- Evidence: The focal adaptive banner renders under `{!dayComplete && isToday && (...)}` (today/page.tsx:1285) with no vacationMode check. `adapt(state)` (engine.ts:1154) never inspects vacationMode — it runs getSignals→baselineAdapt and returns a real mode. On the supplement-bearing vacation Today (finding #1), the user therefore sees a prominent 'Recovery mode / Easing back in / Normal day' banner with mode accent and reasons, while the app simultaneously claims they're on a declared break with a quiet timeline. Because no behaviors are logged during the break, gapDays grows and the banner will typically assert an 'easing/recovery' read that makes no sense for someone who intentionally paused.
- Why: The adaptive layer is the app's 'it's thinking about you' voice. Firing it during a user-declared break is incoherent and undercuts the calm-break promise.
- Fix: Add `&& !state.settings.vacationMode` to the banner gate at today/page.tsx:1285 (or short-circuit adapt() to a neutral 'On a break' adaptation when vacationMode is set). Subsumed by the finding #1 wrapper fix if supplements go quiet on vacation.
- Verify: CONFIRMED open. The calm-break empty-state early-return at today/page.tsx:939 is gated on `timeline.length === 0 && !hasSupplementsToday`. compileTimeline returns [] under vacation (engine.ts:181), but `hasSupplementsToday` (today/page.tsx:928-938) calls supplementsForBlock (supplements.ts:303-317), which has NO vacation gating — it filters only by block/daysActive/contraindications. So a vacationing user with any scheduled supplement gets hasSupplementsToday=true, the break empty-state is SKIPPED, and execution falls through to the full Today render (today/page.tsx:1010+). There, the focal adaptive banner at today/page.tsx:1285 is gated only by `{!dayComplete && isToday}` with no vacationMode check; on an unhandled-supplement day dayComplete is false (today/page.tsx:781-786), so it renders. adaptation=adapt(state) (today/page.tsx:578) and adapt() (engine.ts:1154) plus baselineAdapt (engine.ts:1000) never inspect vacationMode and always return a real mode/headline/tone (e.g. "Recovery mode"/"Welcome back…easing back in" at engine.ts:1018-1027) plus signal chips and a mode accent. Net: the app shows a "thinking about you / easing back" banner on the same day it can show "you're on a break" — incoherent. NOTE: one sub-claim is inaccurate — gapDays does NOT grow during the break; getSignals skips vacation days transparently (engine.ts:868-878, `if (!vacays.has(cursor)) gapDays++`), so the specific "rebuild/easing back" copy won't fire from the gap; the banner instead shows normal/recovery/essentials per recovery/adherence/sleep signals. The core inconsistency stands. Severity medium (not high): requires the narrow combo of vacationMode ON + a supplement scheduled for today's day-index/block; behavior-only vacationers correctly hit the calm break state.
### [MEDIUM] (nonsense-but-works) Ticking all supplements on vacation triggers the full "You've closed the day — every behavior done" celebration
- Persona: vacation-mode
- Area: src/app/today/page.tsx:781-786 (dayComplete) + 1196-1240 (celebration) + 743-779 (suppProgToday/allSuppHandledToday)
- Evidence: dayComplete (today/page.tsx:781-786) = `isToday && (prog.total>0 ? prog.done===prog.total : suppProgToday.total>0) && allSuppHandledToday`. On vacation prog.total===0 (empty timeline), so it collapses to `isToday && suppProgToday.total>0 && allSuppHandledToday` — all computed from `state.supplements` with no vacation awareness (743-779). A vacation user who taps their morning supplements done thus flips dayComplete true and gets the celebratory panel: 'You've closed the day … Every behavior, done. This is the quiet, compounding work that actually changes a healthspan.' (1230-1237). Meanwhile that day does NOT count toward the streak (hasAnyActivity, scoring.ts:238-264, reads legacy supplementEntries not supplementCompletions), so the app celebrates 'every behavior done' on a day it simultaneously treats as a frozen no-op.
- Why: A loud completion celebration during a declared break is the opposite of 'you're resting' and is internally inconsistent (celebrated as complete, yet streak-transparent and behavior-empty).
- Fix: Force dayComplete=false when vacationMode is on (e.g. `const dayComplete = isToday && !state.settings.vacationMode && (...)`). Again resolved for free if supplements are made quiet on vacation per finding #1.
- Verify: Verified end-to-end against live code; the finding is real and currently open. (1) engine.ts:181 `if (state.settings?.vacationMode) return [];` makes compileTimeline return [], so today/page.tsx timeline is empty and prog.total===0. (2) supplements.ts:303-317 supplementsForBlock filters only by block/daysActive/contraindication — NO vacation awareness — so a vacation user with scheduled supplements still has them; hasSupplementsToday (today/page.tsx:928-938) is therefore true. (3) The "You're on a break" early-return surface at today/page.tsx:939 is gated `timeline.length === 0 && !hasSupplementsToday`, so with supplements present it is BYPASSED and execution falls through to the main return (line 1012). (4) dayComplete (today/page.tsx:781-786) with prog.total===0 collapses to `isToday && suppProgToday.total>0 && allSuppHandledToday`; suppProgToday (763-779) and allSuppHandledToday (743-758) are computed purely from state.supplements + completions/skips with no vacationMode term. (5) When true, the celebration panel renders at today/page.tsx:1197-1240: "You've closed the day … Every behavior, done. This is the quiet, compounding work that actually changes a healthspan. Rest well." Meanwhile scoring.ts:117 and 142-147 treat vacation days as transparent (streak walks through them), so the same day is celebrated as complete yet counts as a frozen no-op — exactly the internal inconsistency described. Not in the already-fixed list; the prior "supplement-only day completes" change enabled this path without adding a vacation guard. Medium: user-visible tonal/consistency bug reachable only in the vacation+scheduled-supplement state, no data loss or crash.
### [MEDIUM] (ux-confusion) “Link it” silently discards the block the user picked when the linked atom's anchor disagrees
- Persona: custom-pack-author
- Area: src/app/protocols/page.tsx:1213-1247 (Link-it userFields) + pickAtomToDraft:406-429; engine block re-file src/lib/engine.ts:372-394
- Evidence: In the custom-behavior form the block selector defaults to "morning" (bDraft initial state, page.tsx:107-114). When the user types a title that matches a library atom, the "Link it" banner (page.tsx:1196-1247) appears ABOVE the block selector. Clicking it builds userFields = { block: bDraft.block } and ONLY adds customTime when the user typed a time (page.tsx:1217-1221). pickAtomToDraft then does `{ ...atom, ...userFields }` (page.tsx:416-426) — it overrides `block` but NEVER overrides the atom's `anchor`/`offsetMin`. So linking an evening atom (e.g. Magnesium anchor:"bed" offsetMin:-45 packs.ts:73-76; Wind-down ritual bed/-30 :101-104; Screens off bed/-60 :269-272; Dim the lights bed/-120 :286-289; Strategic melatonin bed :334-336) while the block toggle is still on the default "morning" and no time is typed yields a BehaviorDef with block:"morning" + anchor:"bed" + customTime:undefined. At compile, effectiveMinutes() returns the bed-anchored evening clock time (time.ts:30-41 → resolveMinutes:12-24), and engine.ts:392-393 then RE-DERIVES the block from that clock time (`it.block = blockForMinutes(m, settings)`) because blockPinned is false (no override exists; blockPinned needs ov.block, engine.ts:255). Net: the behavior the user filed under Morning silently appears under Evening. Contrast the free-text path (addBehaviorToDraft, page.tsx:357-366) which DERIVES anchor/offsetMin FROM the chosen block, so there the block sticks.
- Why: The whole point of the block toggle in this form is to let the author say when the behavior runs. For the recommended (Link-it) path it is a no-op whenever it disagrees with the curated atom's anchor — and the most common library matches (sleep/evening atoms) are exactly the ones a user is likely to type while the toggle still reads the default "morning". The author sees their morning choice ignored with no explanation, undermining trust in the builder.
- Fix: When the user has explicitly chosen a block in the Link-it path, carry that intent through to the stored behavior, not just the `block` label. Simplest: in the userFields built at page.tsx:1217, when block !== "anytime" and no time was typed, also set anchor + offsetMin to the same block-sensible defaults the free-text path uses (page.tsx:357-366) so resolveMinutes lands in the chosen block. Alternatively, pin the choice by also writing a `blockPinned`-equivalent so engine.ts:392 doesn't re-file it. Either way the author's block selection must survive the linked atom's anchor.
- Verify: Confirmed open. Chain verified end-to-end against live code: bDraft defaults block:"morning" (page.tsx:109); "Link it" sets userFields.block=bDraft.block and only adds customTime if a time was typed (page.tsx:1217-1227); pickAtomToDraft spreads {...atom, ...userFields} overriding block but NEVER anchor/offsetMin (page.tsx:415-426); saveCustom stores draft.behaviors into a custom pack with no behaviorOverride (page.tsx:331). So an evening atom keeps anchor:"bed"+negative offset but block:"morning". At compile ov is undefined → blockPinned=!!ov?.block&&!ov?.customTime=false (engine.ts:255); effectiveMinutes falls to resolveMinutes (time.ts:39-40) yielding the bed-anchored evening time; engine re-derives block since not pinned: `if(it.blockPinned)continue; it.block=blockForMinutes(m,settings)` (engine.ts:392-393), and blockForMinutes returns "evening" near bed (time.ts:287-291). Net: filed under Morning, silently shown under Evening, with no UI explanation. Free-text path derives anchor/offset FROM block (page.tsx:357-366), so block sticks there — contrast confirmed. Design comments (page.tsx:422-424, engine.ts:250-255) show this is the customTime-less-block-is-not-a-pin invariant, so the toggle is a genuine no-op on the recommended path, not intended UX. Caveat: the finding's example list is partly wrong — Magnesium (packs.ts:74) is icon:"pill"/supplement, filtered from the picker (page.tsx:128) and dropped by the engine; Dim the lights (packs.ts:283) and Screens off (packs.ts:266) have timeWindow.strict:true so they reach the same Evening outcome via the strict-clamp branch (engine.ts:381-385), not the soft branch. But Wind-down ritual (packs.ts:101, anchor:bed/-30, no timeWindow, icon:wind) and Strategic melatonin (packs.ts:331, no timeWindow) are valid examples that exercise the exact soft path described. Bug and user-visible symptom are real; medium severity (no data-loss/safety, but silently discards explicit input on the recommended path for the most common evening matches).
### [MEDIUM] (data-risk) “Make it editable” (fork) silently drops every prior per-behavior edit the author made on the official pack
- Persona: custom-pack-author
- Area: src/app/protocols/page.tsx:1451-1477 (Make it editable) → duplicatePack src/lib/storage.ts:1253-1288; override prune storage.ts:283-321; override lookup engine.ts:211
- Evidence: Official-pack behaviors are editable IN PLACE: tapping a behavior in the PackSheet opens BehaviorSheet for any source (page.tsx:1372-1413, no source guard), and edits write setBehaviorOverride(detail.canonicalKey, …) keyed by the PLAIN canonical key (page.tsx:1527-1528). "Make it editable" calls duplicatePack(p) (page.tsx:1459), which re-keys every behavior to `fork:${newId}:${b.canonicalKey}` and removes the source pack from installedPacks (storage.ts:1276-1287) but copies NO behaviorOverrides. The engine looks up overrides strictly by the (now namespaced) canonicalKey — `overrides[b.canonicalKey]` (engine.ts:211), not by derivedFrom — so the fork's behaviors have no override. Meanwhile normalize() prunes the old override because its plain key no longer belongs to any installed pack (storage.ts:288-300). Concretely: an author who set Magnesium dose=400mg / moved Wind-down to 9:30pm on the installed Better Sleep pack, then taps "Make it editable", loses both customizations. The toast says only "…replaces the original" (page.tsx:1463-1465) — nothing warns that personalizations are reset.
- Why: This persona forks expressly to keep customizing a pack. If they tuned doses/times before forking (fully supported in-place), the fork — sold as "convert to an editable copy" — quietly resets those tunings to the curated defaults. Silent loss of user-authored data on an action whose copy implies continuity.
- Fix: In duplicatePack, migrate matching overrides: for each source behavior with an override at its old canonicalKey, copy it to the fork's new key (`fork:newId:oldKey`) in state.behaviorOverrides before returning. (You already namespace the keys; just carry the override map across the same rename.) Optionally also soften the toast to mention edits are preserved once they actually are.
- Verify: Confirmed open. (1) Official-pack behaviors are editable in place with no source guard (protocols/page.tsx:1372-1413); the edit writes setBehaviorOverride(detail.canonicalKey, next) (page.tsx:1528) and detail is a TimelineItem whose canonicalKey is spread from the originating behavior (engine.ts:232), i.e. the PLAIN key for an official pack. (2) duplicatePack re-keys every behavior to `fork:${newId}:${b.canonicalKey}` + derivedFrom (storage.ts:1276-1280) but returns `{...state, customPacks, installedPacks}` (storage.ts:1287) — behaviorOverrides is passed through unchanged, still under the plain key; no override migration. (3) The engine override lookup is strictly overrides[b.canonicalKey] (engine.ts:211); effectiveKey (engine.ts:1197) is used only for the merge key (engine.ts:227), NOT for override lookup — so the fork's `fork:...` keyed behaviors never find the plain-keyed override and render curated defaults. (4) normalize then permanently prunes the orphaned plain-keyed override because it no longer belongs to any installed pack (storage.ts:288-300, run via loadState storage.ts:461-462 / sync datasource.ts:766; the source pack was removed at storage.ts:1284). (5) The toast (page.tsx:1463-1465) mentions only that the copy 'replaces the original' — no warning that per-behavior dose/timing edits reset. The only persona test (custom-pack-power.test.ts:371-387) edits AFTER forking, so it never exercises this edit-then-fork path. Not in the session's fixed list. Severity medium: requires the specific in-place-edit-then-fork ordering and resets to curated defaults rather than corrupting data, but silently discards user-authored personalizations on an action whose copy implies continuity.
### [MEDIUM] (data-risk) Biomarker plausibility guard is one-sided: a ceiling but no floor — a low typo silently poisons bands and the recovery engine
- Persona: biomarker-heavy
- Area: src/lib/biomarkers.ts (BiomarkerDef — no `min`); src/lib/storage.ts:1300; src/app/biomarkers/page.tsx:68
- Evidence: BiomarkerDef declares `max?` (biomarkers.ts:30) but no `min`. The only low-end guard anywhere is `entry.value <= 0` (storage.ts:1300; mirrored in biomarkers/page.tsx:68). So a fat-fingered LOW reading is accepted: e.g. HRV `4` (meant 40) is >0 and < max(400) → stored. biomarkerBand (biomarkers.ts:165-167, higher-is-better) puts 4 in 'Watch'. For a trial/premium user (every new user is in-trial = premium), getSignals gates biomarkerConcern on premium (engine.ts:953) and HRV is in BIO_RECOVERY (engine.ts:734), so the bogus reading flips the day to 'Ease in'/lighter mode — exactly the path biomarkers.test.ts:84 proves for HRV 30. The dedup is per (metric,date), so the bad value only self-corrects if re-logged the SAME day; on a prior day it stays 'latest' (biomarkerConcern picks most-recent within 30d) until superseded or manually deleted.
- Why: The biomarker-heavy persona logs HRV/resting-HR/BP daily and is the one most likely to mis-key a value or paste a wrong-unit number. The code already decided implausible HIGH values must never poison bands/engine (the `max` ceiling, added this session) — but the symmetric low-end risk is unguarded, so a single low typo quietly drives the adaptive day for HRV/restingHR and shows falsely-reassuring 'Optimal' for low 'lower-is-better' readings.
- Fix: Add an optional `min` to BiomarkerDef with physiologic floors (e.g. hrv 5, restingHR 25, systolic 60, diastolic 35, weight 25, vo2max 10, gripStrength 5) and reject `value < min` in BOTH addBiomarker (storage.ts) and the UI submit (biomarkers/page.tsx), mirroring the existing `max` checks and toast ('That looks too low for … — check the value').
- Verify: CONFIRMED OPEN. The plausibility guard is one-sided exactly as claimed. (1) BiomarkerDef declares max? at biomarkers.ts:30 but NO min field — grep for any min/floor across biomarkers.ts, storage.ts, biomarkers/page.tsx, engine.ts returns zero matches. (2) The only low-end guard is `entry.value <= 0` (storage.ts:1300) mirrored in biomarkers/page.tsx:68; a typo'd HRV of 4 is >0 and <max(400) so it passes both and is stored. (3) biomarkerBand (biomarkers.ts:165-167, higher-is-better) puts 4 in 'Watch' (4>=65 no, 4>=40 no). (4) Every new user is premium during trial — entitlements.ts:4-5 and onboarding/page.tsx:311 set premiumTrialEndsAt — so the premium gate at engine.ts:953 opens biomarkerConcern. (5) hrv is in BIO_RECOVERY (engine.ts:734). (6) bioRecoveryFlag + bioConcern flips the day to mode:'lighter', headline 'Ease in' (engine.ts:1075-1081), muting optional behaviors (engine.ts:1820-1826) — the exact path biomarkers.test.ts proves for HRV 30. (7) biomarkerConcern picks the most-recent Watch reading within 30d (engine.ts:746-768) and dedup is per-(metric,date) (storage.ts:1337-1338), so a low typo on a prior day persists as 'latest' until a later-dated reading supersedes it or it's manually deleted. NOT in the ALREADY-FIXED list — that list covers only the high 'plausibility caps' (the max ceiling) and future-date heal; the symmetric low floor was never added, and existing tests cover only the ceiling and <=0, never a low-but-positive typo. Real data-integrity gap, not intended behavior. Severity medium (not high): requires a user mis-key, and the false signal only nudges the day conservatively 'lighter' rather than dangerously escalating, but it does corrupt bands/trends and silently steers the adaptive engine, and produces a falsely-reassuring 'Optimal' for low lower-is-better readings (systolic/diastolic/restingHR via biomarkers.ts:159-162).
### [MEDIUM] (data-risk) normalize() heals future-dated biomarkers on load but does NOT scrub non-positive / above-max readings — the plausibility guarantee has a hole at the import/cloud boundary
- Persona: biomarker-heavy
- Area: src/lib/storage.ts:421-423 (normalize) vs storage.ts:1300-1309 (addBiomarker); parseState storage.ts:458-462
- Evidence: normalize() maps biomarkers ONLY to clamp future dates: `b?.date && b.date > today ? { ...b, date: today } : b` (storage.ts:421-423). It does not drop `value <= 0`, non-finite, or `value > def.max` entries, and does not dedup the (metric,date) collisions its own date-heal can create. Those guards live exclusively in addBiomarker (storage.ts:1300-1309). All imported and cloud-loaded state flows through parseState → normalize (storage.ts:461-462), NOT through addBiomarker — so an implausible value arriving via Import or an older/legacy save survives load and poisons that metric's band, sparkline scale, forecast linfit (analytics.ts:837) and the engine recovery read. The biomarkers.test.ts:91 comment even claims addBiomarker is a backstop 'so it can never poison … even if it slips past the UI guard' — but the load path is exactly the path that bypasses addBiomarker.
- Why: This is the sibling of a this-session fix: the future-date heal was added to normalize at storage.ts:421 to close a load-boundary data-risk, but the equally-poisoning plausibility/positivity cases were not closed at the same boundary. A biomarker-heavy user who imports a backup (or whose old cloud row carried a bad value) gets an un-healed poison reading, defeating the stated 'can never poison' guarantee.
- Fix: In normalize(), filter biomarkers through the same value guard as addBiomarker: drop entries where value is non-finite, <= 0, or (when biomarkerDef(metric).max != null) > max, in addition to the existing future-date clamp. Optionally collapse (metric,date) duplicates last-write-wins so the date-heal can't create same-day twins.
- Verify: CONFIRMED open. normalize() at storage.ts:421-423 maps biomarkers ONLY to clamp future dates (`b?.date && b.date > today ? {...b, date: today} : b`); it does NOT drop value<=0, non-finite, or value>def.max, and does not dedup the (metric,date) collision its own date-heal can create. The positivity/plausibility drops (storage.ts:1300-1309) and the dedup (storage.ts:1337-1339) live exclusively in addBiomarker. Load/import/cloud paths all bypass addBiomarker: parseState->normalize (storage.ts:461-462), importState->parseState (storage.ts:1364-1365), the UI import handler saves the parsed state directly (profile/page.tsx:117,124), and cloud sync normalizes via loadState() (datasource.ts:766). The poison reaches real consumers unfiltered: biomarkerBand() reads raw value (biomarkers.ts:154-168) and the forecast linfit builds points from b.value (analytics.ts:825,836). The test comment at biomarkers.test.ts:88-90 over-claims addBiomarker is a backstop 'so it can never poison ... even if it slips past the UI guard', yet every test (lines 91-108) hits addBiomarker directly and none covers the load/normalize path that actually bypasses it. Genuine sibling gap to the session's future-date heal: the future-date case was closed at the load boundary (storage.ts:421) but the positivity/plausibility/dedup cases were not. Medium, not high: degraded band/sparkline/forecast for one metric, triggered by importing a bad backup or a legacy/out-of-range cloud row, not a crash or data loss and not on the normal UI-entry path.
### [MEDIUM] (dead-option) Per-behavior "Reminder" toggle is a live, ON-by-default control on "anytime" behaviors that can never fire a reminder
- Persona: reminders-quiet-hours
- Area: BehaviorSheet per-behavior reminder toggle (src/components/BehaviorSheet.tsx)
- Evidence: BehaviorSheet.tsx:492-523 renders the Reminder toggle unconditionally (it is a top-level sibling in the sheet body, NOT inside an `item.block !== "anytime"` guard, unlike the time/nudge controls at :289). It defaults ON (`aria-checked={!ov.reminderOff}`, :504) and its caption reads "Notify me at this behavior's time." (:497-498). But for an anytime behavior `effectiveMinutes` returns null (time.ts:38 `if (item.block === "anytime") return null`), and BOTH reminder paths bail on that: in-tab Reminders.tsx:184-185 (`const sched = effectiveMinutes(...); if (sched == null) continue;`) and the server-subscribe path Reminders.tsx:98-99. packs.ts has 39 `block: "anytime"` behaviors, so this is common — and it also hits the persona's real un-healed windowless supplements (recon: melatonin/coq10 ship thin).
- Why: This persona deliberately curates per-behavior reminders. Flipping the green ON switch with the affirmative copy "Notify me at this behavior's time" on an anytime habit is a guaranteed no-op — the behavior has no time and is silently never reminded. The control lies about what it does for a whole class of behaviors.
- Fix: Gate the toggle (or its copy) on whether the item is timed. For `item.block === "anytime"` either hide the toggle, or replace the caption with something honest like "This behavior has no set time, so it won't send a reminder. Give it a time above to enable reminders." Mirror the existing pattern at BehaviorSheet.tsx:289 that already hides the nudge controls for anytime.
- Verify: CONFIRMED open issue. The per-behavior Reminder toggle is rendered unconditionally for "anytime" behaviors, where it can never fire. Evidence read live:

- BehaviorSheet.tsx:493 opens the reminder toggle <div> as a top-level sibling inside the sheet body (space-y-6), with NO `item.block !== "anytime"` guard. Line 492 is only a comment (verified via grep). Contrast BehaviorSheet.tsx:289 where the time/nudge controls ARE guarded by exactly `item.block !== "anytime" && !ov.stackAfter`. So the toggle has no such guard while the timing controls do.
- Defaults ON: BehaviorSheet.tsx:504 `aria-checked={!ov.reminderOff}`; :511 and :517 color/transform also key off `!ov.reminderOff`. With no override, reminderOff is undefined → green/ON.
- Affirmative copy: BehaviorSheet.tsx:497-498 reads "Notify me at this behavior's time." when notificationsEnabled is true. Crucially, notificationsEnabled only swaps the COPY (line 497) — it does not gate whether the toggle renders, so an anytime behavior still shows an ON green switch with this affirmative promise.
- anytime → null time: time.ts:16 (`if (item.block === "anytime") return null` in resolveMinutes) and time.ts:38-40 (effectiveMinutes returns customTime only if present, else resolveMinutes → null).
- Both reminder paths bail on null: in-tab Reminders.tsx:184-185 (`const sched = effectiveMinutes(it, state.settings); if (sched == null) continue;`) and server-subscribe Reminders.tsx:98-99 (identical). Reminders.tsx:101-102 even comments "untimed 'anytime' items stay reminder-free." So the engine knows anytime can never fire, yet the UI promises it will.
- Scope: grep found 39 `block: "anytime"` behaviors in packs.ts, so this hits a common class.

This is NOT covered by the session's "honest copy" / "behavior-editing reminder/snooze honesty" fixes — those addressed the global notifications-off copy (line 497-499) and snooze hard-window honesty, not an anytime guard on the reminder toggle. The toggle still renders ON with "Notify me at this behavior's time" for all 39 anytime behaviors, a guaranteed silent no-op. Medium severity: misleading dead control across a whole class of behaviors, no data-loss/safety impact.
### [MEDIUM] (inconsistency) Background push fires reminders on days a behavior is NOT scheduled (daysActive ignored) and as a generic body — diverges from the day-aware, named in-tab path
- Persona: reminders-quiet-hours
- Area: push_subscriptions.reminder_times flat list (supabase/schema.sql, api/push/send-due, Reminders.tsx Effect 1)
- Evidence: Both Reminders effects build times for TODAY only (dayIndexInTz/dateKeyInTz, Reminders.tsx:65-70 and :149-151), and compileTimeline filters to the current weekday (engine.ts:397 `(it) => !it.daysActive || it.daysActive[dayIndex]`). Effect 1 then stores just a flat `reminder_times text[]` + timezone (schema.sql:471-475; subscribe upsert route.ts:77) with NO day-of-week dimension. The cron (cron-reminders.sql:25 `* * * * *`) calls send-due, which fires whenever the current minute matches any stored time (send-due/route.ts:113-114) — every day, forever, until the subscription is next refreshed (which only happens when the app is opened and `state` changes, Reminders.tsx:121-123/124-133). So a Mon/Wed/Fri-only behavior at 17:00 pushes a generic "Reminder — check Today" at 17:00 on Tue/Thu/Sat/Sun too. The in-tab path correctly suppresses those (day-filtered + named + skips isDone at :181).
- Why: For this persona (per-behavior reminders + smart timing) the background channel silently contradicts the schedule they set: phantom 17:00 nudges on rest days, and a generic body instead of the behavior name. NB: background push is owner-setup-gated (VAPID keys + pg_cron with placeholder secrets in cron-reminders.sql), so it only manifests once the owner wires it — but the schema/code make the divergence unavoidable when they do.
- Fix: Give reminders a day dimension: store `{time, days}` per slot (or a 7-element mask) in push_subscriptions and have send-due check the current weekday in the sub's tz before firing. Re-subscribe at day rollover, not only on state change. Until then, soften the Profile copy that implies the closed-app nudge mirrors Today's schedule (profile/page.tsx:557-562).
- Verify: CONFIRMED REAL and open. The server push subscription stores only a flat HH:MM list with no day-of-week dimension: schema.sql:471 has exactly one reminder field `reminder_times text[]` (verified by grep — no reminder_days/day column exists), and subscribe/route.ts:77 persists `reminder_times: body.reminderTimes ?? []` verbatim. Reminders.tsx Effect 1 builds times for TODAY only (dayIndexInTz at :65, dateKeyInTz at :70, compileTimeline(state, dayIdx) at :78) and flattens to a bare `reminderTimes` array sent via subscribeToPush at :123 — the daysActive filter at engine.ts:397 (`(it) => !it.daysActive || it.daysActive[dayIndex]`) runs at compile time but its weekday context is discarded before storage. send-due/route.ts:113-114 then fires whenever the current minute matches any stored time (`if (times.includes(hm)) due.push(s)`) with no weekday check, and cron-reminders.sql:25 runs `* * * * *` (every minute, every day). The body is generic ("Reminder — check Today for your next behavior." at send-due:127), not the behavior name — the code comment at send-due:23-25 admits per-behavior detail is deferred to "Phase 3.7". By contrast the in-tab path (Effect 2) is day-aware (re-runs compileTimeline live), uses the real title/dose (showNotification(it.title, {body: it.dose...}) at Reminders.tsx:207-208), and skips done items (isDone at :181). So a Mon/Wed/Fri-only 17:00 behavior pushes a generic nudge at 17:00 on Tue/Thu/Sat/Sun too once the owner wires push. NOT among the session's fixes — resolveReminderMinutes (Reminders.tsx:105) only adds window-clamp/quiet-hours, no day dimension. Severity is medium not high because background push is owner-setup-gated: cron-reminders.sql:14,28,31 still has placeholder YOUR_DOMAIN/YOUR_PUSH_CRON_SECRET, so it only manifests after the owner wires VAPID + pg_cron — but the schema/code make the divergence structurally unavoidable when they do.
### [MEDIUM] (inconsistency) Import has zero confirmation while the equally-destructive 'Reset all data' gets a full confirm Sheet — inconsistent guarding of destructive actions
- Persona: import-backup-overwrites
- Area: src/app/profile/page.tsx:114-129 (Import) vs 1002-1011 (Reset confirm Sheet)
- Evidence: Import fires immediately on file pick: the hidden input's onChange (profile/page.tsx:851-855) calls `doImport(f)` directly, and doImport (114-129) saves + reloads with only a 'Data restored' toast — no dialog. Reset, which is no MORE destructive (it preserves identity/trial and tells the user to export), is gated behind a Sheet (1002) titled 'Reset all data?' with body 'This permanently clears your protocols, tracking, biomarkers, and streaks on this device. Consider exporting first. This cannot be undone.' (1007-1011) plus an explicit alert-styled confirm Button. Import can erase the same data with one tap and no warning.
- Why: The user's mental model of 'restore my backup' is additive/safe, the opposite of 'reset'. Giving the genuinely-safe-sounding action LESS friction than the scary-sounding one inverts the safety gradient — the action most likely to be taken casually is the one with no guardrail. A returning user who imports an old export to 'get my stuff back' is exactly the persona that loses data here.
- Fix: Route Import through a confirm Sheet mirroring Reset's, e.g. 'Restore from backup?' / 'This replaces the data currently on this device (and in your account) with the contents of this file. Anything tracked since this backup was made will be lost. Export your current data first if unsure.' — or, preferably, pair it with the merge fix above so the confirm can honestly say 'merge' instead of 'replace'.
- Verify: Confirmed against live code at /Users/rami/Claude/healthkit-website/src/app/profile/page.tsx. Import has zero confirmation: the hidden file input's onChange (lines 851-855) calls doImport(f) directly on file pick, and doImport (lines 114-129) parses via importState then immediately `await activeDataSource.save(parsed)` + reload, with only a "Data restored" toast — no dialog. grep confirms only a `confirmReset` state exists (line 55, set at line 859); there is NO `confirmImport` state. importState (storage.ts:1364-1366) is a pure parser returning a full AppState that REPLACES current state (not additive), so it erases the same protocols/tracking/biomarkers/streaks. Meanwhile Reset is gated behind a Sheet (lines 1002-1011) titled "Reset all data?" with the full warning "This permanently clears your protocols, tracking, biomarkers, and streaks on this device. Consider exporting first. This cannot be undone." plus a confirm Button. So the equally-destructive, safe-sounding Import has zero friction while the scary-sounding Reset has full friction — an inverted safety gradient. Not in the already-fixed list (that covers deleteAccount order and reset rebuild, not Import gating) and not intended behavior. Medium is right: real data-loss footgun but requires a deliberate file pick.
### [MEDIUM] (data-risk) Import bypasses every entitlement clamp — an old/edited backup re-writes tier, trial window, biomarker count and installed-pack count verbatim
- Persona: import-backup-overwrites
- Area: src/lib/storage.ts:179-437 (normalize) & 408-436 return; entitlement guards only at storage.ts:1176 (installPack) and 1315 (addBiomarker)
- Evidence: The import path is parseState → normalize, and normalize NEVER calls getAccess/getFreeBiomarkers/getFreePacks. It merges settings as `settings: { ...d.settings, ...s.settings }` (storage.ts:410) so the file's tier/subscriptionStatus/trialStartDate/premiumTrialEndsAt all win (types.ts:98,166,168 confirm these are plain settings fields). biomarkers (storage.ts:421-423) and installedPacks (191-193) are taken from the file with only future-date / dedupe heals — the free-tier caps that installPack (1176 `if (!getAccess(state).premium)`) and addBiomarker (1315 same) enforce 'so non-UI code paths (cloud sync, import, future API) can't bypass' (their own comments) are NOT applied because import goes through normalize, not those mutators. So importing a backup taken during a trial re-arms premiumTrialEndsAt to that old date (reviving or downgrading entitlements depending on direction), and a hand-edited or pre-cap backup can seat >3 biomarkers / >3 official packs on a free account.
- Why: A direct consequence of the wholesale-overwrite design: the guards the codebase deliberately added to make import/sync tamper-proof are on the wrong functions. A user restoring an older backup can silently flip their own membership state (e.g. land back inside an expired trial, or lose premium framing if the backup predates an upgrade), and the free-tier caps the rest of the app treats as invariants are violable via the Import button.
- Fix: Run imported state through the same clamps before persisting — either fold the getAccess-based caps into normalize() (preferred: it already runs on every load/sync and is the documented hardening seam), or have doImport reconstruct entitlement-bearing fields from the CURRENT state rather than trusting the file (don't import tier/subscriptionStatus/premiumTrialEndsAt; keep the device's). The merge fix in the first finding largely subsumes this if mergeStates' tier/trial reconciliation (datasource.ts:441-449, which already takes the more-premium / later side) is used instead of a raw settings spread.
- Verify: Confirmed open. Import path: importState (storage.ts:1364-1365) → parseState (storage.ts:458-467, calls normalize for v3) → normalize (storage.ts:179-437). normalize merges settings file-wins at storage.ts:410 `settings: { ...d.settings, ...s.settings }`, so the imported file's tier/subscriptionStatus/trialStartDate/premiumTrialEndsAt all override (plain settings fields per types.ts:98-99,166,168). getAccess reads tier + premiumTrialEndsAt directly from state.settings (entitlements.ts:61-62,96-98), so an imported trial date re-arms premium/inTrial. The free-tier caps live ONLY in mutators that import bypasses: installPack getAccess/getFreePacks guard at storage.ts:1176/1188 and addBiomarker getAccess/getFreeBiomarkers guard at storage.ts:1315/1321. A grep confirms those three entitlement symbols (imported at storage.ts:28-31) appear nowhere inside normalize/parseState. In normalize, installedPacks is dedupe-only (storage.ts:191-193) and biomarkers gets only a future-date heal (storage.ts:421-423) — no count cap. The consuming side does not re-clamp either: profile/page.tsx:117-124 calls importState then activeDataSource.save(parsed); LocalDataSource.save (datasource.ts:45-46) just calls saveState (storage.ts:573). The guards' own comments (storage.ts:1174-1175 and 1313-1314: 'non-UI code paths (cloud sync, import, future API) can't bypass') are on the wrong functions — import never routes through them. So a hand-edited/old/pre-cap backup can seat >3 biomarkers / >3 official packs and re-arm an expired trial on a free account. Not in the ALREADY-FIXED list (that session's biomarker heal was future-date + plausibility clamp, present at 422/1307, which is NOT a free-tier count cap; the session's import fix was the customPack namespace guard at 228-281, orthogonal). Severity medium is right: entitlements are client-side/local only (Stripe inert), so this is a local-trust product-invariant integrity gap, not server-side privilege escalation.
### [MEDIUM] (inconsistency) Coverage gap let the installedPacks asymmetry ship: the 2026-06-09 gate test asserts supplements/overrides are 'meaningful' but never installedPacks
- Persona: guest-packs-only-merge
- Area: src/lib/__tests__/data-safety-merge.test.ts:58-91 — 'first-sign-in conflict gate sees config slices (audit 2026-06-09)'
- Evidence: The test block added with the fix (data-safety-merge.test.ts:58-91) verifies slicesDiffer flags supplements (line 62-66), customPacks + behaviorOverrides (68-78), and that hasMeaningfulData counts supplements and overrides as meaningful (84-91) — but there is NO case asserting hasMeaningfulData for an installedPacks-only divergence, and no end-to-end case that a packs-only guest reaches the conflict prompt rather than cloud-wins. The block comment itself (line 59) reads 'Before the fix the gate compared only logs/biomarkers/installedPacks' — so installedPacks was explicitly in scope of the area being fixed, yet it was dropped from hasMeaningfulData and never re-tested. cloudSync.test.ts:260 ('conflict prompt fires once') exercises a data-bearing local, not packs-only. So the finding above has zero regression protection and was masked at fix time.
- Why: This is why a same-commit 'data-loss fix' shipped with the adjacent data-loss hole still open — the test enumerates three of the four divergence slices and omits the fourth, giving false confidence that the conflict gate is now complete.
- Fix: Add two assertions alongside the fix: (1) a packs-only state is treated as conflict-worthy by the gate (whatever predicate is chosen — e.g. the new installedPacksDiverge, or hasMeaningfulData if installedPacks is folded in), and (2) an integration-style assertion that chooseCloudLoad / the load path does NOT silently return cloud for a guest whose only divergence is installedPacks. Mirror the existing slicesDiffer installedPacks case so both predicates are tested symmetrically across all four slices.
- Verify: Confirmed against live code. (1) hasMeaningfulData (src/lib/datasource.ts:183-191) checks dailyLogs/biomarkers/customPacks/supplements/behaviorOverrides but NOT installedPacks. (2) slicesDiffer (datasource.ts:193-208) DOES check installedPacks (lines 198-199). (3) The gate at datasource.ts:744-745 is hasMeaningfulData(local) && slicesDiffer(local, cloud) — an AND — so a guest whose ONLY divergence is installedPacks passes slicesDiffer but fails hasMeaningfulData, gets no conflict prompt, and falls through to cloud-wins (line 763). The asymmetry is real and latent. (4) The cited test block (src/lib/__tests__/data-safety-merge.test.ts:58-91) asserts slicesDiffer for supplements (62-66) and customPacks+behaviorOverrides (68-78), and hasMeaningfulData for supplements+overrides (84-91), but has NO installedPacks-only case for either function — exactly as the finding states, despite the block comment at line 59 naming installedPacks as in-scope of the fix. (5) hasMeaningfulData is referenced in tests ONLY in this file (verified by grep across src/), only via the supplements/overrides cases. (6) cloudSync.test.ts T4 'conflict prompt fires once' (line ~260) seeds local with dailyLogs+biomarkers; installedPacks there is the shared makeState default (cloudSync.test.ts:178), so it is NOT a packs-only divergence and gives zero regression protection for this path. Every checkable claim in the finding holds; this is a currently-open coverage gap, not in the ALREADY-FIXED or KNOWN-DEFERRED lists. Medium is fair: the test-coverage/inconsistency claim is unambiguously true, though the practical data-loss blast radius of a packs-only guest is small (pack lists union on mergeStates at datasource.ts:490-492, and a guest with no logs/supps/overrides has little to lose).
### [MEDIUM] (safety) SupplementSheet modal has ZERO aria attributes — day toggles, When-pills and the inputs are unnamed/stateless to AT
- Persona: a11y-authed-surfaces
- Area: src/components/SupplementSheet.tsx (whole file; 103-118, 132-157, 67-128)
- Evidence: `grep aria SupplementSheet.tsx` → no matches at all. The Days-active row (133-153) maps `DAYS=['M','T','W','T','F','S','S']` (line 28) to buttons whose only text is a single letter (duplicated T/T, S/S) with NO aria-label and NO aria-pressed — on/off is conveyed purely by `background: on?'--warm':'--surface-2'` (148-150). The When block-pills (103-118) likewise carry no aria-pressed (state only via background, 110-113). Name/Dose/Brand/timing/Notes inputs (67-127) use `<Eyebrow>` (a <p>) as their visible label with no htmlFor/id or aria-label. A screen-reader user editing a supplement hears 'T button', 'T button', 'S button'… with no day name and no selected state, and edit fields announced only by placeholder.
- Why: This is a core editing surface (every curated + custom supplement routes through it) and it is completely opaque to assistive tech: you cannot tell which days are active, which timing block is selected, or which field you're in. axe rates these as serious (toggle-button name/state, label) — never run here because the sheet lives only on authed routes.
- Fix: Per day button add `aria-label={fullDayName[i]}` (Monday…Sunday — also disambiguates the duplicate letters) and `aria-pressed={on}`. Give the When-pills `aria-pressed={on}` (or reuse the Segmented primitive). Associate each input with its label via `id`+`htmlFor` or an `aria-label`. Same fix pattern as BehaviorSheet below.
- Verify: Confirmed against live code. `grep -iE "aria|htmlFor|id=|role="` on src/components/SupplementSheet.tsx returns NO MATCHES — the file has zero accessibility attributes. Days-active buttons (SupplementSheet.tsx:133-156) map DAYS=["M","T","W","T","F","S","S"] (line 28, note duplicated T/T and S/S) to <button>s with only a single-letter text child; on/off state is conveyed purely via inline style background --warm vs --surface-2 (lines 148-151) with no aria-pressed and no aria-label. When-block pills (lines 103-118) likewise carry no aria-pressed; selection is only style background/color (lines 111-112). Name/Dose/Brand/timing/Notes inputs (lines 67-126, 238) are labeled only by <Eyebrow>, which renders <p className="t-eyebrow"> (src/components/ui/index.tsx:56-60) — no htmlFor/id/aria-label association; Dose/Brand/timing/Notes have only a placeholder. I also confirmed the parent Sheet (ui/index.tsx:208-306) provides a focus trap + Escape but renders no role="dialog"/aria-modal/aria-labelledby (the `title` prop is visual only), so dialog semantics are weak too. This is a genuine, currently-open WCAG/axe-serious defect (toggle-button name+state, form-field label) on a core editing surface and is NOT in the already-fixed or known-deferred lists. Severity downgraded to medium: it is a pure AT/accessibility defect with no data-loss, no functional break for sighted/pointer users, and despite the finding's "[safety]" tag it is not a health-safety issue.
### [MEDIUM] (safety) BehaviorSheet 'Active days' + 'When' controls: single-letter buttons, no aria-pressed, no day names
- Persona: a11y-authed-surfaces
- Area: src/components/BehaviorSheet.tsx:467-489 (Active days), 186-228 (When pills)
- Evidence: Active-days buttons (469-487) render `DAYS=['M','T','W','T','F','S','S']` (line 24) as the button's only content, with no aria-label and no aria-pressed; selected = `background: on?color:'--surface-2'` (480-483). The When block-pills (196-226) set `style.background = on?color:'transparent'` (219-221) with no aria-pressed — selection is colour-only. `grep aria-pressed BehaviorSheet.tsx` → none; only the stack <select> (331) and reminder switch (503-505) carry aria. So a screen-reader user toggling a behavior's schedule hears two identical 'T' and two identical 'S' buttons with no on/off state, and can't perceive which time block is active.
- Why: BehaviorSheet is THE per-behavior editor opened from Today and Protocols (the freshly-shipped workout-swap entry point lands here too, BehaviorSheet.tsx:580-588). Schedule editing is unusable non-visually. Serious axe violations (toggle-button-name, button state) that the critical-only scan on public routes never reaches.
- Fix: Add `aria-label` with the full weekday name and `aria-pressed={on}` to each Active-days button; add `aria-pressed={on}` to the When pills (forbidden ones already get a title — also set `aria-disabled`). Consider replacing the When pill-row with the existing Segmented primitive which already does aria-pressed correctly.
- Verify: Confirmed open against live code. Active-days buttons (BehaviorSheet.tsx:472-486) render only `{d}` from DAYS=['M','T','W','T','F','S','S'] (line 24) — two identical 'T' and two identical 'S', state conveyed solely by background color (lines 481-482), no aria-label, no aria-pressed. When pills (lines 196-226) render `{b}` (line 225) with state solely via background (line 220), no aria-pressed (the block name itself IS readable via CSS capitalize, so the precise gap is missing toggle-state semantics). grep over the whole file (verified) shows the ONLY aria attributes are line 331 (stack <select> aria-label) and lines 503-505 (reminder switch role/aria-checked/aria-label) — exactly as claimed; neither control group has any aria. The onSwap workout-swap button is in this same sheet (lines 580-588), corroborating it's the swap-flow editor. Not covered by any item in the 'already fixed this session' list. A genuine serious-tier axe violation (toggle-button-name + button state). Severity downgraded from the claimed 'high' to medium: it's a real, confirmed, serious a11y defect, but it degrades screen-reader access rather than producing harmful health guidance, data loss, or money errors, which is what 'high' is reserved for in this codebase.
### [MEDIUM] (safety) Protocols 'Your system / Discover' tab control conveys the active tab by colour only (no aria-pressed/role=tab)
- Persona: a11y-authed-surfaces
- Area: src/app/protocols/page.tsx:460-484
- Evidence: The segmented control (472-483) renders two <button>s whose active state is `style={{ background: viewMode===m.id?'--text-1':'transparent', color: … }}` (476-479) with NO aria-pressed, no role='tab'/aria-selected, no aria-current. There is no programmatic signal of which half (Your system vs Discover) is showing. The comment at 457 even calls it a 'Segmented control', but it does not use the Segmented primitive (ui/index.tsx:159-189) which correctly sets aria-pressed.
- Why: A non-visual user can switch tabs but gets no confirmation of the current view — they must infer it from the changed content list. This is the main IA switch on the merged Protocols/Library hub. Serious, and a clean inconsistency: the codebase already has the accessible primitive and uses it in Profile (theme) and GoalsCard.
- Fix: Replace the hand-rolled buttons with `<Segmented options={[{value:'yours',label:'Your system'},{value:'discover',label:`Discover · ${n}`}]} value={viewMode} onChange={setViewMode} />`, or minimally add `aria-pressed={viewMode===m.id}` to each button.
- Verify: Confirmed open. src/app/protocols/page.tsx:472-483 renders the two "Your system"/"Discover" buttons with only onClick + className + inline style (active state = background/color swap at 477-478 on viewMode===m.id). grep for aria-pressed/role/aria-selected/aria-current in this file returns nothing relevant — the sole role= is at line 829 (role="switch", an unrelated control). The accessible primitive Segmented exists at src/components/ui/index.tsx:159-189 and sets aria-pressed={active} (line 176), and is genuinely used elsewhere: src/app/profile/page.tsx:200 (theme) and src/components/GoalsCard.tsx:178. So the hand-rolled control at 460-484 reimplements the wrapper inline and omits the programmatic active-state signal — a real WCAG state-exposure gap and a clean inconsistency with the codebase's own primitive. Not in the already-fixed protocols list. Medium is right: tab switching still functions; only non-visual confirmation of the current view is missing (non-blocking).
### [MEDIUM] (safety) Daily check-in sleep/energy rating buttons: selected rating signalled by colour only (no aria-pressed / radiogroup)
- Persona: a11y-authed-surfaces
- Area: src/components/today/DailyCheckInCard.tsx:90-131
- Evidence: Sleep buttons (96-107) and energy buttons (118-130) set selection via `background: sleepQ===o.q?'--sleep':'--surface-2'` / `energy===o.e?'--readiness':…` with no aria-pressed and no group semantics. The prompts 'How did you sleep?' / 'Energy right now?' are plain <p> (87-89, 109-111), not a fieldset/legend or aria-labelledby for the button group. Button text ('Poor/OK/Great') exists, so they are named — but the chosen value is invisible to AT.
- Why: This is the two-tap morning check-in that drives adapt() — the daily ritual. A screen-reader user can tap a rating but gets no feedback on what's currently selected, and the option groups aren't associated with their question. Serious (selected-state) rather than critical, so untouched by the current scan.
- Fix: Make each option `aria-pressed` (or wrap each row as `role='radiogroup'` with `aria-label` and the options as `role='radio' aria-checked`). Associate the group with its question via `aria-labelledby` pointing at the prompt <p> (give it an id).
- Verify: Confirmed open in /Users/rami/Claude/healthkit-website/src/components/today/DailyCheckInCard.tsx. Sleep buttons (lines 96-107) and energy buttons (lines 118-130) are plain <button>s whose ONLY selected-state signal is the inline style background/color swap (line 101: `background: sleepQ === o.q ? "var(--sleep)" : "var(--surface-2)"`; lines 123-125: `energy === o.e ? "var(--readiness)" : "var(--surface-2)"`). There is no aria-pressed, no role, and the wrapping containers (lines 90 and 112) are bare `<div className="flex gap-2">` with no role="radiogroup"/fieldset/legend/aria-labelledby. The two prompts are plain <p> elements (lines 87-89 "How did you sleep?", lines 109-111 "Energy right now?") with no id or association to the button group. Button text gives each option an accessible name, but the chosen value and the question-to-group association are invisible to AT (WCAG 1.4.1 color-only + 4.1.2 missing selected state). Not in the already-fixed-this-session list and not in known-deferred. Medium is right: serious selected-state/grouping gap on the daily check-in, buttons still operable, screen-reader-specific.
### [MEDIUM] (safety) Biomarker log sheet: 'Value' and 'Date' inputs have no associated label (only a sibling <Eyebrow> <p>); Value autoFocuses unnamed
- Persona: a11y-authed-surfaces
- Area: src/app/biomarkers/page.tsx:256-277
- Evidence: The Value input (259-266) and Date input (270-276) use `<Eyebrow>Value ({open.unit})</Eyebrow>` / `<Eyebrow>Date</Eyebrow>` (258,269) as labels — Eyebrow renders `<p className='t-eyebrow'>` (ui/index.tsx:49-61), not a <label>, and there is no htmlFor/id link or aria-label. The Value field also has `autoFocus` (260), so the sheet opens with focus on a field a screen reader announces only by its placeholder ('0', line 264). This is the bloodwork/body-metric data-entry field.
- Why: Entering a health reading non-visually, you land in a field announced as '0, edit' with no idea it's e.g. 'Resting heart rate (bpm)'. axe flags input-without-label as serious. On an authed-only route, never scanned.
- Fix: Associate the inputs: either `<label htmlFor='bm-value'>` + `id='bm-value'`, or add `aria-label={`Value in ${open.unit}`}` and `aria-label='Date'` directly on the inputs.
- Verify: Confirmed real and open against live code. src/components/ui/index.tsx:57 — Eyebrow renders `<p className="t-eyebrow">`, not a `<label>`. src/app/biomarkers/page.tsx:258 uses `<Eyebrow>Value ({open.unit})</Eyebrow>` and :269 `<Eyebrow>Date</Eyebrow>` as captions only. The Value input (:259-266) has `autoFocus` (:260) and `placeholder={open.placeholder ?? "0"}` (:264); the Date input (:270-276) is `type="date"`. A targeted grep over lines 256-278 found NO aria-label/aria-labelledby/htmlFor/id=/<label>, so neither input is programmatically labeled — the `<p>` Eyebrows are siblings, not linked. Because of autoFocus, the sheet opens with focus on a field a screen reader announces only via its placeholder ("0"), with no indication it is e.g. "Resting heart rate (bpm)". This is a genuine WCAG 4.1.2/1.3.1 (axe input-without-label, serious) issue on the bloodwork/body-metric data-entry field. Not in the already-fixed-this-session list (those were data/safety/engine/copy fixes) nor in KNOWN-DEFERRED. Severity kept at medium: real a11y violation on a health-data field with focus auto-landing there, but it is a single authed-only internal sheet, not high-blast-radius.
### [MEDIUM] (inconsistency) Sleep-schedule + Day-block + onboarding time pickers have no programmatic label (visible text is an unlinked <p>/<Eyebrow>)
- Persona: a11y-authed-surfaces
- Area: src/app/profile/page.tsx:224-238 & 282-296; src/app/onboarding/page.tsx:506-520
- Evidence: Profile Bedtime/Wake `<input type='time'>` (225,234) are labelled by a sibling `<p className='t-caption'>` (223,232) with no htmlFor/aria-label. Day-block 'Starts' time inputs (283) sit under a `<p>` (281) only. Onboarding Bedtime/Wake (507,516) sit under `<Eyebrow>` (505,514) = an unlinked <p>. Notably the SAME file labels its Quiet-hours pickers correctly with `aria-label='Quiet hours start'/'end'` (profile 570,587) — proving the omission is an inconsistency, not the house style. axe: label / `select-name`-class serious violation for the unlabeled time inputs.
- Why: A screen-reader user setting their sleep window — the anchor for the entire timeline — hears 'time, edit' twice with no Bedtime/Wake distinction, both in onboarding and Profile. Inconsistent with the quiet-hours fields right below.
- Fix: Add `aria-label` to each: 'Bedtime' / 'Wake time' (profile 225/234 and onboarding 507/516), and `aria-label={`${label} starts`}` for the day-block 'Starts' inputs (profile 283). Mirror the quiet-hours treatment already in the file.
- Verify: Confirmed open against live code. Profile Bedtime/Wake time inputs (src/app/profile/page.tsx:225,234) are preceded only by sibling <p className="t-caption"> at 223,232 with no htmlFor/aria-label/wrapping <label>. Day-block "Starts" time input (profile/page.tsx:283) sits under only <p>Starts</p> at 281. Onboarding Bedtime/Wake inputs (src/app/onboarding/page.tsx:507,516) sit under <Eyebrow>Bedtime/Wake</Eyebrow> (505,514); Eyebrow renders a plain <p className="t-eyebrow"> (src/components/ui/index.tsx:56-60) with no for/id linkage, so it gives the input no accessible name. The inconsistency is real and verified: the quiet-hours pickers in the SAME profile file ARE labeled via aria-label="Quiet hours start"/"end" (profile/page.tsx:570,587), and grep shows 9 other aria-label uses in that file — so programmatic labeling is the house style and these time pickers are the omission. Not in the ALREADY-FIXED list and not in KNOWN-DEFERRED. WCAG 4.1.2/3.3.2 name gap on the sleep-window flow; medium since controls still function and type=time natively announces role/value.
### [MEDIUM] (safety) Correlation explorer: two native <select>s with no accessible name ('Compare A vs B')
- Persona: a11y-authed-surfaces
- Area: src/components/CorrelationExplorer.tsx:20-40 (Select) used at 144-148
- Evidence: The reusable Select (28-38) has no aria-label and no associated <label>; it's rendered twice in a `grid-cols-[1fr_auto_1fr]` with a literal 'vs' span between (144-147). The only descriptive text is the section Eyebrow 'Correlation explorer' and 'Compare any two signals…' (140-142) — not linked to either combobox. A screen-reader user hears two identical unnamed comboboxes and cannot tell which is the first vs second factor.
- Why: Premium Insights feature; non-visually you can't distinguish the X-factor select from the Y-factor select. axe `select-name` = serious. Authed + premium-gated, so triply outside the current scan.
- Fix: Pass a label into Select (e.g. `aria-label='First signal to compare'` and 'Second signal to compare'), or wrap each in a <label>. Minimal: add an `ariaLabel` prop to the Select component and set it at the two call sites (145,147).
- Verify: Confirmed against live code. CorrelationExplorer.tsx:28-38 defines a bare <select> with only value/onChange/className — no aria-label, no aria-labelledby, no id-linked <label>. It is rendered twice at lines 145 and 147 inside a grid-cols-[1fr_auto_1fr] with a literal "vs" span (line 146) between them, both pulling identical options from listFactors() (analytics.ts:554). A grep of the whole file for aria-label/aria-labelledby/sr-only/htmlFor returned zero matches, and the sole usage site (insights/page.tsx:473-476) wraps it in no labeled container. The descriptive Eyebrow "Correlation explorer" (line 140) and "Compare any two signals…" (line 142) are not programmatically associated with either combobox. A screen-reader user hears two identical unnamed comboboxes and cannot distinguish the first factor from the second — a genuine WCAG 4.1.2 / axe select-name failure. Not in the ALREADY-FIXED list, not KNOWN-DEFERRED, not intended. Medium is right: premium- and auth-gated, does not block the core loop, but breaks the feature for AT users.
### [MEDIUM] (ux-confusion) Bulk "Move to…" silently no-ops on hard-window behaviors (morning-sunlight, wind-down…) while the per-item move menu correctly disables those blocks
- Persona: iphone-se-onboarding-sheets
- Area: Today bulk-action bar / BulkMoveSheet — src/components/today/BulkMoveSheet.tsx + src/app/today/page.tsx
- Evidence: Two move surfaces enforce the same hard-window constraint but give opposite feedback. PER-ITEM move menu (src/app/today/page.tsx:2409-2419) computes `const forbidden = !!it.timeWindow?.strict && !windowBlocks(it, state.settings).includes(target)` and renders the target button `disabled={target === block || forbidden}` (greyed, opacity-40, cursor-not-allowed at :2438) so the user sees morning-sunlight can't go to Evening. BULK path does NOT: BulkMoveSheet.tsx:31 `const BLOCKS = ["morning","afternoon","evening","anytime"]` are rendered unconditionally (:43-52, no `disabled`, no windowBlocks awareness), and the parent handler at today/page.tsx:3025-3045 just builds `moves` and calls `requestBlockMove(moves, b)`. `requestBlockMove` (today/page.tsx:457-464) filters strict-window items that the target can't reach — `const allowed = moves.filter(m => it?.timeWindow?.strict ? windowBlocks(it, state.settings).includes(toBlock) : true); if (allowed.length === 0) return;` — and `commitBlockMove` (:410-428) fires no toast. So selecting only strict-window rows and tapping a forbidden block = silent return (bar vanishes, selection clears via :3043, NOTHING moved, no message); a mixed selection = silent PARTIAL move. The 4 affected behaviors are real default-pack atoms with `strict: true`: morning-sunlight (packs.ts:19, morning-only), wind-down (:91), screens-off (:266), dim-lights (:283, all evening-only) — and per LIVE RECON the load-time heal re-overlays `timeWindow` onto these by canonicalKey, so they ARE strict on the live bundle.
- Why: This persona lives in the bulk-action surface on a narrow phone where tapping the wrong block is easy. They multi-select several morning/evening anchors, tap a block, the action bar disappears, and they reasonably believe everything moved — but the circadian behaviors silently stayed put with zero explanation. The per-item path teaches them why (disabled block); the bulk path leaves them with a phantom no-op. Same constraint, two contradictory UIs.
- Fix: Make BulkMoveSheet window-aware like the per-item menu. Pass the selected items (or a precomputed set of reachable blocks = intersection of windowBlocks across the strict-window members) into BulkMoveSheet, and render forbidden blocks `disabled` with the same opacity-40 treatment. Minimal version: in today/page.tsx compute `const strictSel = [...selectedKeys].map(k=>timeline.find(t=>t.canonicalKey===k)).filter(it=>it?.timeWindow?.strict)` and a `disabledBlocks` set, thread it as a prop; BulkMoveSheet disables those buttons. If any selected item would be dropped, at minimum show a toast ("Kept morning sunlight in Morning — its timing is fixed") instead of a silent return at requestBlockMove:464.
- Verify: CONFIRMED OPEN. The two move surfaces enforce the same hard-window constraint with opposite feedback. PER-ITEM menu: src/app/today/page.tsx:2409-2414 computes `forbidden = !!it.timeWindow?.strict && !windowBlocks(it, state.settings).includes(target)` and line 2419 sets `disabled={target === block || forbidden}` with `disabled:opacity-40 disabled:cursor-not-allowed` (line 2438) — forbidden blocks are visibly greyed. BULK sheet: src/components/today/BulkMoveSheet.tsx:31 `const BLOCKS = ["morning","afternoon","evening","anytime"]` rendered unconditionally (lines 43-52), NO `disabled`, and grep confirms windowBlocks/timeWindow are never imported there. The parent handler src/app/today/page.tsx:3025-3045 builds `moves`, calls `requestBlockMove(moves, b)`, then immediately clears selection + closes sheet (3042-3044). requestBlockMove (457-464) filters strict-window items the target can't reach (`const allowed = moves.filter(...)`) and does a bare `if (allowed.length === 0) return;` (line 464) — no toast; commitBlockMove (410-428) also fires no toast; grep for "toast" in the whole file returns only one unrelated comment at line 631. So all-strict selection -> silent return (bar vanishes, nothing moved, no message); mixed selection -> silent partial move. Affected atoms are genuinely strict in src/lib/packs.ts: morning-sunlight (line 19, {min:0,max:120}), wind-down (91), screens-off (266), dim-lights (283), and the load-time heal re-overlays `timeWindow` by canonicalKey (src/lib/knowledge.ts:154 lists "timeWindow"; healProtocols applied at load, lines 174-206). This is NOT in the ALREADY-FIXED list (that list covers "move-menu disables window-forbidden blocks" — the per-item path only — and the strict-window filter inside requestBlockMove, but neither addresses the bulk sheet's missing disabled state nor adds any silent-drop toast). windowBlocks defined at src/lib/time.ts:262. Real open UX-confusion bug.
### [MEDIUM] (ux-confusion) validateBundleGovernance warnings are computed then silently discarded — they never reach the admin
- Persona: cms-publish-thin-bundle-source
- Area: src/lib/cms/publish.ts publishBundle / PublishResult ↔ src/app/admin/page.tsx publish button
- Evidence: validateBundleGovernance returns {errors, warnings} (publish.ts:584-587,683). publishBundle calls it (line 549) but only consumes validation.errors (550-557); the warnings array is never read. PublishResult's success variant is `{ ok: true; version: number; checksum: string }` (publish.ts:502-504) — no warnings field. The admin UI awaits publishBundle and on success shows only `Published v${r.version} (${r.checksum}) — runtime adopted.` and on failure `r.reason` (admin/page.tsx:5260-5279); it never imports or calls validateBundleGovernance itself. The module docstring claims 'warnings surface in the reason text so the admin sees them' (publish.ts:546-548), but on the ok path there is no reason text and warnings are dropped.
- Why: Even the partial safety/quality net that exists (empty-rationale, evidence-without-tier, avoid-without-targets) is invisible. An admin publishing a new behavior gets a clean 'Published … runtime adopted' toast with zero indication that anything shipped degraded. The docstring is an overclaim relative to the code: warnings are surfaced only when there is ALSO a blocking error, which is exactly when warnings are least relevant.
- Fix: Add `warnings: string[]` to the PublishResult ok variant and populate it from validateBundleGovernance; render them in the admin success path (e.g. an amber 'Published with N notes' list). Or call validateBundleGovernance in the diff preview (previewNextBundle already assembles the same bundle) and show warnings BEFORE the irreversible Publish click. Either way the docstring at publish.ts:546-548 must match reality.
- Verify: Confirmed open against live code. publish.ts:584-587 defines BundleValidationResult {errors, warnings}; publish.ts:683 returns both. But publishBundle reads ONLY validation.errors (publish.ts:550-557) — grep confirms `warnings` appears solely inside validateBundleGovernance's body/return, never in any caller. PublishResult's success variant is {ok:true; version; checksum} with no warnings field (publish.ts:502-504), so warnings cannot exit. admin/page.tsx:5260-5278 awaits publishBundle and on r.ok shows only `Published v${r.version} (${r.checksum}) — runtime adopted.`; on failure shows r.reason; it never imports/calls the validator (grep for validateBundleGovernance/.warnings outside publish.ts returned nothing). The docstring at publish.ts:546-548 ("warnings surface in the reason text") is an overclaim — and actually worse than the finding states: the only reason built from validation maps validation.errors (publish.ts:553-555), so warnings never reach any reason string under ANY path, not just the ok path. The quality net (empty-rationale 627-631, evidence-without-tier 634-637, avoid-without-targets 641-644, inert/ineffective interactions 670-681) is computed then silently discarded. Medium, not high: blocking governance errors (reserved-namespace 616-623, all-false daysActive 647-655, validateAtom failures) still block publish; only non-blocking quality warnings are invisible. Not in the already-fixed-this-session list nor KNOWN-DEFERRED.
### [MEDIUM] (data-risk) Trial/premium is computed purely from device Date.now() with no server-time anchor — rolling the clock back keeps Premium on forever (offline-unbounded)
- Persona: entitlement-clock-skew-rollback
- Area: src/lib/entitlements.ts:60-88 (getAccess); consumers storage.ts:1176,1315; engine.ts:953; insights/page.tsx:60
- Evidence: getAccess(): `const now = Date.now();` (entitlements.ts:63) then `if (now < end) { inTrial = true; ... }` (line 70) and `premium: paid || inTrial` (line 82). There is no comparison against any server/network clock — grep confirms the ONLY Date.now() in the entitlement path is here and at line 104. trialStartDate is never read as a gate (today/page.tsx:801 uses it only for the first-day-soft UI). So a user who sets the device clock backward to before premiumTrialEndsAt makes `now < end` true on every evaluation: getAccess.premium stays true. Every premium gate keys off exactly this — installPack free-cap bypass (storage.ts:1176 `if (!getAccess(state).premium)`), biomarker cap (storage.ts:1315), biomarker-aware adaptation (engine.ts:953 `getAccess(state).premium`), Insights real-time (insights/page.tsx:60). The datasource server-clock baseline (lastCloudUpdatedAt, datasource.ts:792/867) is a document-write concurrency guard ONLY and is never consulted for entitlements, so it provides no protection here. Offline (the documented free feature: 'local/cloud sync + export', and PWA), there is literally nothing that can correct the clock — premium is unbounded.
- Why: The entire freemium conversion mechanic (14-day reverse trial) is defeated by Settings → Date & Time → off. A user holding the clock in the past keeps the full intelligence layer, complete Library (free-cap bypassed in code, not just UI), biomarker-aware adaptation, and unlimited history indefinitely without paying. Because the gate is shared code, it is not a cosmetic bypass — it is the real entitlement.
- Fix: Anchor trial expiry to a monotonic/non-rewindable signal instead of raw Date.now(). Minimum viable: persist a `lastSeenAt` high-water mark in settings on every load/save and, in getAccess, treat `effectiveNow = max(Date.now(), lastSeenAt)` so the clock can advance but never rewind past the furthest point already observed (this kills backward-skew while staying offline-safe). When Supabase is active, prefer the server `updated_at` already read in datasource (readStateUpdatedAt) as the authoritative 'now' for trial math. Document that offline trials use the local high-water mark.
- Verify: Confirmed open against live code. entitlements.ts:63 `const now = Date.now();` then :70 `if (now < end) { inTrial = true; ... }` and :82 `premium: paid || inTrial` — the only two Date.now() in the file are :63 and :104, both client-clock (grep confirmed). No server/network/NTP/trusted-time anchor exists anywhere in src/ (grep for serverTime|networkTime|ntp|clockSkew returned only unrelated engine.ts restraintPack lines). All four cited gates key purely on getAccess(state).premium: storage.ts:1176 (installPack free-cap), storage.ts:1315 (biomarker cap), engine.ts:953 (biomarker-aware adaptation), insights/page.tsx:60. The datasource server timestamp lastCloudUpdatedAt is only an optimistic-concurrency write guard (datasource.ts:831-847 comment + usage at :841-842) and is never imported outside datasource.ts nor consulted for entitlements (cross-file grep empty). trialStartDate is UI-only (today/page.tsx:799-816 firstDaySoft soft-entry), never a gate. So setting the device clock back before premiumTrialEndsAt keeps now<end true on every evaluation → premium stays true unbounded; datasource.ts:446 laterIso even preserves the later trial-end across sync. Not in the ALREADY-FIXED list (those cover maybeExtendTrial one-shot idempotency and biomarker future-date healing, not the entitlement clock-rollback vector). Severity medium not high: it defeats the freemium conversion mechanic (real business-logic gap, shared-code gate) but is not data-corruption/safety, requires deliberate OS-clock manipulation, and is inherent to a by-design offline-first PWA whose Stripe/backend entitlement check is env-gated and inert — there is no server to anchor against in the shipped product.
### [MEDIUM] (bug) One-shot trial-extension guard can be permanently burned by a forward clock jump, then never re-granted on correction
- Persona: entitlement-clock-skew-rollback
- Area: src/lib/entitlements.ts:95-122 (maybeExtendTrial)
- Evidence: maybeExtendTrial fires when `end - now < 3*86_400_000 && end - now > -7*86_400_000` (lines 108-109) AND engagedDays < AHA_DAYS AND `!trialExtendedAt` (line 102). It is called on every load/resync (useAppState.ts:94,173,383). A user ~10 days into the trial who is under-engaged and jumps the device clock FORWARD into that ±window triggers the branch: it sets `premiumTrialEndsAt = new Date(now + 7*86_400_000)` and STAMPS `trialExtendedAt = new Date(now)` (lines 116-119) — both off the skewed `now`. The stamp is permanent: line 102 `if (trialExtendedAt) return state` blocks ALL future extensions. When the user corrects the clock back, the engagement-grace extension they were designed to receive *legitimately* near the real expiry (the whole point of the 'never paywall before the aha' contract, entitlements.ts:5-7) can never be granted — it was consumed by the artificial jump. (Tests cover the honest one-shot and the no-double-extend at entitlements.test.ts:152, but none exercise a skewed `now` burning the guard.)
- Why: The reverse-trial's kindness mechanism (auto-extend under-engaged users so they don't get paywalled before value) is silently disabled for any user whose device clock ever jumped forward through the window — including innocent causes (travel across the dateline, a dead-battery RTC reset to a future epoch, a manual nudge). They then hit a hard paywall at real expiry with the grace already spent, contradicting the documented engagement-gated promise.
- Fix: Same monotonic-now fix as the backward case applies (compute the window off a server/high-water `effectiveNow`, not raw Date.now(), so a forward jump that later corrects doesn't satisfy the window). Additionally, gate the *stamp* on the same anchored clock so trialExtendedAt is only written when a non-skewed now is in-window; and consider storing the pre-extension end so a detected skew correction can roll back an extension that was granted off a future-dated now.
- Verify: CONFIRMED OPEN. entitlements.ts:104 sources `now = Date.now()` raw with no skew sanitization; lines 108-109 define the ±window (end-now < 3d AND > -7d); line 111 gates on engagedDays < AHA_DAYS; lines 116/119 stamp BOTH premiumTrialEndsAt (now+7d) and trialExtendedAt off the skewed now; line 102 `if (trialExtendedAt) return state` then permanently blocks all future extensions. Called on every load/resync exactly as claimed: useAppState.ts:94 (initial load), :173 (resync on STATE_EVENT/focus/visibilitychange), :383 (refresh). The burn is durable, not transient: normalize() (storage.ts:179) heals future-dated *biomarkers* (line 181-185) and clamps biomarkers, but never touches premiumTrialEndsAt/trialExtendedAt — they pass through the generic spread at storage.ts:232, so a clock correction never un-stamps the guard. Worse, the skewed extension persists across sync: datasource.ts:446 merges premiumTrialEndsAt via laterIso() (later wins) and trialExtendedAt falls through the settings spread, so neither resets on correction. Test gap confirmed: src/lib/__tests__/entitlements.test.ts covers honest one-shot (line 119), engaged-no-extend (128), forgiving post-expiry (137), and no-double-extend once stamped (152), but uses no vi.useFakeTimers/setSystemTime — no test exercises a skewed `now` burning the guard. The line 102 one-shot guard was added THIS session per memory, but it is the mechanism being exploited, not a fix for the clock-skew burn; no clock-skew clamp on trial dates exists anywhere. This contradicts the documented 'never paywall before the aha' contract (entitlements.ts:5-7). Medium (not high) because it needs a specific precondition — a forward clock jump into the ~10-day window while under-engaged and before legitimate extension — affecting a narrow population, and the harm is a silently-disabled kindness mechanism (premature hard paywall), not data loss or a crash.
### [MEDIUM] (data-risk) Cross-device merge takes max(premiumTrialEndsAt) — one clock-skewed device inflates the trial for the whole account, with no clamp
- Persona: entitlement-clock-skew-rollback
- Area: src/lib/datasource.ts:446-449 (mergeStates → laterIso on premiumTrialEndsAt)
- Evidence: mergeStates: `premiumTrialEndsAt: laterIso(cloud.settings.premiumTrialEndsAt, local.settings.premiumTrialEndsAt)` where laterIso returns the chronologically LATER ISO (datasource.ts:419-420). Combined with finding #2: a forward-skewed device runs maybeExtendTrial and writes `premiumTrialEndsAt = skewed_now + 7d` (a far-future date if the jump was large). On the next sync, that far-future value is `> ` the honest device's value, so laterIso keeps it and it propagates to the cloud row and every other device. There is no plausibility cap on premiumTrialEndsAt (contrast: biomarker future-date heal + plausibility caps were explicitly added this session per the recon, but no equivalent guard exists for the trial date). So premium across ALL the user's devices is extended to the skew-inflated date, and an honest device can never pull it back (max-wins, monotonic upward).
- Why: Sync is advertised as a free feature, so this is reachable by ordinary multi-device free users. A single mis-set device (or a deliberate one) permanently lifts the paywall account-wide. This is exactly the 'sync interacts with a skewed device' interaction the scenario targets, and it amplifies finding #1/#2 from one-device to all-devices.
- Fix: Clamp premiumTrialEndsAt on merge to a sane horizon relative to the account's trial start, e.g. `min(laterIso(...), trialStartDate + MAX_TRIAL_DAYS)` (mirroring the biomarker plausibility-cap pattern just added). Better, anchor the trial to server time so a client-fabricated future end can't enter the merge in the first place. At minimum, don't let a client-supplied premiumTrialEndsAt that exceeds trialStartDate + (14 + grace) days survive normalize().
- Verify: CONFIRMED OPEN. (1) mergeStates takes the max: datasource.ts:446-449 sets premiumTrialEndsAt = laterIso(cloud, local), and laterIso (datasource.ts:419-420: `new Date(x) > new Date(y) ? x : y`) returns the chronologically LATER ISO. (2) No clamp exists anywhere: grep across datasource.ts, storage.ts and the save path shows premiumTrialEndsAt is referenced ONLY at datasource.ts:446-448 — no Math.min/plausibility cap, and chooseCloudLoad (datasource.ts:531) / save apply none. The session's biomarker future-date heal + plausibility caps did NOT add an equivalent trial guard. (3) Skewed source: entitlements.ts:116 writes premiumTrialEndsAt = new Date(now + 7*86_400_000) where now = Date.now() (device clock, line 104) — a forward-skewed device produces a far-future value. (4) Propagation: useAppState.ts:173 (sync) and :383 (refresh) call setState(maybeExtendTrial(raw)); after a dirty merge load that max-value persists to cloud via the debounced save (useAppState.ts:129), and maybeExtendTrial only ever pushes the date OUT (never shortens) so an honest device can't claw it back, and the next merge's laterIso keeps the larger cloud value — monotonic-upward, account-wide. The maybeExtendTrial one-shot fix (trialExtendedAt guard at entitlements.ts:102) is per-device and does nothing about the cross-device max-merge or the clock-derived write, so this is NOT the already-fixed item. One caveat: a VERY large forward skew fails the window gate at entitlements.ts:108-109 (end-now > -7d), so the unbounded far-future extreme needs a specific skew profile; a modest forward skew still reliably inflates the trial account-wide with no clawback, which is the core defect at the cited lines. Reachable by ordinary multi-device free users since sync is a free feature; impact is paywall/monetization leak (not data loss or safety), hence medium.
### [MEDIUM] (ux-confusion) CorrelationExplorer renders a full interactive 'compare your logs' tool on day zero, reporting '0 of 8 days' while the same page says 'check in for about a week'
- Persona: first-ever-day-zero-empty
- Area: src/components/CorrelationExplorer.tsx + src/app/insights/page.tsx
- Evidence: Insights renders <CorrelationExplorer logs={...} premium={access.premium} /> unconditionally at insights/page.tsx:473-476 — there is NO loggedDays/data gate around it (unlike every sibling card: ConsistencyCalendar gates at loggedDays>=3 (386), MoodEnergyTrends at >=4, BenchmarksCard/WhenConsistent at confident>=14d, PersonalRecords/MonthlyReport/ForecastCard/OnThisDay/JournalHistory all self-hide). CorrelationExplorer.tsx itself has no logs.length guard: after the premium check (line 56) it always renders the two factor <Select> dropdowns + a verdict box. For a brand-new TRIAL user (access.premium=true on day zero — onboarding sets premiumTrialEndsAt=+14d, onboarding/page.tsx:311) with dailyLogs=[], correlate([],'sleepQuality','energy') returns n=0,r=null (analytics.ts:622-624), so the card shows title 'Not enough overlapping days yet' / body '0 of 8 days where both were logged.' (CorrelationExplorer.tsx:96-101). Meanwhile the page's own `nothing` flag (insights/page.tsx:192-199) is computed ONLY from keystone/works/review/identity/model/topStreaks/insights — it ignores the always-on cards — so it is true, and the EmptyState 'Patterns are forming … Check in for about a week and your first personalized read appears here' (632-649) renders simultaneously. Net day-zero view: an empty 'patterns forming, ~10 check-ins to go' state AND a live interactive correlation tool announcing '0 of 8 days', directly contradicting each other.
- Why: Every other analytics surface honors the app's stated contract ('Patterns surfaced from your own data — only when there's enough signal', insights/page.tsx:230-233) by hiding until there's data. The CorrelationExplorer breaks that contract for trial users: it presents a working analytics instrument and a 'not enough data' verdict before the user has logged anything, which both undercuts the calm empty state and makes the premium feature's first impression a dead '0 of 8' readout.
- Fix: Gate the card on minimal real signal the same way its siblings are gated. Simplest: in insights/page.tsx wrap it like the others, e.g. `{loggedDays >= 3 && <CorrelationExplorer .../>}` (loggedDays already computed at 159-171), OR add an internal cold-start branch in CorrelationExplorer that, when logs.filter(hasAnyActivity).length is ~0, renders a one-line calm teaser ('Compare any two of your signals once you've logged a few days') instead of the live dropdowns+verdict. Keep it visible for premium discovery, just not as a working '0 of 8' tool on an empty account.
- Verify: CONFIRMED open. Every cited line checks out. (1) New trial user is premium day-zero: onboarding/page.tsx:311-313 sets premiumTrialEndsAt=+14d; entitlements.ts:70,82 → now<end → inTrial → premium=true. (2) CorrelationExplorer renders with NO data gate: insights/page.tsx:473-476 has no loggedDays/length guard (cf. ConsistencyCalendar gated at loggedDays>=3, line 386). Inside CorrelationExplorer.tsx, after the premium check (line 56) it always renders the two Selects + verdict box (138-169); no logs.length guard. (3) correlate([],'sleepQuality','energy'): loop yields n=0, n<MIN_PAIRS(8) → {n:0,r:null} (analytics.ts:622-624); component shows 'Not enough overlapping days yet' / '0 of 8 days where both were logged' (CorrelationExplorer.tsx:95-101); the '8' matches MIN_PAIRS=8 (analytics.ts:606). (4) The page's `nothing` flag (insights/page.tsx:192-199) is computed only from insights/ks/works/review/identity/model/topStreaks — it does NOT reference CorrelationExplorer, so on day-zero nothing=true and the EmptyState 'Check in for about a week...' renders (line 647, loggedDays===0 branch). (5) CorrelationExplorer (line 473) lives inside the always-rendered fragment opened at line 257, which is ABOVE/OUTSIDE the `nothing ? EmptyState : ...` ternary (line 632) — so both render at once. Net: a brand-new trial user (the default per the reverse-trial model) sees an empty 'patterns forming, ~10 check-ins to go' state AND a live correlation tool reporting '0 of 8 days' simultaneously. Not on the 'already fixed this session' list (which covers /upgrade copy gating + insights causation hedging, not the CorrelationExplorer render gate). Medium is right: real contradiction, affects every newly-onboarded (trial=premium) user, but it's UX/first-impression confusion, not a functional/data bug.

## LOW (23)
### [LOW] (inconsistency) "Tomorrow's first focus" card is sourced from TODAY's compiled timeline, so a morning behavior scheduled today-but-not-tomorrow would be mislabeled as tomorrow's kickoff
- Persona: new-user-midday
- Area: src/app/today/page.tsx tomorrowFirstFocus (first-day soft entry card)
- Evidence: today/page.tsx:819-829 — `tomorrowFirstFocus` is derived from `timeline` (the SELECTED-day compile = today): `timeline.filter(it => !it.muted && it.block === "morning")` then sorts by leverage. The card (1739-1799) is labeled "Tomorrow's first focus" with a MORNING badge and copy "Your system kicks off here in the morning." But it never compiles TOMORROW's timeline (contrast WeekAhead.tsx:31-43, which compiles `compileTimeline(state, dayIndexOfTomorrow)` per upcoming weekday). For the standard persona this is harmless — all better-sleep morning atoms (MORNING_SUNLIGHT etc., packs.ts:16-36) are daily with no `daysActive` restriction, so today's morning focus == tomorrow's. The defect only bites a starter/added pack whose top-leverage morning behavior has a `daysActive` mask that includes today but excludes tomorrow.
- Why: It's a latent correctness gap in a card whose entire promise is about TOMORROW. It doesn't fire for the default 2pm sleep-focused persona, but it's a real mislabel waiting on any day-specific morning behavior, and it's cheap to make correct.
- Fix: Compile tomorrow's timeline for this card: `compileTimeline(state, dayIndexOfKeyInTz(tz, addDaysToKey(dateKeyInTz(tz), 1)))`, filter `!muted && block==="morning" && isActionable`, then pick max leverage — instead of reusing today's `timeline`. Keeps the label honest for day-specific schedules.
- Verify: CONFIRMED OPEN. today/page.tsx:819-829: tomorrowFirstFocus filters `timeline` (block==="morning", sort by leverage desc) and never compiles tomorrow. today/page.tsx:581-582 + 555-557: timeline = compileTimeline(state, selDayIdx) where selDayIdx = dayIndexOfKeyInTz(tz, selectedDate); firstDaySoft requires isToday (line 800), so timeline IS today's compile. Card at today/page.tsx:1739-1799 is labeled "Tomorrow's first focus" + MORNING badge (1751) + "Your system kicks off here in the morning" (1792). WeekAhead.tsx:31-43 proves the correct per-day pattern (compileTimeline(state, di) per weekday) exists and is used elsewhere — Today does not use it. The defect concretely bites: packs.ts:979-993 defines bp-check ("Check blood pressure"), block:"morning", leverage 2, daysActive:[true,false,false,false,false,false,false] (Monday-only). A block scan of all daysActive entries in packs.ts confirms bp-check is the ONLY morning-block behavior with a day mask (others at lines 465/810/935/954/976/1197/1342/1357/2406/2422/2470/2533/2548/2563 are afternoon/evening/anytime). So a user joining mid-day Monday with that pack installed would see "Check blood pressure" mislabeled as tomorrow's (Tuesday's) kickoff, when it won't recur Tuesday. Not in fixed/deferred lists. Low severity: narrow conjunction (first-day soft entry + that pack + Monday + bp-check top morning leverage); the default 2pm sleep persona never triggers it since its morning atoms (MORNING_SUNLIGHT packs.ts:15-35 etc.) carry no daysActive.
### [LOW] (inconsistency) The 'adaptive suggestions / keystone / weekly review' sold as the Premium intelligence layer render on Today for ALL tiers — the trial isn't actually gating those surfaces
- Persona: trial-active-day3
- Area: src/app/today/page.tsx (suggestions, Focus chip, weekly/identity pointers)
- Evidence: /upgrade markets "Keystone, weekly review, correlations & adaptive suggestions" as Premium (upgrade/page.tsx:16-17). On Today these run with no access.premium guard: activeSuggestions = suggestions(state) (today/page.tsx:892-895) renders the "Suggestion" card (line 1957); the Operating-summary Focus chip uses keystone() (ksItem, line 1368); the "Your week" and "Who you're becoming" pointer cards (showWeekly/showIdentity, lines 1682-1729) surface weeklyReview()/identityReflection() — none gated by tier. Only the Insights page (via intelState peek delay) and biomarker-aware adaptation (engine.ts:953) are actually premium-gated.
- Why: For the trial persona the countdown implies these Today surfaces are part of what they'd lose at expiry, but a free/expired user sees the identical Today suggestion + Focus chip + weekly/identity pointers. The differentiation the trial sells doesn't exist on the Today surface, so the 'full intelligence left' framing overstates what is tier-exclusive. (Doesn't misbehave on day 3 — all these self-gate on data volume — but the premium-exclusivity claim is inconsistent with the code.)
- Fix: Decide the intended boundary and make copy match: either (a) keep these Today surfaces free (they're lightweight nudges) and narrow the /upgrade 'adaptive suggestions / weekly review' claim to mean the full Insights versions + real-time delivery, or (b) gate the Today suggestion card / weekly+identity pointers behind access.premium so the trial genuinely unlocks them. Today's option (a) is the smaller, more honest change.
- Verify: Confirmed against live code. The intelligence surfaces /upgrade sells as Premium (upgrade/page.tsx:16-17 VALUE[0]: "Keystone, weekly review, correlations & adaptive suggestions — built from your own data", with trial copy "X days of full intelligence left" at line 104) render on Today with NO tier guard for any user. Verified: (1) suggestion card today/page.tsx:1957 gated only by `isToday && activeSuggestions.length>0`; activeSuggestions=suggestions(state) at 892-895. (2) Focus/keystone chip at 1368 uses ksItem (734-738 ← keystone(state) at 580), no tier check. (3) Weekly/identity pointer cards at 1682 (showIdentity) and 1710 (showWeekly), defined 677-679 from reflectionsOk (675 = `isToday && !showTrialExtension && freshlyMasteredToShow.length===0`) — no premium check. Decisive: grep for `access.premium|access.paid|access.inTrial|!access|PremiumGate` across today/page.tsx returns exactly ONE hit, line 1493, which is the trial countdown status line itself, not a content gate. The intel engine is not tier-aware either — src/lib/intel.ts has zero premium/access/getAccess references (its only "tier" usage is evidence trust-tiers). So free/trial/expired users see identical suggestion + Focus chip + weekly/identity pointers; only the Insights page (peek delay) and biomarker-aware adaptation (engine.ts) are truly premium-gated. Real, open inconsistency between the trial's "full intelligence left" framing and what is actually tier-exclusive on Today. Not in the ALREADY-FIXED list (that gated /upgrade copy on the viewer's access.premium and added a trial row, not the gating of these Today content surfaces) and not in KNOWN-DEFERRED. Severity low: marketing/honesty inconsistency, not a functional defect — cards self-gate on data volume and are pointers into the actually-gated Insights page, so nothing misbehaves on day 3.
### [LOW] (ux-confusion) Inert-Stripe path: /upgrade shows a fully interactive 3-tier price selector that leads nowhere — selecting Annual/Monthly/Lifetime does nothing, the only real action is "Continue on the free plan"
- Persona: trial-expired-free
- Area: src/app/upgrade/page.tsx
- Evidence: upgrade/page.tsx:145-177 renders the three price cards (annual $79.99 / monthly $8.99 / lifetime $179) with onClick={() => setPlan(p)} and a selected-state ring — UNCONDITIONALLY, not gated on billingConfigured. Then upgrade/page.tsx:179-233: when !billingConfigured, the only controls are a static notice (":trialExpired ? 'Your trial has ended — paid plans are coming soon'") plus a "Continue on the free plan" button. So a trial-expired user can tap between three priced plans, watch the selection highlight move, and find no way to actually buy — the selector is decorative on the live (inert-Stripe) deployment.
- Why: Presenting a working-looking plan picker with real prices and then no purchase path reads as a broken/unfinished checkout to a user who is being asked to pay. It also wastes the one screen where intent is highest.
- Fix: When !billingConfigured, either hide the price selector entirely (keep just the value props + "coming soon" + "Continue on the free plan"), or render the three tiers as a non-interactive "here's what Premium will cost" preview (no selected-state affordance, no onClick) so it doesn't imply a choice that does nothing.
- Verify: Confirmed against live code at /Users/rami/Claude/healthkit-website/src/app/upgrade/page.tsx. The 3-tier price selector (lines 145-177) renders UNCONDITIONALLY — outside the `billingConfigured` ternary, which only begins at line 180. Each card has onClick={() => setPlan(p)} (line 151) and a selected-state ring (lines 154-160). The `plan` value is consumed only by startCheckout(plan) at line 184, which is inside the `billingConfigured ?` true-branch. Since Stripe is inert on the live deploy (billing.ts:18-20: billingConfigured is false with no STRIPE_CHECKOUT_* env; CLAUDE.md status confirms "Stripe: Payment Links env-gated, inert"), execution falls to the else branch. For a trial-expired free user (!access.premium), lines 219-229 render exactly the static notice "Your trial has ended — paid plans are coming soon" (line 221, matching the quoted evidence) plus a single "Continue on the free plan" button (line 228). So tapping Annual/Monthly/Lifetime moves the highlight via setPlan but leads to no purchase path — the selector is decorative. This is NOT covered by the session fix "/upgrade copy gated on access.premium," which only reworked the action-button block (lines 179-233); the price-card block (145-177) was left untouched and unconditional. Not in KNOWN-DEFERRED. Severity is genuinely low: the honest "coming soon / Continue on the free plan" copy sits directly below the selector, so the true state is communicated even though the interactive priced cards are misleading.
### [LOW] (ux-confusion) Profile membership CTA reads "Restore full intelligence" for a lapsed-trial user, but on the live inert-Stripe path /upgrade can't restore anything ("paid plans are coming soon")
- Persona: trial-expired-free
- Area: src/app/profile/page.tsx
- Evidence: profile/page.tsx:808-815: for the non-paid/non-trial branch the button text is `access.trialExpired ? "Restore full intelligence" : "Explore Premium"`, onClick router.push('/upgrade'). On the live deployment billingConfigured is false, so /upgrade for a trialExpired user shows "Your trial has ended — paid plans are coming soon" + "Continue on the free plan" (upgrade/page.tsx:217-231) — there is no action that restores intelligence. The destination copy is honest; the button label promises a restore that cannot occur.
- Why: "Restore full intelligence" sets an expectation (one tap brings Premium back) that the inert-billing reality can't meet, so the user taps through to a dead-end "coming soon" and the membership card feels broken.
- Fix: Make the label honest about availability, e.g. gate on billingConfigured: when billing is off show "See what Premium adds" (or "Premium — coming soon") instead of "Restore full intelligence"; reserve the restore wording for when a real checkout exists.
- Verify: Confirmed open. profile/page.tsx:808-814 — in the non-paid/non-trial else branch the membership CTA label is `access.trialExpired ? "Restore full intelligence" : "Explore Premium"`, onClick router.push('/upgrade') (line 809). entitlements.ts:76-86 sets trialExpired=true exactly for a lapsed-trial, non-paid user, so that user does see "Restore full intelligence". On /upgrade: getAccess returns paid=false and premium=false for a lapsed trial, so the access.paid early return (upgrade/page.tsx:57) is skipped; billing.ts makes billingConfigured=false on the live deploy (no Stripe env), so upgrade/page.tsx:180 takes the else; access.premium is false so lines 217-231 render — "Your trial has ended — paid plans are coming soon" + only a "Continue on the free plan" button (lines 220-221, 228). There is no restore/checkout action reachable, so the label promises a one-tap restore the destination cannot deliver. NOT among this session's fixes (the gated /upgrade copy fix was the access.premium split at upgrade/page.tsx:205, not the profile button label) and not in KNOWN-DEFERRED. Severity low: it is honest-dead-end copy mismatch only — user still lands on a truthful screen and can stay free; no data loss, broken function, or safety impact. Fix: gate the profile label on billingConfigured (e.g. show "Explore Premium" / "See what Premium adds" when billing is inert) or have it read "See what's coming" for trialExpired when !billingConfigured.
### [LOW] (bug) Biomarker-aware ease can be silently masked when a recovery marker and a non-recovery marker are both in "Watch" the same day
- Persona: paid-premium
- Area: src/lib/engine.ts:746-772 (biomarkerConcern pick) → engine.ts:1075 (bioRecoveryFlag ease branch)
- Evidence: biomarkerConcern() collects all in-window Watch markers, then `candidates.sort((a,b) => b.date.localeCompare(a.date))` (engine.ts:767) and takes candidates[0]. The sort is by date only and is stable, so when the two most-recent Watch readings share a date (e.g. a premium user logs HRV=30 [recovery, Watch] AND waist=110 [non-recovery, Watch] on the same day), the winner is decided by Map insertion order (engine.ts:746-748), i.e. their order in state.biomarkers — not by recovery relevance. If the non-recovery marker is ordered first, `pick.metric = waist` → `recovery: BIO_RECOVERY.has("waist") === false` (engine.ts:771). The ease branch `if (s.bioRecoveryFlag && s.bioConcern)` (engine.ts:1075) then does NOT fire, so the day is NOT eased despite a recovery-relevant HRV sitting in the Watch band.
- Why: Biomarker-aware adaptation is the core paid feature. A genuine low-HRV recovery signal gets suppressed by an unrelated co-logged marker, so the day fails to ease exactly when the premium engine should have caught it. Narrow trigger, but it's the adaptation engine making the wrong call, not cosmetics.
- Fix: When picking the candidate, prioritize recovery markers: sort BIO_RECOVERY members ahead of others among same-date (or all) Watch candidates, OR compute `recovery` as `candidates.some(c => BIO_RECOVERY.has(c.metric))` so any in-window recovery Watch flags recovery regardless of which marker supplies the display text.
- Verify: CONFIRMED open. engine.ts:768-771 — biomarkerConcern() returns a SINGLE {text, recovery} derived from pick=candidates[0], with recovery: BIO_RECOVERY.has(pick.metric) — i.e. the recovery flag reflects only the one winning marker, not "is ANY in-window Watch marker recovery-relevant." The candidates array is built in Map iteration order (engine.ts:758), and the Map's insertion order (engine.ts:747-748, sorted by date ASC, stable) equals each metric's order in state.biomarkers. The final sort at engine.ts:767 `candidates.sort((a,b)=>b.date.localeCompare(a.date))` is date-only and stable, so two same-date Watch readings keep their array (insertion) order. Concrete trace with a premium user logging waist=110 then hrv=30 on the same day: waist (direction "lower", optimal 90, watch 102 per biomarkers.ts:54-60) → 110 is "Watch"; hrv (direction "higher", watch 40 per biomarkers.ts:89-95) → 30 is "Watch". Both candidates share the date; stable sort leaves [waist, hrv]; pick=waist → recovery=false. At engine.ts:1075 `if (s.bioRecoveryFlag && s.bioConcern)` the ease branch then does NOT fire even though an HRV-in-Watch recovery signal exists. storage.ts:1340 (`[...deduped, e]`) confirms entries are appended in log order with no recovery-aware sort, so waist-first is reachable. Not in the already-fixed list (task #141 only dropped hsCRP; the engine.ts:749-751 comment fixed oldest→most-recent for which TEXT shows, not the recovery-flag tie). Severity low: narrow trigger (premium + two Watch markers, one recovery one not, sharing the latest date, non-recovery logged first) and recoveryProxy/bioConcern text still surface independently — but the paid biomarker-driven ease is genuinely suppressed as claimed.
### [LOW] (ux-confusion) "Nothing needs easing today" banner contradicts itself by listing a flagged biomarker as the day's reason
- Persona: paid-premium
- Area: src/lib/engine.ts:1099-1109 (normal-mode adapt) → src/app/today/page.tsx:1315-1318,1434-1446 (banner tone + reason bullet)
- Evidence: In normal mode, baselineAdapt returns headline "Today", tone "Nothing needs easing today — your day holds its full shape…" and `reasons: s.bioConcern ? [s.bioConcern] : []` (engine.ts:1107-1108). Today renders the tone (page.tsx:1318) and then, unconditionally on reasons.length>0, the first reason as the inline "why behind the read" bullet (page.tsx:1434-1446). For a premium user with a non-recovery marker in Watch (waist/bodyFat/systolic/diastolic/vo2max/gripStrength — none are in BIO_RECOVERY, engine.ts:734), the card reads: tone "Nothing needs easing today" immediately above bullet "Recent waist (110 cm) is outside its optimal range." The same concern is also surfaced inconsistently across modes — it's dropped in the "primed" branch (engine.ts:1083-1089 has no bioConcern in reasons) but shown in normal.
- Why: This persona scrutinizes the 'why' (the Attia/Huberman audience the product targets). A 'why this is your day's shape' bullet that states an unrelated long-term body concern under a 'nothing needs easing' headline reads as a non-sequitur / mild self-contradiction, and the show-in-normal / hide-in-primed asymmetry makes it feel arbitrary.
- Fix: Either omit bioConcern from `reasons` in the normal branch (engine.ts:1108) so an un-actioned long-term marker doesn't masquerade as the day's operating reason, or give it its own labeled, non-'reason' affordance (e.g. a calm 'A marker to keep an eye on' note distinct from the adaptation rationale), and surface it consistently across modes.
- Verify: Confirmed real and open. engine.ts:734 BIO_RECOVERY={"hrv","restingHR"} only. biomarkerConcern (engine.ts:736-773) skips only direction==="range" (line 760) and returns text like "Recent waist (110 cm) is outside its optimal range" (770) with recovery=BIO_RECOVERY.has(metric) (771). In biomarkers.ts, waist/bodyFat/systolic/diastolic are direction "lower" (58/70/104/115) and vo2max/gripStrength "higher" (126/137) — none are range, none are recovery markers, so each can set bioConcern with bioRecoveryFlag=false. baselineAdapt (engine.ts:1000) therefore skips the recovery-flag branch (1075) and, when recovery is fine and no other signal fires, returns the normal-mode object at 1099-1109: tone "Nothing needs easing today — your day holds its full shape" (1107) AND reasons:[bioConcern] (1108). today/page.tsx renders adaptation.tone (1318) then unconditionally on reasons.length>0 (1434) renders adaptation.reasons[0] as the inline 'why' bullet (1445) — so "Nothing needs easing today" sits directly above "Recent waist (110 cm) is outside its optimal range", a genuine tonal non-sequitur. Show/hide asymmetry also confirmed: primed branch reasons (1088) omits bioConcern while recovery (1032) and normal (1108) include it. Premium-gated, requires a Watch-band non-recovery marker. Not in the already-fixed list (MODE_DEFAULT_COPY at 1118 is a separate CMS-mode/copy issue). Low severity is correct — copy/tone, not functional.
### [LOW] (ux-confusion) Insights "Your next habit" recommends a specific behavior but sends you to a pack-only catalog where it can't be added
- Persona: paid-premium
- Area: src/app/insights/page.tsx:424-459 (card) ↔ src/lib/intel.ts:1067-1111 (nextBestAddition) ↔ src/app/library/page.tsx (redirect to /protocols#discover)
- Evidence: nextBestAddition() returns a single behavior ATOM (key/title/rationale, plus `fromPacks` it does not surface) — intel.ts:1101-1110. The Insights card shows `rec.title` (a behavior name, e.g. a specific lever) and the CTA "Add it from the Library", pushing to /library (insights/page.tsx:426,454). /library immediately redirects to /protocols#discover (library/page.tsx:21), whose Discover view lists protocol PACKS, not individual behaviors (protocols/page.tsx:65 viewMode 'discover' + discoverOpen is a ProtocolPack). The named behavior is not directly addable there, and the card never names or links the pack that contains it even though nextBestAddition already computed `fromPacks` (intel.ts:1109).
- Why: For a premium user who's installed several packs, this is the growth surface telling them to add a precise behavior, then dropping them on a pack catalog where that exact item doesn't appear as an addable row — a dead-feeling handoff that undercuts the 'intelligent recommendation' premise.
- Fix: Use the `fromPacks` already returned: have the card name/deep-link the pack that contains the behavior ("Add it via the {packName} protocol") and open that pack's Discover sheet, or route to the custom-behavior atom picker pre-seeded with the recommended atom (protocols.tsx already supports linking an atom by derivedFrom) instead of a bare /library push.
- Verify: Verified the full chain in live code. nextBestAddition() returns a single behavior atom — title=top.title (a behavior name) and computes fromPacks=top.fromOfficialPacks but the card never surfaces it (intel.ts:1101-1110). The Insights card renders rec.title with CTA "Add it from the Library" and navigates only via router.push("/library") (insights/page.tsx:446,454,426); grep confirms rec.fromPacks is never read in that file. /library redirects to /protocols#discover (library/page.tsx:21-23). In Protocols, viewMode "discover" and discoverOpen are ProtocolPack (protocols/page.tsx:65-66); the discover sheet renders pack behaviors as read-only display rows (no per-behavior add control) with a single whole-pack Install/Remove button (protocols/page.tsx:896-940, 942-998). There is no addable row for an individual behavior, no behavior-key deep-link, and no scroll/highlight — confirming the recommended behavior is not directly addable and the containing pack is never named or linked. addBehaviorToDraft (protocols/page.tsx:349) is for the custom-pack builder, not the catalog. Not in the already-fixed intel/insights list (those were overclaim-copy/consistency/correlation/goal-unit changes). Genuine low-severity UX dead-end handoff.
### [LOW] (ux-confusion) BehaviorSheet on a 'Resting today' (conflict-muted) row still shows the reminder toggle ON, implying a notification will fire for a behavior that can't be done
- Persona: power-user-conflicts
- Area: src/components/BehaviorSheet.tsx:492-523 (Reminder toggle); src/components/Reminders.tsx:94,181
- Evidence: Opening the sheet on conflict-muted strength correctly shows the 'Currently resting' explanation (BehaviorSheet.tsx:645-652 → humanizeMuteReason). But the Reminder control still renders default-ON with copy "Notify me at this behavior's time." (BehaviorSheet.tsx:497-499) and a lit switch (line 511, `!ov.reminderOff`). In reality Reminders.tsx skips muted items (`if (it.muted) continue;` at 94 and 181), so no reminder ever fires — the toggle is a benign dead control while the behavior is conflict-muted, contradicting the 'Resting today' framing right above it.
- Why: Minor, but for a detail-oriented power user it's an inconsistency: the row says 'resting / on pause today', yet its reminder appears active. It's the same 'control that does nothing in this state' pattern the team has been pruning (e.g. snooze disabled for hard-window items).
- Fix: When item.muteReason starts with 'conflict pair:' (or item.muted from a conflict), disable the reminder switch and swap the copy to something like "Paused by your {pack} protocol — no reminder while it's resting." Mirrors the existing hard-window snooze-disabled treatment (BehaviorSheet.tsx:383,397-404).
- Verify: Confirmed open. BehaviorSheet.tsx:492-523 renders the Reminder toggle unconditionally with no check on item.muted/muteReason; the switch is lit whenever !ov.reminderOff (lines 504, 511, 517) and copy reads "Notify me at this behavior's time." (497-499). For a conflict-muted row the user hasn't explicitly silenced, ov.reminderOff is undefined, so it shows ON. The same sheet renders the "Currently resting:" explanation via AboutThisBehavior (BehaviorSheet.tsx:645-652 → humanizeMuteReason, which for a "conflict pair" reason produces "...on pause today", lines 683-696). Reminders.tsx truly skips muted items in both paths — server push at line 94 (`if (it.muted) continue;`) and in-tab at line 181 (`if (it.muted || isDone(...)) continue;`) — so no notification ever fires; the toggle is a benign dead control that contradicts the "resting/on pause today" framing above it. Not a regression: the session's reminder fixes (hard-window clamp/quiet-hours/honest copy) never gated the per-behavior toggle on muted state. Severity correctly LOW: cosmetic only, no spurious notification and no functional harm; it's the same "control that does nothing in this state" pattern already pruned elsewhere in this very file (snooze "Later today" disabled with a note for hard-window items at lines 383-404; forbidden block pills disabled at 198/230-236; stacked-item timing controls hidden with a note at 246-251).
### [LOW] (inconsistency) Recovery chip can read "Good" while the banner says "Lighter day / Last night was rough" (poor sleep + high energy)
- Persona: recovery-mode
- Area: src/app/today/page.tsx signal-chip thresholds vs baselineAdapt sleep override
- Evidence: The Recovery chip is bucketed purely on recoveryProxy: >=78 High, >=60 Good, >=45 Moderate, else Easing (today/page.tsx:1329-1338). But baselineAdapt has a sleep-quality override BELOW the proxy check: sleepQuality<=2 forces "lighter" mode regardless of proxy (engine.ts:1048-1054). Verified: "Poor sleep (2) + High energy (5)" → proxy=64 → chip "Recovery: Good" but mode=lighter → banner "Lighter day / Last night was rough." The chip and headline disagree on the same screen. (The intended High↔Primed and Easing↔Recovery alignments at 78/45 are correct; this gap is the 60–77 band colliding with the poor-sleep override.)
- Why: Minor but it's exactly the "chip cutoff vs primed/mode" coherence the persona brief calls out. A user sees a reassuring "Good" recovery chip directly above a banner telling them last night was rough and the day is lighter.
- Fix: Make the chip reflect the chosen mode, not just the raw proxy: when mode is lighter/recovery due to the sleep override, cap the Recovery chip label at "Moderate" (or surface the Sleep chip as the lead signal). Alternatively factor the sleep-poor override into the chip bucket so a poor-sleep day can't read "Good."
- Verify: Confirmed real and open. Recovery chip buckets purely on recoveryProxy (today/page.tsx:1332-1338): >=78 High, >=60 Good, >=45 Moderate, else Easing. The banner renders adaptation.headline (today/page.tsx:1313, from adapt(state) at :578). In baselineAdapt, the sleepQuality<=2 override (engine.ts:1048-1054) returns mode "lighter" / headline "Lighter day" / "Last night was rough" and sits BELOW the recovery-mode check (engine.ts:1023, proxy<45) but ABOVE the primed check (engine.ts:1083). Verified the proxy math at engine.ts:911-919: sleep=2 -> (2/5)*100=40 @w0.6; energy=5 -> 100 @w0.4; (24+40)/1.0 = 64. So poor sleep(2)+high energy(5) => proxy=64 => chip 'Good' (>=60), while sleepQuality=2 forces 'Lighter day / Last night was rough' on the same screen. Recovery-mode (64<45 false) and essentials (adherence-gated, independent) do not pre-empt. Not in the already-fixed list (MODE_DEFAULT_COPY/today-only-recovery fixes don't touch this 60-77 band vs sleep-override collision) nor known-deferred. Severity genuinely low: both copies are calm/non-alarming, requires the narrow high-energy-despite-poor-sleep combo, nothing breaks functionally.
### [LOW] (ux-confusion) TimezoneSentry "Not now" is remembered permanently per device-timezone string — a repeat traveler never sees the prompt again for a city they once dismissed
- Persona: traveler-tz
- Area: src/components/TimezoneSentry.tsx (DISMISS_KEY logic)
- Evidence: Dismiss stamps `localStorage[pz-tz-dismissed] = device` (TimezoneSentry.tsx:84-86) and the effect early-returns whenever `dismissed === dev` (TimezoneSentry.tsx:45-49). The stamp is never cleared on returning home (the effect returns earlier at line 43 when `dev === stored`, so it never overwrites the stale dismiss). So a traveler who taps 'Not now' in Asia/Tokyo on one trip — keeping home tz — will, on every FUTURE trip back to Asia/Tokyo (including a genuine relocation), have the prompt silently suppressed because the stored dismiss string still equals the device tz. Only Update (line 97 removeItem) clears it.
- Why: For the persona who repeatedly visits the same cities, the 'your timezone changed' prompt — the only path to re-anchor protocols/reminders to a new locale — quietly stops appearing for previously-dismissed zones, so a later real relocation to that zone is missed.
- Fix: Scope the dismissal so it doesn't outlive the trip: e.g. store the dismissed device tz together with the stored (home) tz it was dismissed against, and treat the dismiss as valid only while `stored` is unchanged; or clear DISMISS_KEY whenever `deviceTz() === stored` (back home). That re-arms the prompt for the next genuine move.
- Verify: Confirmed against live code in /Users/rami/Claude/healthkit-website/src/components/TimezoneSentry.tsx. Line 43 `if (dev === stored) return;` early-returns on returning home, so the dismiss stamp is never overwritten/cleared there. Line 49 `if (dismissed === dev) return;` suppresses the banner whenever device tz equals the previously-dismissed string. Line 85 "Not now" sets `localStorage[pz-tz-dismissed] = device`; line 97 (Update) is the ONLY place that clears it — grep confirms the key `pz-tz-dismissed` has exactly 3 refs (read 47, set 85, remove 97), all in this file. So a repeat traveler who taps "Not now" in a city, returns home, then revisits that same city (incl. a genuine relocation) has the prompt permanently suppressed. The stamp is per-tz-string with no expiry. Severity is low: the bug only triggers at the narrow intersection of (a) a previously-dismissed zone and (b) a later real relocation to that exact zone; everyday drift and first-visit cases work. It is also the ONLY re-anchor path — `updateSettings({ timezone })` appears solely at TimezoneSentry.tsx:95; onboarding has zero timezone references and Profile only reads via getTz (profile/page.tsx:10,416; line 1060 is reset-carry, not an editor) — so the "only path to re-anchor" framing is accurate. The component's own doc (lines 22-25) says it should re-prompt "until the device tz changes again," which the implementation does not honor. Not in the already-fixed-this-session list and not in known-deferred.
### [LOW] (overclaim) 'Day complete' celebration tells a supplement-only user 'Every behavior, done.' when they have zero behaviors
- Persona: supplement-only
- Area: src/app/today/page.tsx (dayComplete celebration copy, lines 1230-1237)  ⚠️ REGRESSION (recent commit)
- Evidence: The day-complete card renders the headline 'You've closed the day' (today/page.tsx:1231) and the body 'Every behavior, done. This is the quiet, compounding work that actually changes a healthspan. Rest well.' (lines 1234-1237). This card now fires for supplement-only days: dayComplete (lines 781-786) is true when `prog.total > 0 ? prog.done===prog.total : suppProgToday.total > 0` AND allSuppHandledToday — i.e. a behavior-less day completes purely on supplements (the suppProgToday path was added this session, commit 1d3b69b). For a supplement-only user prog.total===0, so 'Every behavior, done' is literally false — they completed a supplement stack, not behaviors.
- Why: The same fix that correctly let a supplement-only day reach completion routes it into behavior-worded celebration copy, producing a 'works-but-makes-no-sense' moment: the headline congratulates the user for finishing 'every behavior' they never had. It reads as boilerplate the app forgot to adapt for the no-behavior case.
- Fix: Branch the body copy on whether any behaviors were in play. e.g. `const onlySupps = prog.total === 0 && suppProgToday.total > 0;` then render `onlySupps ? 'Your full stack, taken. This is the quiet, compounding work that actually changes a healthspan. Rest well.' : 'Every behavior, done. …'`. Keeps the calm tone, drops the false 'behavior' claim.
- Verify: Confirmed open. today/page.tsx:785 `dayComplete` uses `(prog.total > 0 ? prog.done === prog.total : suppProgToday.total > 0) && allSuppHandledToday`; `prog` is behaviors-only (line 711-712 = `timelineProgress(timeline, log)`), and the inline comment at lines 759-762 explicitly states the `suppProgToday` branch exists so a SUPPLEMENT-ONLY day (no behaviors, prog.total===0) reaches "Day complete". The celebration card body at lines 1234-1237 is the unconditional literal "Every behavior, done. This is the quiet, compounding work that actually changes a healthspan. Rest well." — the only dynamic element is the name interpolation at line 1232. There is NO branch adapting the copy for prog.total===0, so a supplement-only user is told "Every behavior, done" when they had zero behaviors. The ALREADY-FIXED note only covers the completion gate ("supplement-only day completes"), not the celebration copy; the copy mismatch is a real, unaddressed side-effect. Severity is low rather than medium: it triggers only for the narrow supplement-only cohort and is purely cosmetic copy incoherence (no broken state, no data issue), consistent with the project's other LOW cosmetic-copy notes.
### [LOW] (ux-confusion) Today empty-state tells a weekend-only user 'blank canvas — install a protocol' on weekdays, when they already have a system
- Persona: weekend-only-custom
- Area: src/app/today/page.tsx empty-state branch (939-1007)
- Evidence: The empty state fires on `timeline.length === 0 && !hasSupplementsToday` (today/page.tsx:939) and only distinguishes vacationMode vs not. For not-on-vacation it renders 'Your day is a blank canvas' + 'Install a protocol and Protocolize will assemble an adaptive daily system for you.' + a 'Discover protocols' CTA (today/page.tsx:986,996,998-1003). There is NO check for behaviors scheduled on OTHER days. A persona whose only active pack is a custom Sat/Sun behavior (reachable: uninstallPack leaves customPacks intact, protocols/page.tsx:1494 + storage.ts upsertCustomPack) hits this every Mon–Fri even though they have a real, intentionally weekend-only system.
- Why: It misrepresents the user's state — they have a protocol, it's just not scheduled today — and pushes them to 'install a protocol' they don't need, which is confusing and slightly insulting for a deliberate weekend-only setup.
- Fix: Before the generic 'blank canvas' copy, check whether any behavior is scheduled on a different day this week (e.g. compileTimeline(state, d) for d != selDayIdx, like manageRows/analyticsItems already do). If so, show a 'Nothing scheduled today — your next behavior is <Sat>' rest-day style message (mirroring the existing restDays copy at today/page.tsx:1160) instead of the install-a-protocol empty state.
- Verify: Confirmed open. today/page.tsx:939 fires the empty state on `timeline.length===0 && !hasSupplementsToday` and only branches on `state.settings.vacationMode` (line 942); the not-on-vacation copy is "Your day is a blank canvas" + "Install a protocol..." + "Discover protocols" CTA (lines 986/996/998-1003). There is NO awareness of behaviors scheduled on other days — grep over the whole file shows no daysActive/other-day logic near the empty state. `compileTimeline` day-filters via `daysActive[dayIndex]` (engine.ts:397), so a behavior active only Sat/Sun yields timeline.length===0 Mon-Fri. The persona IS reachable: BehaviorSheet.tsx:477 lets a user set daysActive (weekend-only), and a custom weekend-only pack as the sole installed pack produces an empty weekday timeline -> blank-canvas state, misrepresenting a deliberate setup. CAVEAT: the finding's cited reachability mechanism is WRONG — protocols/page.tsx:1494 calls uninstallPack ONLY for official packs (custom packs hit deleteCustomPack at line 1491, which strips both customPacks AND installedPacks, storage.ts:1228-1233), and engine.ts:209 skips any pack not in installedPacks, so an "uninstalled-but-kept" customPack renders nothing. The issue is real via the simpler never-uninstalled path, not via the path described. Not in the fixed (a9f2115..7fd3724) or deferred lists. Low severity: narrow edge persona, copy-only misrepresentation, no functional/data harm.
### [LOW] (ux-confusion) No way to set a custom behavior's active days at creation — weekend-only requires a hidden second step
- Persona: weekend-only-custom
- Area: src/app/protocols/page.tsx custom builder addBehaviorToDraft (349-397) + pickAtomToDraft (406-430); create form UI (1187-1325)
- Evidence: The custom-behavior create form exposes title, block, time, rationale, timingReason only (protocols/page.tsx:1188-1313) and addBehaviorToDraft builds a BehaviorDef with NO daysActive field (349-388), so every newly created custom behavior is all-7-days by default. To make it Sat/Sun only the persona must save the behavior, find its row, open the BehaviorSheet, and use the 'Active days' toggle (BehaviorSheet.tsx:465-490) which writes a separate behaviorOverride. There is no day selector at the point of creation and no hint that one exists.
- Why: The persona's defining action — 'active Sat/Sun only' — isn't expressible in the builder where they're literally defining the behavior; it's only reachable via a post-hoc editor sheet, so the obvious path silently creates an every-day behavior. Discoverability gap for the persona's core scenario.
- Fix: Add an 'Active days' M T W T F S S toggle row to the custom builder form (reuse the exact control from BehaviorSheet.tsx:466-490) and pass the resulting daysActive into the BehaviorDef in addBehaviorToDraft/pickAtomToDraft. Then weekend-only is a one-step creation, consistent with how the BehaviorSheet already presents it.
- Verify: Verified against live code. The custom-behavior create form (protocols/page.tsx:1187-1325) exposes only title (1188), block segmented control (1248-1268), optional "Time of day" (1274-1289), rationale (1290-1297) and timingReason (1303-1313) — no day selector. The bDraft state shape (107-114) has only {title, block, time, dose, rationale, timingReason}; no daysActive. addBehaviorToDraft (349-388) constructs a BehaviorDef with NO daysActive field, so a new custom behavior is all-7-days by default. The only day selector is BehaviorSheet.tsx "Active days" (465-490), which calls patch({daysActive: next}) writing a behaviorOverride — reachable only after the behavior is saved and its row's editor sheet is opened. So "active Sat/Sun only" requires a hidden second step at creation time, exactly as claimed.

This is NOT covered by the session's "all-days union (weekend behaviors reachable)" fix: that change is in the engine's weekday union (protocols/page.tsx:228 comment) so weekend-only behaviors aren't dropped from the merged timeline — a timeline-visibility concern, not the creation-form discoverability gap reported here.

Severity is genuinely LOW (correctly classified by reporter): the behavior IS fully expressible, just in two steps, and nothing is broken/unreachable. It is a discoverability gap — notably the library-pick path hints "You can still customize dose, days, and time after picking" (1105-1106) but the custom path gives no equivalent days hint. Intended-minimal-form is a defensible design, but the persona's core "Sat/Sun only" action has no in-builder affordance, so the report stands as a real open UX issue.
### [LOW] (ux-confusion) Toggling all 'Active days' off in the sheet makes a behavior vanish live with no 'paused' indication until reload
- Persona: weekend-only-custom
- Area: src/components/BehaviorSheet.tsx Active-days toggle (469-488); heal lives in storage.ts normalize (308-318), not in the live write
- Evidence: The day toggle flips next[i] with no floor (BehaviorSheet.tsx:474-477) and saves via setBehaviorOverride, which writes the override raw with no normalize (storage.ts:1203-1212; useAppState Local save just calls saveState, datasource.ts:45-46, no normalize on save). The all-false→disabled:true heal only runs inside normalize() at LOAD (storage.ts:301-318). So if the persona toggles Sat off then Sun off (or otherwise clears every day), the engine's day filter (engine.ts:397) drops the row from Today AND Protocols immediately, with no dimmed/paused row and no toast — it just disappears. It only re-appears (correctly, as a dimmed 'paused' row) after a reload triggers normalize.
- Why: A user fiddling with the weekend toggles can accidentally make their behavior silently disappear with no explanation or undo affordance in-session — looks like data loss. The healthy 'disabled' representation exists but isn't applied on the live edit path.
- Fix: Apply the all-false→{disabled:true, daysActive:undefined} normalization at write time too: either in setBehaviorOverride (storage.ts:1203) or in the BehaviorSheet onChange wrapper, so an all-off selection immediately becomes a visible paused row (matching the post-reload state). Optionally disable the last active day-button or show a 'this hides the behavior — pause instead?' hint.
- Verify: CONFIRMED OPEN. The live edit path applies no all-false heal: BehaviorSheet day toggle flips next[i] with no floor and saves raw (BehaviorSheet.tsx:476-477; patch=onChange at :105; only daysActive read is :97, no .every/.some guard). onChange wires straight to setBehaviorOverride (today/page.tsx:3167, protocols/page.tsx:1528) → useAppState.ts:355-356 → storage.ts setBehaviorOverride writes the override verbatim (storage.ts:1208-1211). The save path runs NO normalize: datasource LocalDataSource.save just calls saveState (datasource.ts:45-46), and saveState is a bare localStorage.setItem(JSON.stringify(state)) (storage.ts:573-586). The engine reads the in-memory state directly (overrides=state.behaviorOverrides, engine.ts:184) and takes the all-false array as-is (daysActive: ov?.daysActive ?? b.daysActive at :234; existing.daysActive=ov.daysActive at :345). Because disabled is NOT set on this path, engine.ts:215 does not treat it as paused, and the day filter at engine.ts:397 (!it.daysActive || it.daysActive[dayIndex]) drops the row from BOTH Today and Protocols immediately — no dimmed row, no toast. The all-false→{disabled:true, daysActive:undefined} heal lives ONLY inside normalize() (storage.ts:308-318), which runs at loadState (storage.ts:469), not on save. The paused/dimmed representation is only reachable via includeDisabled, which Protocols passes (protocols/page.tsx:235) but Today does not — and only after a reload triggers normalize. So the row silently vanishes mid-session and only reappears as a paused row after reload, exactly as described. Not in the session's ALREADY-FIXED list; the heal comment (storage.ts:302-306) itself calls the un-healed all-false state a UI bug, so this is not intended behavior. Severity low: requires deliberately toggling all 7 days off (edge case, not one-tap), fully recoverable with no real data loss (override self-heals on next load), consequence is transient UX confusion only.
### [LOW] (inconsistency) Create-from-scratch behaviors are permanently keyed under the literal namespace "draft", not the saved pack id
- Persona: custom-pack-author
- Area: src/app/protocols/page.tsx:351 & 410 (`packId = editingId ?? "draft"`) → customCanonicalKey src/lib/packs.ts:2694-2705; saved pack id page.tsx:323
- Evidence: While building a NEW protocol, editingId is null, so both addBehaviorToDraft (page.tsx:351) and pickAtomToDraft (page.tsx:410) generate canonicalKeys as `custom:draft:<base>-<rand>` (packs.ts:2704). saveCustom then persists the pack under id `custom-${Date.now()}` (page.tsx:323) WITHOUT rewriting those keys, so every from-scratch protocol's behaviors carry the packId segment "draft" instead of their real pack id (only the 4-char random suffix, packs.ts:2703, keeps them distinct). The namespace's stated purpose is per-pack isolation (packs.ts:2688-2692). It still functions because free-text customs merge by full canonicalKey (random-suffixed → unique) and library picks merge by derivedFrom, so nothing actually collides — but the key no longer means what the comment claims.
- Why: Not user-visible today and not a live collision (the random suffix saves it), but it's a latent footgun: the isolation guarantee now rests entirely on Math.random() rather than the pack id, and any future code that parses/keys on the packId segment (e.g. grouping a pack's behaviors by `custom:<packId>:`) would mis-bucket every from-scratch pack under "draft".
- Fix: Allocate the pack id once when the builder opens for creation (e.g. set editingId/newId at openCreate time) so behaviors are keyed under the real id; OR in saveCustom, when creating, rewrite each behavior's canonicalKey to swap the `:draft:` segment for the new pack id (and keep derivedFrom intact). Low effort, removes the reliance on the random suffix for isolation.
- Verify: CONFIRMED as a real but cosmetic/semantic inconsistency (correctly low). The mechanism is exactly as described and I verified every link in the chain against live code:

1. page.tsx:351 (addBehaviorToDraft) and page.tsx:410 (pickAtomToDraft) both set `const packId = editingId ?? "draft"`. While creating a NEW pack, editingId is null, so packId is the literal "draft".
2. packs.ts:2694-2705 customCanonicalKey returns `custom:${packId}:${base || "item"}-${suffix}` where suffix = Math.random().toString(36).slice(2,6) (packs.ts:2703) — so keys become `custom:draft:<base>-<rand>`.
3. page.tsx:322-333 saveCustom builds the pack with id `editingId ?? \`custom-${Date.now()}\`` (line 323) and persists draft.behaviors verbatim (line 331) via upsertCustomPack — it does NOT rewrite the canonicalKeys.
4. CRITICAL confirmation that nothing heals it later: storage.ts normalize (lines 232-281) maps every customPack behavior, and lines 236-241 explicitly say "Already in a reserved namespace? Trust it." — if the key startsWith("custom:") it returns it UNCHANGED. The packId-based re-namespacing (lines 257-279 using p.id) only fires for BARE keys, never for already-`custom:`-prefixed ones. So the "draft" segment is preserved permanently across reloads.

The finding's "not a live collision" claim is also TRUE: engine.ts:227 merges by `mergeKey = b.derivedFrom ?? b.canonicalKey`. Free-text customs merge by full canonicalKey (random suffix at packs.ts:2703 keeps them unique); library picks merge by derivedFrom (packId segment irrelevant). I grepped all consumers of the namespace (engine.ts:116, 1632; supplements.ts:184; governance.ts:182,442; storage.ts:237) — every one uses only startsWith("custom:")/startsWith("fork:"); NONE splits on ":" to extract or bucket by the packId segment. The observed shape `custom:draft:magnesium-before-bed-ab12` is even baked into supplements.test.ts:139 as the real from-scratch format.

So: the comment at packs.ts:2687-2692 promises per-pack isolation, but from-scratch packs are all keyed under the literal "draft" instead of their real `custom-<ts>` id. It's not user-visible and not a live bug (random suffix saves it) — purely the latent semantic footgun the finding describes. Not in the already-fixed list, not in known-deferred. Correctly rated low/inconsistency.
### [LOW] (ux-confusion) Blood pressure burns 2 of the 3 free metric slots, and nothing tells the user — a free biomarker user can't record a complete BP alongside one other marker
- Persona: biomarker-heavy
- Area: src/lib/biomarkers.ts:100,111 (systolic & diastolic are separate metrics); src/app/biomarkers/page.tsx:53-56, 138-140, 157
- Evidence: Systolic (biomarkers.ts:100) and diastolic (biomarkers.ts:111) are two independent BiomarkerDef entries with distinct keys, and the free cap counts `new Set(biomarkers.map(b => b.metric)).size >= getFreeBiomarkers()` = 3 distinct metrics (biomarkers/page.tsx:53-56). A free biomarker-heavy user who tracks weight + HRV + systolic is then locked out of diastolic (the 4th distinct metric) — i.e. they can never log a full blood-pressure reading without spending 2 of their 3 slots on it. The lock copy never explains this: the row subtitle reads 'Premium — upgrade to track more' (page.tsx:157) and the tap toast reads 'Free plan tracks 3 metrics — upgrade to add more' (page.tsx:139). 'BP' is presented as two line items with no hint they're a pair or that they consume two slots.
- Why: Blood pressure is a single clinical reading the persona thinks of as one thing ('120/80'). Splitting it into two capped metrics with no explanation makes the free tier feel arbitrarily stingy and confusing precisely for the user the cap is meant to convert — they hit the wall mid-BP-entry and the copy gives no model for why.
- Fix: Either (a) treat systolic+diastolic as one logical 'Blood Pressure' metric for cap-counting (count the pair as 1 slot, with a combined log row that captures both numbers), or (b) at minimum make the lock copy honest about the pairing (e.g. 'Blood pressure uses two of your free metrics (systolic + diastolic)') so the user understands the cost before they commit a slot.
- Verify: Confirmed open. biomarkers.ts:100-109 (key "systolic") and 111-120 (key "diastolic") are two distinct BiomarkerDef entries. biomarkers/page.tsx:53-56 counts the free cap as `new Set(...map(b=>b.metric)).size >= getFreeBiomarkers()`, and entitlements.ts:20 sets FREE_BIOMARKERS=3 — so a full BP reading burns 2 of 3 free slots. The submit guard (page.tsx:80-87) uses the same per-key distinct logic. The lock subtitle is "Premium — upgrade to track more" (page.tsx:157) and the toast is "Free plan tracks 3 metrics — upgrade to add more" (page.tsx:138-140); a grep of both files shows no copy anywhere indicating the two BP entries are a pair or consume two slots. This is distinct from the shipped "biomarker cap shown up front" fix (the capReached lock at page.tsx:50-56, which works). Severity dropped to low: copy-only, no data/functionality impact, BP is still recordable, and the cap itself functions correctly and is disclosed — only the BP-pairing nuance is unexplained.
### [LOW] (nonsense-but-works) biomarkerBand tags dangerously-LOW 'lower-is-better' readings as green 'Optimal' with no caution
- Persona: biomarker-heavy
- Area: src/lib/biomarkers.ts:159-162 (biomarkerBand, 'lower' branch); rendered as a badge in src/app/biomarkers/page.tsx:173-182
- Evidence: For direction 'lower' the band is purely `value <= optimal → Optimal` (biomarkers.ts:160). There is no low bound, so hypotensive/bradycardic readings render a reassuring green 'Optimal' pill: systolic 70 (optimal 120) → Optimal; diastolic 45 (optimal 80) → Optimal; resting HR 35 (optimal 60) → Optimal. The badge is shown verbatim on the metric row (biomarkers/page.tsx:173-182) and in history (page.tsx:295). targetText only ever says 'Target ≤ X' (biomarkers.ts:172), reinforcing 'lower is always better'.
- Why: The biomarker-heavy persona logs BP and resting HR daily, where a genuinely low value (hypotension, bradycardia) is the opposite of 'optimal'. Flashing green 'Optimal' on those readings is a 'works-but-makes-no-sense' moment that mildly misinforms — and pairs with finding #1 (a low typo lands here too).
- Fix: Give 'lower'-direction markers an optional low-watch edge (or reuse the proposed `min`) so very-low readings render a neutral/caution band ('Low — worth a look') instead of 'Optimal'. Keep it calm and non-clinical, consistent with the page's 'not medical advice' framing.
- Verify: Confirmed against live code. src/lib/biomarkers.ts:159-162: the 'lower' branch is exactly `if (value <= optimal) return Optimal; if (value <= watch) return Good; return Watch` — there is NO lower bound, so dangerously-low readings score green 'Optimal'. The cited markers all use direction:'lower' with the claimed optimals: systolic optimal 120 (line 105), diastolic optimal 80 (line 116), restingHR optimal 60 (line 83). So systolic 70, diastolic 45, restingHR 35 each satisfy value<=optimal and render the green 'Optimal' pill. That pill is shown verbatim on the metric row at src/app/biomarkers/page.tsx:173-182 (bandInfo.label) and in history at page.tsx:293-295 (biomarkerBand(open, h.value)). targetText at biomarkers.ts:172 only ever says `Target ≤ ${optimal}`, with no low-side caveat. The session's 'biomarker plausibility caps' fix only added the `max` ceiling (rejects typo-highs); it did not add a low floor, so this is NOT already fixed. Severity LOW (not raised): the screen is explicitly framed as non-medical self-tracking (page.tsx:208-210 'For your own tracking, not medical advice.'), the actual numeric value is shown beside the badge, and it's a cosmetic/UX 'nonsense-but-works' gap rather than a crash or data-corruption bug — matching the finding's own low rating.
### [LOW] (inconsistency) Reminders header comment claims in-tab + background dedupe via tag, but they use different tags — duplicate notification when app is open at the reminder minute
- Persona: reminders-quiet-hours
- Area: In-tab vs background notification tags (src/components/Reminders.tsx, api/push/send-due)
- Evidence: Reminders.tsx:40-42 comment: "Both can fire on the same minute on rare occasion; the service worker dedupes via `tag` (renotify:false)." But the in-tab notification uses a per-behavior tag `pz-${it.canonicalKey}` (Reminders.tsx:209) while the server push hard-codes `tag: "pz-reminder"` (api/push/send-due/route.ts:127, also the SW default at public/sw.js:188). Different tags => the OS does NOT collapse them. Effect 1 keeps the push subscription active whenever notificationsEnabled regardless of tab focus (Reminders.tsx:52-123), so with the tab open at the fire minute the user can get BOTH the named in-tab notification ("Morning sunlight") and the generic server push ("Reminder — check Today for your next behavior.").
- Why: The de-dup the comment promises does not exist; a power user with the PWA open can see two notifications for the same minute (one specific, one generic), which feels broken and noisy — exactly the opposite of the calm, per-behavior intent.
- Fix: Either align the tags (have the server send a deterministic per-slot tag, or have in-tab also use `pz-reminder` when it's the only generic) so renotify:false actually collapses them, or suppress the in-tab scheduler's overlap, or just correct the comment to state the two paths can co-fire. Tag alignment is the real fix; the comment is currently misleading.
- Verify: Confirmed against live code. The header comment at src/components/Reminders.tsx:40-42 claims the service worker "dedupes via `tag` (renotify:false)", but the two notification paths emit DIFFERENT tags, so the OS (which only collapses same-tag notifications) shows both. In-tab path: Reminders.tsx:209 uses `tag: \`pz-${it.canonicalKey}\`` with the real behavior title (Reminders.tsx:207 `reg.showNotification(it.title, ...)`). Server push: src/app/api/push/send-due/route.ts:127 hard-codes `tag: "pz-reminder"` with generic body "Reminder — check Today for your next behavior." (line 126); the SW default is also `pz-reminder` (public/sw.js:188), and the push handler (sw.js:176-193) calls showNotification unconditionally — no focus/visibility check to suppress the server push when a tab is open. Both paths can fire on the same minute: Effect 1 keeps the push subscription active whenever notificationsEnabled regardless of tab focus (Reminders.tsx:52-123, no document.hidden gate), and Effect 2 explicitly fires at deltaMin===0 to match the server's same-minute match (Reminders.tsx:198-202 comment + `if (deltaMin < 0 || deltaMin > 12*60) continue`). Both compute the identical minute via the same pipeline (applySwaps→shapeTimeline→injectOneOffs→applySnoozes→applyStacks) and the shared resolveReminderMinutes(...) call. So with the PWA open at the fire minute, the user gets two notifications (one specific, one generic). NOT in the ALREADY-FIXED reminders list (that batch covered hard-window clamp + quiet-hours fallback + "honest copy", which did not touch the dedup claim at lines 40-42). Severity low: narrow window (tab open at exact minute) and requires fully-wired push (Supabase service-role + VAPID + cron secret, all env-gated per CLAUDE.md), but the comment's promise is genuinely false against the code.
### [LOW] (nonsense-but-works) Smart-timing learned time has no circular-clock handling — a behavior logged around midnight learns a nonsensical ~noon reminder
- Persona: reminders-quiet-hours
- Area: learnedReminderMinutes median (src/lib/smartReminders.ts)
- Evidence: learnedReminderMinutes (smartReminders.ts:29-40) collects raw minutes-since-midnight (recorded as wall-clock nowMinutesInTz in storage.ts:699) and takes a plain linear sort + median with no circular/modular handling and no outlier rejection. A pre-bed behavior logged some nights at 23:50 (=1430) and some at 00:10 (=10) yields median ≈ 720 (noon). resolveReminderMinutes then clampToWindow's that — which saves STRICT-window behaviors (clamped back into e.g. the wind-down window) — but a windowless evening/bed behavior (recon confirms the live melatonin/coq10 rows ship with NO timeWindow) passes 720 straight through, so Smart timing would nudge a midnight habit at noon.
- Why: This persona explicitly turns on Smart timing for behaviors near their bedtime; the one place a night-owl's data straddles midnight is exactly where the median breaks, and the clamp only rescues hard-window items. The reminder lands at the worst possible time and undermines trust in the feature.
- Fix: Compute the median on the circle (e.g. shift samples that cluster near midnight onto a contiguous range before averaging, or use a circular-mean / mode), or at minimum reject samples whose spread exceeds ~12h as "no stable rhythm" and fall back to the scheduled time. Keep the existing clamp as a second guard.
- Verify: CONFIRMED OPEN. learnedReminderMinutes (smartReminders.ts:36-40) does a plain linear sort + arithmetic median with no circular/modular handling and no outlier rejection. Completion times are stored as raw wall-clock minutes-since-midnight (storage.ts:699 `nowMinutesInTz`), so 23:50=1430 and 00:10=10 are treated as far apart on a linear axis; a midnight-straddling behavior can median toward midday. resolveReminderMinutes (smartReminders.ts:63) only clamps via clampToWindow, and clampToWindow is a no-op when the behavior has no timeWindow (time.ts:244-245 `if (!w) return min`; resolveTimeWindow returns null at time.ts:211-212). The cited behaviors genuinely ship windowless: STRATEGIC_MELATONIN (packs.ts:327-347, anchor:"bed" offsetMin:-50, no timeWindow) and coq10 (packs.ts:1866-1880, no timeWindow); the B() helper is identity (packs.ts:11, `const B = (b)=>b`) so no default window is injected, and the activePacks() heal cannot add a timeWindow the code atom itself lacks (knowledge.ts:184-185 only fills fields where base[f] is defined). Both reminder paths feed the windowless item straight into resolveReminderMinutes (Reminders.tsx:105-111 background, 189-195 in-tab), so a bad learned median passes through unclamped. This is NOT the already-fixed item: the session fix was "hard-window clamp + quiet-hours fallback" (test time-window.test.ts:153-155 even asserts a windowless learned time is left UNTOUCHED), which is precisely the gap here. Severity low is correct: it requires Smart timing ON, >=5 samples (SMART_MIN_SAMPLES, smartReminders.ts:12,35), a windowless bed-anchored behavior, AND a genuine near-even midnight straddle to hit the worst-case ~noon result — the melatonin/night-owl persona is a plausible but narrow vector (the coq10 example is weak since wake+90 never straddles midnight). No circular/outlier/MAD guard exists anywhere in the smart-timing path (grep of smartReminders.ts finds only docstring words).
### [LOW] (ux-confusion) Quiet-hours window with equal start and end is silently inert while the UI presents it as an active do-not-disturb window
- Persona: reminders-quiet-hours
- Area: Quiet hours config vs inQuietHours (src/app/profile/page.tsx, src/lib/time.ts)
- Evidence: inQuietHours returns false when start === end (time.ts:64 `if (s === e) return false;`). The Profile quiet-hours inputs (profile/page.tsx:566-602) accept any two times independently with no validation, so a user can set both to e.g. 22:00. The caption then still reads "No reminders fire between these times — a wind-down window so nothing buzzes overnight." (profile/page.tsx:603-606), but with start===end nothing is suppressed — reminders fire as if quiet hours were off.
- Why: A user who fat-fingers identical times (or sets a 0-length window) believes they've silenced overnight reminders; the app quietly does the opposite while still showing a configured window. Silent disagreement between the displayed setting and behavior.
- Fix: In the Profile handler, detect start===end and either reject it (toast "Start and end can't be the same") or treat a 0-length window as off and update the caption to say so. Optionally surface an inline note when the configured window is degenerate.
- Verify: Confirmed open. inQuietHours returns false when start===end (src/lib/time.ts:64 `if (s === e) return false;`), explicitly verified by test src/lib/__tests__/clock-blocks.test.ts:190 ({start:"22:00",end:"22:00"} → false). This is the live gate for the reminder pipeline via resolveReminderMinutes (src/lib/smartReminders.ts:64,67), used by both reminder paths in Reminders.tsx. The Profile UI exposes two fully independent <input type="time"> fields (src/app/profile/page.tsx:568-600) whose onChange handlers do NO cross-field validation — each writes its own value while preserving the other, so a user can set both to the same time (start=22:00 auto-fills end=07:00, then edit end→22:00). The caption (src/app/profile/page.tsx:603-606) unconditionally states "No reminders fire between these times — a wind-down window so nothing buzzes overnight," yet with start===end nothing is suppressed and reminders fire normally. Genuine silent disagreement between displayed setting and behavior, reachable via the live UI. Not in the already-fixed list (the reminders fix covered hard-window clamp / quiet-hours fallback / smart-timing copy, not the degenerate equal-times window) and not in known-deferred. Low severity is correct: requires identical fat-fingered times, failure mode is unwanted reminders firing (no data loss/safety impact), and it sits behind notifications-enabled. Note the code comment at src/lib/time.ts:55 documents start===end as intentional "no quiet hours" — so the math is intended; the UI copy failing to reflect it is the real, open UX defect.
### [LOW] (ux-confusion) Signed-in second tab's check-in is correctly NOT clobbered, but the two tabs stay visibly divergent until an unrelated refocus (no live cross-tab reconcile)
- Persona: two-tabs-same-browser
- Area: src/lib/datasource.ts (SupabaseDataSource.save concurrency guard + notify) + src/hooks/useAppState.ts
- Evidence: For signed-in users the optimistic-concurrency guard DOES protect cross-tab against silent loss: each tab is a separate JS realm with its own module-level `lastCloudUpdatedAt` (datasource.ts:76) but both share the same cloud row + auth session. Trace: both tabs load → lastCloudUpdatedAt=R0 each. Tab A saves X → head re-read R0, `R0>R0` false → upsert → server stamp R1, tab A's lastCloudUpdatedAt=R1. Tab B (still R0, stale memory) saves Y → head re-read returns R1 → guard `head.updated_at (R1) > lastCloudUpdatedAt (R0)` is TRUE (datasource.ts:839-843) → it ABORTS the upsert (returns at :846) without clobbering, and tab B's local edit Y was already written to localStorage (:801) + markPendingSync (:804). On tab B's NEXT load(), `dirty=hasPendingSync()` is true → chooseCloudLoad takes the local-preferring mergeStates path (:531, :756-772) → Tab A's X (in cloud) and Tab B's Y (in local) both survive. GOOD — no data loss. BUT the abort path only fires `notify()` (datasource.ts:845), an in-page CustomEvent that the OTHER tab cannot receive, and does NOT re-pull or push. So after the interleaved saves: Tab B still shows Y-but-not-X, Tab A still shows X-but-not-Y, on screen, with neither reconciled — until some unrelated `focus`/`visibilitychange`/pull-to-refresh triggers a load() (useAppState.ts:176-178). Tab A in particular won't see Y until Tab B successfully reconciles+pushes (a later load) AND Tab A then refocuses.
- Why: For this two-tabs persona the signed-in experience is 'I checked it in the other tab but this tab still shows it undone' — a confusing staleness that looks like the app dropped the edit (even though the data is safe and will reconcile). Same root cause as finding #1: no `storage`-event/BroadcastChannel listener means tabs never learn of each other live; the cross-device guard saves the data but does nothing for live UI coherence within one browser.
- Fix: The same `storage`-event listener proposed in finding #1 fixes this too: when tab B writes the shared STORAGE_KEY, tab A's `storage` handler calls sync()→load(), which (clean → cloud-wins, dirty → merge) pulls the reconciled state and both tabs converge without a manual refocus. Optionally, after the guard aborts (datasource.ts:844-846), proactively trigger a local reconcile (re-run load() so the aborting tab immediately merges in the other tab's cloud edit) instead of only emitting an in-page notify the peer can't hear.
- Verify: CONFIRMED OPEN. Mechanism verified end-to-end: lastCloudUpdatedAt is module-level (datasource.ts:76), so each tab has its own baseline. The optimistic-concurrency guard at datasource.ts:839-843 (head.updated_at > lastCloudUpdatedAt) aborts BEFORE the upsert at :846 — Tab A's write is never clobbered, and Tab B's edit was already saved to localStorage (:801) + markPendingSync (:804), so Tab B's next load() routes through chooseCloudLoad with dirty=hasPendingSync() (:756,:763) → local-preferring mergeStates (:531,:510-513). Data is safe (finding agrees). The real defect: the abort path's ONLY side effect is notify() at :845, which is window.dispatchEvent(new CustomEvent(STATE_EVENT)) (:811-814) — a same-document event that cannot cross tabs. Grep of all of src/ found NO storage-event listener, NO BroadcastChannel, NO postMessage relay. The sole STATE_EVENT listener (useAppState.ts:176) only re-pulls alongside focus/visibilitychange handlers (:177-178). So after interleaved saves both tabs stay visibly divergent until an unrelated focus/visibility/reload fires load(). Not covered by this session's fixes (those addressed data-safety only). Downgraded to LOW: data self-heals, narrow two-tabs-same-browser persona, and divergence clears on the very next focus event (which is fired simply by clicking back into a tab) — transient cosmetic staleness, no loss. Cited line numbers in the evidence are a few off but point to the correct code.
### [LOW] (ux-confusion) Edit-mode move-menu is role='menu' but has no arrow-key navigation or aria-orientation (Tab-only)
- Persona: a11y-authed-surfaces
- Area: src/app/today/page.tsx:2380-2456
- Evidence: The popover is `role='menu'` (2382) containing `role='menuitem'` buttons (2418). The trigger correctly has aria-haspopup='menu' + aria-expanded (2345-2348). But there is no keydown handler for ArrowUp/ArrowDown/Home/End on the menu, no roving tabindex, and no aria-orientation — items are reached only by Tab. Per WAI-ARIA, role='menu' implies arrow-key navigation; a screen reader announcing 'menu' sets that expectation, which isn't met. (The items are real <button>s so they remain operable.)
- Why: Minor: the control still works via Tab/Enter and the surrounding focus is fine, but the menu semantics over-promise a keyboard model that isn't implemented — mild confusion for AT users in edit mode. Not a blocker; flagged for completeness since the task calls out tab/menu controls.
- Fix: Either implement arrow-key roving focus + aria-orientation='vertical' on the role='menu', or drop role='menu'/'menuitem' and present it as a labelled group of buttons (`role='group' aria-label='Move to'`) which matches the actual Tab-based interaction.
- Verify: Confirmed by reading src/app/today/page.tsx:2380-2456. The popover div is role="menu" (2382) with role="menuitem" buttons (2418); the trigger correctly sets aria-haspopup="menu" (2345) + aria-expanded (2346-2348). The menu container (2381-2393) has only onClick + onPointerDown handlers — no onKeyDown — and no aria-orientation; the menuitem buttons (2416-2445) carry no tabIndex/roving-tabindex. A file-wide grep for onKeyDown/aria-orientation/tabIndex/ArrowUp/ArrowDown returns hits ONLY at 2573-2578, which the inline comment (2568-2574) and code (role="button" row body, Enter/Space-to-complete) confirm belong to an unrelated row element, not this menu. So items are reachable by Tab only, while the menu/menuitem roles imply an arrow-key model that is not implemented. Real and open. Severity LOW: items are genuine <button>s, so the control is fully operable via Tab+Enter — impact is over-promised semantics / mild AT expectation mismatch, not a functional block. Not in the already-fixed list (which only covers move-menu window-forbidden disabling, present and distinct at 2409-2419) nor the known-deferred list.
### [LOW] (dead-option) Custom day-block label has no length cap — a long label overflows the BulkMoveSheet grid buttons and the Today block headers on a phone
- Persona: iphone-se-onboarding-sheets
- Area: Profile block-label input → BulkMoveSheet / Today block headers — src/app/profile/page.tsx + src/components/today/BulkMoveSheet.tsx + src/app/today/page.tsx
- Evidence: The Profile NAME field is capped (`maxLength={40}` at src/app/profile/page.tsx:145) but the three custom block-label inputs (profile/page.tsx:265-278, the shared `input` class, no maxLength) are uncapped, and storage.ts has no blockLabels normalization/trim (grep for blockLabels in storage.ts returns nothing). The label flows untruncated into (a) BulkMoveSheet buttons — BulkMoveSheet.tsx:44-52 render `{blockLabel(b, blockLabels)}` with no `truncate`/`break-words`, and (b) the Today block header Eyebrow (today/page.tsx:2177-2180), also untruncated. Headless measurement at a 320px viewport (Playwright, system font that matches the app's `--font-sans` SF Pro stack): a 24-char single-word label ("Predawnpreworkoutroutine") in the `grid-cols-2 minmax(0,1fr)` BulkMoveSheet overflows its own button — button clientWidth=132px vs scrollWidth=176px (text spills ~44px past the rounded chrome). The grid container itself doesn't push document width (no horizontal page scroll), but the visible button background is overrun. In the Today header (normal flow, not a fixed grid) the same long word extends the header line and can push the "NOW" pill / cause page-level horizontal scroll. (Default labels Morning/Afternoon/Evening/Anytime all fit cleanly at 320px — verified.)
- Why: The app already decided long free-text needs a cap (the name field) but left the block-label fields — which feed more layout-sensitive surfaces (a 2-col sheet grid and a header with an adjacent pill) — uncapped. A user who renames "Morning" to something long gets visibly broken buttons/headers specifically on the narrow viewport this persona uses, while the guard that would have prevented it exists 120 lines up in the same file.
- Fix: Add `maxLength={24}` (or similar) to the three block-label inputs at profile/page.tsx:265-278 to match the existing name-field guard, AND add `truncate min-w-0` to the BulkMoveSheet button (BulkMoveSheet.tsx:44-52) and the Today block-header Eyebrow content (today/page.tsx:2177-2180) so even a pre-existing long label degrades to an ellipsis instead of overflowing.
- Verify: Confirmed open. Name field is capped (profile/page.tsx:145, maxLength={40}) but the three block-label inputs (profile/page.tsx:265-278) use only the shared `input` style class (defined profile/page.tsx:80-81, styling only) with NO maxLength prop — uncapped. storage.ts has zero blockLabels handling (grep returns nothing); the only processing is engine.ts:1886 `labels?.[b]?.trim()` which trims whitespace but does not length-cap. The label flows untruncated into both surfaces: (a) BulkMoveSheet.tsx:50 renders {blockLabel(...)} in a button whose className (line 47) has no truncate/break-words/min-w-0, inside a `grid grid-cols-2` (line 42); (b) today/page.tsx:2179 renders it in <Eyebrow> (ui/index.tsx:56-60, a bare <p className="t-eyebrow"> with no truncation) in a flex span next to the NOW pill (lines 2181-2191). So a long single-word custom label will overflow on a narrow viewport — the asymmetry the finding describes is real. Two caveats: only morning/afternoon/evening are user-editable (anytime in BulkMoveSheet's BLOCKS is not renamable from the UI, so that button can't overflow via user input), and I did not independently reproduce the author's exact pixel measurements (132 vs 176px) — but the CSS structure matches the claim. Low/cosmetic, not in KNOWN-DEFERRED, not touched by the a9f2115..7fd3724 fixes.